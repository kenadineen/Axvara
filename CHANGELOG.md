# CHANGELOG — AXVARA

- 2026-09-25 — **Halaman pesanan produk kirim otomatis: detail muncul beberapa detik setelah bayar tanpa refresh, dan muat ulang tetap membuka otomatis (laporan owner, uji CO Canva Invite 1 Bulan `AXV-20260925-F4D93D74`).** Jejak D1 produksi: lunas 13:55:57 UTC, item `shared` terkirim + email 13:56:01, tetapi halaman menampilkan "Estimasi 5–15 menit" (teks produk WR) dan baru memeriksa lagi 20 dtk kemudian; verifikasi otomatis pertama berjalan 13:56:23 (token terbit, tidak pernah dipakai) dan panel sudah menghapus kontak checkout, sehingga setelah refresh muncul form kosong. Perubahan: (1) `GET /api/orders?code=` menambah flag `instant_delivery` (SEMUA baris varian non-WR `shared`/`unique`; CTE `lines` di query gabungan yang sama, tetap 2 statement). (2) `/pesanan/[code]`: order instan yang belum selesai dipoll 2 dtk ×5 lalu 5 dtk ×4 (±30 dtk, 9 permintaan, di bawah `orders:lookup` 20/mnt), lalu jadwal lama 20 dtk; teks "Mengirim produkmu…" + spinner, lewat ±30 dtk "butuh waktu lebih lama"; kirim otomatis yang jatuh ke `manual_required` → "sedang disiapkan admin" + plafon 12 jam (asumsi: plafon sama dengan antrean) dan hanya 3 poll; `fulfillment_status='delivered'` tanpa detail → "Produk sudah dikirim" (dulu "sedang diproses" selamanya, mis. Kirim ke pembeli tanpa isi); estimasi 5–15 menit kini hanya untuk produk WR. (3) `WrCredentialsPanel`: kontak checkout tidak dihapus saat dipakai (bertahan sampai tab ditutup); token yang ditolak (401/403) dibuang lalu jatuh ke kontak checkout; 429/5xx tidak membuang token. SQL persis diuji read-only di D1 produksi: 4 order Canva terkirim instant=1; 3 Canva `manual_required` instant=1 (kini "disiapkan admin", dulu 5–15 menit); Meitu antrean queued=1; Apple Music WR restock tetap 5–15 menit. Docs: PRD FR-S20/FR-S32, ARCHITECTURE (tabel API, Storefront WR, kelas pengiriman, teks pembeli non-WR), DESIGN (blok pasca-pembayaran), README — `src/app/api/orders/route.ts`, `src/app/pesanan/[code]/page.tsx`, `src/components/storefront/WrCredentialsPanel.tsx`, `src/app/checkout/page.tsx` (komentar) — (verifikasi: **1599/1599 test [129 file] hijau** (+12: 4 integrasi D1 flag instan [stok sendiri; WR/antrean/tanpa varian/varian hilang; items kosong + pending; tetap 2 query], 5 perilaku halaman dengan timer palsu [detail muncul pada poll 2 dtk; jadwal 2×5/5×4/20 + teks lebih lama; manual_required 3 poll; delivered; WR tanpa poll cepat], 3 panel [muat ulang di tengah verifikasi; token 403 → kontak; token 429 tidak dibuang]); 2 test lama diperbarui (kontak kini tetap tersimpan; asersi sumber polling); tsc bersih; eslint tanpa peringatan baru; **mutation-test 16 putaran, semua MEMERAH**; dev `GET /` 200 + CSS 200 + `/pesanan/[code]` 200; browser headless agent-browser dengan respons API tiruan (data uji): detail tampil 1,6 dtk setelah server siap tanpa reload, muat ulang membuka otomatis, 5 keadaan teks dicek desktop + mobile 390 px; Obscura tidak tersedia di sandbox)

- 2026-09-25 — **Mobile: pilih varian lewat panel + bottom nav Beranda · Keranjang · Pesanan · Bantuan (keputusan owner).** (1) **PDP mobile**: dulu baris "Tersedia N varian" membuka panel yang langsung ke checkout dan pilihannya hilang saat ditutup, sehingga S&K mobile selalu milik varian pertama (dicek di produksi, Claude Pro). Kini baris "Varian" menampilkan pilihan aktif (label, harga, badge, "Ganti ›") dan membuka `QuickVariantModal` mode **select**: ketuk varian = pilih untuk halaman lalu panel menutup, tanpa stepper/CTA; harga, badge, stok, stepper Jumlah, S&K, cara aktivasi, dan Beli Sekarang (langsung ke checkout) ikut varian itu; menutup panel tidak membuang pilihan; ketukan di panel beli cepat (sticky bar saat belum memilih) juga menjadi pilihan halaman, pilihan otomatis tidak. Judul S&K mobile diberi tautan "Ganti varian" (di luar tombol lipat) hanya bila S&K/cara aktivasi antar varian berbeda (24 dari 29 produk multi-varian di produksi). Panel memakai varian dari halaman (tanpa fetch ulang); lebih dari 6 varian → baris ringkas satu kolom (akun-gsuite 7 varian muat satu layar), lebih dari 10 → kotak cari; varian terpilih digulir ke pandangan. Desktop tidak berubah. (2) **Bottom nav** 4 tab: Beranda, Keranjang (buka drawer, badge jumlah baris), Pesanan (`/lacak-pesanan`, aktif juga di `/pesanan/*`, titik Gold bila ada pesanan perangkat ini belum dibayar ≤75 menit), Bantuan (panel `HelpSheet`: WA Admin, Telegram, Cara Order, Garansi & Replace, Artikel; tetap ada di footer). Dulu Beranda + "Katalog" (`/#katalog`, halaman sama, tak pernah menyala) + Artikel + Cara Order + Lacak. Badge/titik baru dirender setelah mount (anti beda HTML server). Ikon dekoratif `aria-hidden` (dulu nama tombol terbaca "chat Bantuan"). (3) **Tab Pesanan**: `/lacak-pesanan` menampilkan "Pesanan di perangkat ini" (`DeviceOrders`, maks 5 pesanan 30 hari) dari salinan lokal checkout (tanpa WA/email) dengan status dari `GET /api/orders?code=`, tombol Bayar sekarang / Lihat pesanan, dan tombol sembunyikan; batal/kedaluwarsa tidak dicek ulang, 404 dicatat `missing`. Pengingat melayang disembunyikan di halaman ini (daftar sudah memuat tombol Bayar). (4) Helper `src/lib/local-orders.ts` (baca/catat/hapus + event `axvara-orders-changed`) dipakai pengingat, titik nav, dan daftar; `/pesanan/[code]` mencatat status akhir ke salinan lokal agar titik hilang setelah bayar. Docs: DESIGN (§5.5 PDP mobile, QuickVariantModal daftar panjang, §5.7e bottom nav, §5.7d), ARCHITECTURE (struktur komponen + lib), README, PRD FR-S31 — `src/components/storefront/{QuickVariantModal,MobileBottomNav,HelpSheet,DeviceOrders,ProductCopy,PendingOrderReminder}.tsx`, `src/app/produk/[slug]/product-detail-client.tsx`, `src/app/lacak-pesanan/lacak-pesanan-client.tsx`, `src/app/pesanan/[code]/page.tsx`, `src/lib/local-orders.ts` — (verifikasi: **1587/1587 test [128 file] hijau** (+22 test: 5 perilaku PDP sungguhan [panel pilih tanpa auto-pilih/CTA + tanpa fetch ulang, ketuk → harga/badge/S&K/tombol beli ikut + checkout `variant=23`, Ganti varian membuka panel dengan pilihan saat ini + Tutup tidak membuang, beli cepat menyinkronkan ketukan, Ganti varian tersembunyi bila S&K sama], 7 panel [mode select, pilihan halaman, callback hanya dari ketukan, kartu katalog tetap fetch, ≤6 kartu, 7 baris ringkas, >10 cari + tidak ada hasil], 9 nav/pesanan/helper [4 tab, Keranjang buka drawer + badge, titik hanya pending ≤75 menit + hilang saat dicatat, Bantuan 5 tautan + Escape, aktif per rute + tersembunyi di PDP/checkout/admin, status server + Bayar/Lihat + 404 + batal tanpa fetch, maks 5 + sembunyikan, kosong, `freshPendingCodes`], 1 halaman pesanan mencatat status lokal); 1 test pengingat diperluas ke `/lacak-pesanan`; tsc bersih; `next build` sukses; eslint tanpa peringatan baru; **mutation-test 16 putaran, semua MEMERAH** (select tidak menutup, ketukan tidak ke halaman, select auto-pilih, varian halaman diabaikan, tanpa baris ringkas, tanpa cari, baris varian kembali ke checkout, Ganti varian selalu tampil, Keranjang tanpa drawer, titik tanpa batas 75 menit, nav tuli perubahan lokal, batal dicek ulang, 404 tak dicatat, Cara Order hilang, nav tampil di PDP, halaman pesanan tak mencatat); dev `GET / 200`, CSS 200; headless browser 390px dengan data uji sintetis lewat halaman pratinjau sementara (tidak di-commit): baris varian sebelum/sesudah memilih, panel select, S&K + Ganti varian, 7 varian ringkas, bottom nav + badge + titik, panel Bantuan (keterangan dipendekkan setelah terpotong di 390px), tab Pesanan)

- 2026-09-25 — **Detail akun digital + lacak pesanan bisa diverifikasi dengan No. WA ATAU email checkout (permintaan owner).** Dulu panel "Detail Akun Digital" dan `/lacak-pesanan` hanya menerima nomor WA, padahal semua order web baru punya email. Perubahan: (1) helper `src/lib/order-contact.ts` (`parseBuyerContact`, `emailMatches`): input berisi "@" = email, dicocokkan penuh tanpa beda huruf besar/kecil lewat `constantTimeEqual`, dan order tanpa email tidak pernah cocok; (2) `POST /api/orders/lookup` menerima field `contact` (field `wa` lama dan tautan `?wa=` tetap jalan, tautan baru `?email=`), dengan 404 generik yang sama untuk kode salah, WA salah, maupun email salah (anti enumerasi); (3) `POST /api/orders/[code]/credentials` menerima email di samping aturan 6 digit terakhir WA; (4) satu kolom "No. WA atau email checkout" (keyboard email) di panel dan form lacak, baris "Terdaftar: 62812\*\*\*\*7890 · r\*\*\*@gmail.com" (kontak tersamar yang memang sudah tampil di halaman pesanan), riwayat lacak menandai WA/Email; seluruh teks halaman lacak (kalimat pembuka, judul kartu, langkah 2, FAQ, catatan server, meta description) ikut menyebut email; (5) setelah checkout di tab yang sama panel membuka otomatis sekali: kontak disimpan di `sessionStorage` (hilang saat tab ditutup, bukan localStorage) dengan `checkoutContactKey` dan dihapus begitu dipakai. Docs: ARCHITECTURE (route table, struktur, panel storefront, keamanan WR), README, PRD FR-S11a, DESIGN (panel + privasi lacak) — `src/lib/order-contact.ts`, `src/app/api/orders/lookup/route.ts`, `src/app/api/orders/[code]/credentials/route.ts`, `src/components/storefront/WrCredentialsPanel.tsx`, `src/app/lacak-pesanan/lacak-pesanan-client.tsx`, `src/app/pesanan/[code]/page.tsx`, `src/app/checkout/page.tsx` — (verifikasi: **1565/1565 test [125 file] hijau** (+12 test: 7 integrasi D1 [lookup email cocok walau beda huruf/spasi, email salah / order tanpa email / WA salah → 404 dengan pesan identik, WA via `contact` dan `wa` lama + format buruk 400, credentials email → detail + token dan email lain 403, order tanpa email tidak bisa dibuka dengan email sementara WA 6 digit tetap jalan, `parseBuyerContact`], 5 perilaku jsdom [buka otomatis dari checkout lalu kunci dihapus + token tersimpan, email + hint + pesan gagal, checkout menyimpan di sessionStorage, form lacak mengirim email sebagai `contact`, isian bukan WA/email ditolak, tautan `?email=`]; 2 test lama diperbarui untuk label/prop baru), tsc bersih, eslint tanpa peringatan baru; **mutation-test 9 putaran, semua MEMERAH** (email tidak di-lowercase, order tanpa email dianggap cocok, credentials terima email apa pun, lookup abaikan `contact`, panel kirim `wa` lama, kontak checkout tidak dihapus, form lacak membuang huruf, checkout tidak menyimpan kontak, tautan `?email=` diabaikan); dev `GET / 200`, CSS 200; headless browser 390px dengan data uji sintetis: panel Detail Akun (label baru, keyboard email, baris Terdaftar) dan form lacak berisi email)

- 2026-09-25 — **Notif admin order web: "Lunas — Web" sampai ke Axvara_Notif (laporan owner: order web tidak pernah muncul, order Telegram muncul).** Notif lama "Order Baru — Web" di `POST /api/orders` membangun tombol dengan `SITE_URL ?? fallback`; SITE_URL kosong di worker (ARCHITECTURE §16.4) sehingga tombol "Panel Admin" menjadi URL relatif dan Telegram menolak SELURUH pesan, sementara hasil kirim diabaikan (tanpa log/penanda/retry). Notif Telegram/WA memakai `||` sehingga tetap jalan. Koreksi analisis sebelumnya: "20 order web tanpa penanda notif" bukan bukti, karena jalur web memang tidak pernah menulis penanda itu. Perbaikan: (1) keputusan owner **hanya notif saat lunas**, "Order Baru — Web" dihapus; (2) `notifyWebPaidAdmin` dipanggil `ensureFulfillmentForPaidOrder` setelah upaya kirim pertama dan memuat status kirim (`summarizeWebDelivery`: terkirim otomatis / perlu Kirim ke pembeli / sudah diserahkan / sedang dikirim / diproses WR), menggantikan ping serah terima terpisah (satu pesan per order); (3) sekali per order lewat `buyer_notice_log` kunci `admin-paid:<kode>`, kolom `error` menyimpan alasan penolakan Telegram; (4) URL tombol lewat `siteOrigin()` baru (`src/lib/site-url.ts`), tombol WA hanya untuk nomor `62…` valid, 400 Bad Request → kirim ulang tanpa tombol, timeout tidak diulang seketika (anti dobel); (5) cron `operations` fase notify mengulang order web lunas ≤6 jam tanpa ledger `sent` (`WEB_PAID_ADMIN_PENDING_WHERE`, flag `paidAdminWeb`; order lama tidak dibanjiri); (6) setup webhook Telegram ikut memakai `siteOrigin()` (bug `??` yang sama). Docs: ARCHITECTURE (§13 subbagian baru + §16.4), README, PRD FR-S30 — `src/lib/telegram/{order-notifications,keyboards}.ts`, `src/lib/telegram/messages/admin.ts`, `src/lib/site-url.ts`, `src/lib/fulfillment/delivery/ensure.ts`, `src/app/api/orders/route.ts`, `src/app/api/cron/operations/route.ts`, `src/app/api/admin/telegram/setup/route.ts` — (verifikasi: **1553/1553 test [123 file] hijau** (+8 test `tests/web-paid-admin-notif.integration.test.ts` pada D1 nyata + route cron sungguhan: SITE_URL kosong tetap URL absolut + sekali per order, 400 → kirim ulang tanpa tombol + alasan tercatat, timeout → tidak dobel lalu cron mengulang sekali, order >6 jam tidak dikirim, route order tanpa notif Order Baru, ringkasan status, tombol WA hanya nomor valid, `siteOrigin` 7 kasus; 1 test PR sebelumnya diperbarui karena order terkirim otomatis kini juga dinotif), tsc bersih, eslint file tersentuh bersih; **mutation-test 10 putaran, semua MEMERAH** (`??` lama, tanpa kirim ulang, kirim ulang semua kegagalan, cron tanpa flag web, tanpa jendela 6 jam, tanpa klaim idempoten, notif Order Baru kembali, serah terima dihitung otomatis, tombol WA tanpa validasi, ensure tidak memanggil notif); satu test sempat gagal di baseline karena kasus ujinya keliru (nomor yang justru valid), sudah diganti lalu mutasinya diulang; dev `GET / 200`, CSS 200. Belum teruji: pengiriman sungguhan ke grup Telegram produksi; nilai SITE_URL produksi tidak bisa dibaca (secret))

- 2026-09-25 — Pasang file verifikasi Google Search Console `google0ff9ac31de94bad3.html` (isi `google-site-verification: google0ff9ac31de94bad3.html`, 53 byte) di `public/` agar tersaji di `https://axvara.tech/google0ff9ac31de94bad3.html`; file statis public lolos middleware/CSP (hanya header baca, tanpa blokir GET) dan wajib dipertahankan walau verifikasi sudah sukses — public/google0ff9ac31de94bad3.html, docs/SEO-GEO-PLAN.md — (verifikasi: dev GET / 200 + CSS 200, GET /google0ff9ac31de94bad3.html 200 isi identik byte-per-byte vs file GSC, robots.txt allow + sitemap axvara.tech)

- 2026-09-25 — **Produk non-WR untuk pembeli web benar-benar terkirim + tombol "Kirim ke pembeli" membawa isi + semua email pembeli bermerek (laporan owner).** Temuan read-only D1 produksi: 100% item non-WR kanal web berakhir `manual_required` (3 item Canva Invite 1 Bulan `shared` padahal pesan bersamanya sudah terisi), karena `send.ts` memaksa SEMUA item web ke antrean admin (`web_channel_requires_manual_handover`), dari masa sebelum email pembeli wajib. Akibatnya badge "Kirim otomatis" tidak pernah benar untuk web, tombol "Serahkan manual" hanya mengirim kabar tanpa isi (produk tetap harus dikirim lewat WA), dan admin tidak dikabari saat order web lunas. Perbaikan: (1) **Kirim otomatis lewat email**: item web `shared`/`unique` dikirim `sendWebDeliveryEmail` dengan email **"Pesanan Siap"** yang sekaligus memuat tanda terima pembayaran (SATU email, keputusan owner); idempoten per item (`email:fulfillment-item:<id>` di `buyer_notice_log`); Resend gagal → retry 1/5/15/60 menit lalu alarm admin + kabar pembeli; Made By Order → antrean admin; order tanpa email valid → `web_no_buyer_email`. (2) **Tanda terima terpisah** dikirim SETELAH upaya kirim pertama dan dilewati bila semua baris sudah terkirim otomatis (Made By Order, pesanan campuran, dan kirim tertunda tetap menerimanya). (3) **Migrasi 0043**: `fulfillment_items.delivered_ciphertext/iv` (salinan terenkripsi isi yang dikirim; ciphertext sumber disalin untuk shared/unique) + `product_variants.handover_template` (milik admin). (4) **"Kirim ke pembeli"** menggantikan "Serahkan manual": dialog berisi kolom "Detail untuk pembeli" per item yang terisi dari template varian (`{email}` `{nama}` `{kode}` `{produk}` diisi di server) + catatan internal terpisah; isi dienkripsi di UPDATE yang sama dengan flip `delivered`, lalu dikirim lewat email "Pesanan Siap" (web) atau DM Telegram (di-escape), dan **tidak pernah** lewat outbox WA (teks polos, bot mati). Template disunting di panel varian Made By Order (`POST /api/admin/fulfillment` `set_handover_template`). (5) **Halaman pesanan** membuka isi non-WR setelah verifikasi 6 digit WA/token (label baris pesanan); `credentials_ready` + `issueCredentialToken` ikut menghitungnya; token GET kini diverifikasi eksplisit. (6) **Ping Telegram admin** saat order web lunas punya item yang harus diserahkan (sekali per order, `admin-handover:<kode>`). (7) **Semua email pembeli** (tanda terima, diserahkan, bukti ditolak, menunggu Hook, pengiriman tertunda, pengingat QRIS) memakai shell bermerek `renderBrandedNotice`, bukan HTML polos. (8) Teks: blok "Pengiriman Produk" menyebut email (WA hanya untuk order lama tanpa email); PDP menampilkan "Tergantung varian" untuk varian campuran sebelum dipilih (dulu Canva tampil "Kirim otomatis" walau 2 dari 3 variannya Made By Order). Item `manual_required` lama tidak dikirim ulang otomatis. Docs: ARCHITECTURE (§13 subbagian baru, tabel skema + route), README, PRD FR-S29, DESIGN §5.x email — `src/lib/fulfillment/delivery/{send,ensure,handover,buyer-email}.ts`, `src/lib/fulfillment/handover-template.ts`, `src/lib/notify-buyer.ts`, `src/lib/warung-rebahan/email-forward.ts`, `src/lib/telegram/order-notifications.ts`, `src/app/api/admin/orders/[code]/handover`, `src/app/api/admin/fulfillment`, `src/app/api/orders/[code]/credentials`, `src/components/admin/{OrdersManager,sections/ProductVariantRows}.tsx`, `drizzle/migrations/0043_nonwr_delivery.sql` — (verifikasi: **1545/1545 test [122 file] hijau** (+21 test: 7 integrasi D1 jalur bayar asli [shared/unique terkirim + satu email + salinan terenkripsi, Made By Order + tanda terima + ping sekali, campuran, tanpa email, Resend gagal lalu pulih tanpa email ganda, ledger `sent` tidak dikirim ulang], 7 integrasi route [template terisi, isi ter-escape + terenkripsi, tanpa isi, DM Telegram, TIDAK ke outbox WA, akses isi hanya dengan WA/token benar, API template], 2 perilaku dialog admin sungguhan, 5 template email/PDP); 5 test lama yang mengunci perilaku lama diperbarui (tombol, teks WA, `web_no_buyer_email`); tsc bersih; `next build` sukses (percobaan pertama gagal di unduhan Google Fonts `next/font`, lalu sukses saat diulang tanpa perubahan kode); **mutation-test 11 putaran, semua MEMERAH di test yang tepat** (guard web lama, tanda terima selalu, isi boleh ke outbox WA, token tak diverifikasi, detail tak di-escape, isi serah terima tak disimpan, salinan shared tak disimpan, PDP campuran diabaikan, ping tanpa idempoten, dialog tanpa buyer_message, ledger `sent` diabaikan); headless browser dengan data uji sintetis: email "Pesanan Siap" + "Pembayaran Diterima" dirender dari template asli, dialog "Kirim ke pembeli" dan editor template varian di panel admin dev (API di-mock), blok "Pengiriman Produk" di `/pesanan/[code]` menyebut email)

- 2026-09-25 — **Fix centang "Wajib email pembeli" mati lagi setelah refresh (laporan owner).** Nilai sebenarnya TERSIMPAN (D1 produksi read-only: Canva `require_email=1`, `updated_at` 2026-09-24 17:36 UTC = saat owner menyimpan), tetapi editor tidak pernah membacanya: daftar produk admin (`GET /api/products`) tidak membawa kolom ini dan `openEdit` tidak mengambilnya dari detail produk, sehingga centang selalu tampil mati. Akibat yang lebih berbahaya: Simpan berikutnya mengirim `requireEmail:false` dan diam-diam menulis 0 (bot Telegram lalu berhenti meminta email sebelum bayar untuk Canva; checkout web tetap aman karena selalu mewajibkan email). Perbaikan: (1) `useProductManager.openEdit` membaca `requireEmail` dari `GET /api/products/:id`; (2) Simpan hanya mengirim `requireEmail` bila nilainya sudah termuat, jadi detail yang gagal dibaca tidak lagi menimpa kolom; (3) `POST /api/products` kini menerima + menyimpan `requireEmail` (dulu produk baru selalu 0 walau dicentang; kolom ditambah di akhir INSERT supaya adaptor dev in-memory yang membaca parameter berdasarkan posisi tidak bergeser). Docs: ARCHITECTURE (bagian email wajib), PRD FR-S19 — `src/components/admin/useProductManager.ts`, `src/app/api/products/route.ts`, `tests/product-require-email.{behavior,integration}.test.*` — (verifikasi: **1524/1524 test [118 file] hijau** (+4 test perilaku hook sungguhan: centang termuat dari detail + tidak bikin form "belum disimpan", mematikan centang terkirim false, detail gagal = kunci tidak dikirim, produk baru terkirim true; +3 test integrasi D1: POST menyimpan 1/0, PUT tanpa kunci tidak menyentuh kolom + GET detail membacanya, PUT false mematikan), tsc bersih; **mutation-test 3 putaran, semua MEMERAH di test yang tepat** (editor tidak membaca detail, payload kembali `=== true`, POST versi lama); dev `GET / 200`, CSS 200; headless browser di panel admin dengan API produk di-mock `requireEmail=true` (data uji sintetis): kode lama menampilkan centang MATI, kode baru MENYALA di tab Varian)

- 2026-09-24 — **Editor S&K + cara aktivasi pindah ke tab sendiri "Deskripsi & S&K" (permintaan owner: tab Varian terlalu penuh).** Editor produk kini 4 tab: Produk, Varian, **Deskripsi & S&K**, Foto. Tab baru (`sections/ProductCopyTab.tsx`) berisi kolom Deskripsi (+ Deskripsi khusus untuk produk WR) yang dipindah dari tab Produk, lalu daftar varian dengan panel S&K + cara aktivasi per varian (dipindah dari baris varian; `ProductVariantRows.tsx` kembali persis seperti `main`). Panel diberi judul nama varian (+ durasi bila label WR sama, mis. "Premium · 90 Hari"), penanda "Nonaktif", dan juga tersedia untuk produk tanpa varian (varian default). Tab menampilkan pill gold berisi jumlah varian aktif yang S&K-nya perlu ditinjau (entri dimuat saat modal dibuka; sebelum termuat memakai `copyReview` dari daftar). Aksesibilitas tab dibetulkan sekalian: panel `role=tabpanel` + atribut `hidden` (dulu kelas CSS saja, sehingga pembaca layar tetap membaca semua tab), `aria-controls`, dan label kolom deskripsi memakai `htmlFor`/`aria-describedby` (dulu nama kolom ikut memuat teks petunjuk). Di layar 390px keempat tab muat tanpa terpotong (`px-2.5 sm:px-4`, tablist `overflow-x-auto` sebagai cadangan), dan ganti tab menggulir badan modal ke atas (dulu tab baru terbuka di tengah karena scroll dibagi semua tab). Banner WR dan petunjuk badge daftar produk kini merujuk tab Deskripsi & S&K — src/components/admin/ProductEditorModal.tsx, src/components/admin/sections/{ProductCopyTab,VariantCopyEditor,ProductVariantRows,ProductsSection}.tsx, tests/variant-copy-editor.behavior.test.tsx, tests/admin-approved-ui.regression.test.ts, docs/{ARCHITECTURE,DESIGN}.md, README.md — (verifikasi: **1517/1517 test [116 file] hijau** (+4 test perilaku yang merender `ProductEditorModal` sungguhan: empat tab, hanya panel aktif yang tampil, deskripsi + S&K hanya di tab Deskripsi & S&K, tab Varian tanpa editor S&K, jumlah perlu ditinjau di tab, kolom Deskripsi non-WR bisa diedit, penanda Nonaktif, produk baru tanpa panggilan API), tsc bersih, `next build` sukses; **mutation-test 5 putaran, semua MEMERAH di test yang tepat** (editor dikeluarkan dari tab baru, panel Produk selalu tampil, panel Deskripsi & S&K selalu tampil, penanda jumlah dibuang, penanda Nonaktif dibuang) — putaran pertama sempat lolos untuk 'panel Produk selalu tampil', test diperkuat dengan assertion hanya satu tabpanel yang tampil; dev GET / 200 + CSS 200 + /admin 200; login admin dev (password sementara) + agent-browser dengan respons API tiruan dari resolver asli: tab baru desktop 1440px (deskripsi + daftar varian + penanda 1), tab Varian tanpa editor S&K, HP 390px keempat tab muat (scrollWidth 326 = clientWidth 326, sebelum perbaikan padding 342 > 338) dan ganti tab mulai dari atas (scrollTop 0))

- 2026-09-24 — **S&K + cara aktivasi bisa disunting admin per varian (WR & non-WR) + S&K Canva undangan email (permintaan owner).** (1) **Editor per varian** di tab Varian editor produk (`VariantCopyEditor.tsx`): panel "Syarat & Ketentuan · Cara Aktivasi" terlipat dengan badge status (Disunting admin / Versi Axvara (otomatis) / Teks WR — belum versi Axvara / WR mengubah teks — suntingan dijeda / Pakai S&K produk), dua kolom teks yang terbuka berisi salinan yang sedang tampil (format `serializeVariantCopy()` ⇄ `parseAdminVariantCopy()`, bolak-balik tanpa kehilangan isi untuk ke-83 salinan kurasi), teks asli WR sebagai pembanding, tombol sendiri. Disimpan ke kolom baru milik admin `product_variants.admin_terms/admin_activation/admin_copy_fingerprint` (migrasi 0041) HANYA lewat `GET/PUT /api/admin/variant-copy` — bukan tombol Simpan Produk, karena menyimpan foto/badge tidak boleh ikut mencap ulang suntingan yang dijeda. Sync WR dan `PUT /api/products/:id` tidak pernah menulis kolom ini (dikunci test: sweep sync penuh + simpan form produk). (2) **Pengaman ketegasan pemasok**: suntingan dicap sidik jari teks WR saat disimpan (`''` untuk non-WR); resolver kini admin → kurasi → teks WR dirapikan, dan suntingan hanya berlaku selama sidik jari sama. Bila WR mengubah teks, pembeli otomatis melihat teks WR terbaru, editor menampilkan peringatan + tombol "Tandai sudah ditinjau", dan daftar produk admin memberi badge "S&K perlu ditinjau · N varian" (`copyReview` di `GET /api/products` admin; juga untuk teks WR yang belum punya versi Axvara). Teks yang sama dengan salinan otomatis tidak dibekukan ke DB; teks kosong = kembali otomatis. Storefront (`/api/catalog` + SSR PDP yang memakai handler yang sama) tidak pernah menerima kolom mentah admin. (3) **Canva Pro / Premium (non-WR)**: migrasi 0042 menambah `Syarat & Ketentuan:` (undangan Canva dikirim lewat email; pastikan email yang diisi saat checkout aktif) + `Cara Aktivasi:` (buka email undangan lalu terima) ke deskripsi, hanya bila deskripsi masih persis hasil 0040 (isi produksi saat ditulis). Catatan: di produksi tidak ada produk CapCut non-WR (kedua varian CapCut milik WR dan sudah punya S&K), jadi permintaan "CapCut non-WR" diperlakukan sebagai Canva di screenshot owner. Banner WR di editor dan pesan 409 kepemilikan kini menyebut S&K per varian sebagai milik admin — drizzle/migrations/0041_variant_admin_copy.sql, drizzle/migrations/0042_canva_invite_terms.sql, drizzle/schema.sql, src/app/api/admin/variant-copy/route.ts, src/app/api/products/route.ts, src/lib/product-copy/{format,resolve}.ts, src/lib/catalog.ts, src/components/admin/sections/{VariantCopyEditor,ProductVariantRows,ProductsSection}.tsx, src/components/admin/{ProductEditorModal.tsx,product-types.ts}, src/lib/warung-rebahan/ownership.ts, tests/variant-admin-copy.integration.test.ts, tests/variant-copy-editor.behavior.test.tsx, tests/product-copy.test.ts, tests/product-copy-ui.behavior.test.tsx, docs/{ARCHITECTURE,DESIGN,PRD}.md, README.md — (verifikasi: **1513/1513 test [116 file] hijau** (sebelumnya 1484/114; +29 test: bolak-balik 83 salinan kurasi, resolver admin/dijeda/non-WR, integrasi D1 API + storefront + sweep sync penuh + simpan form produk + badge daftar + migrasi 0041/0042, UI editor & badge, render PDP Canva dari migrasi 0042), tsc bersih, `next build` sukses (`/api/admin/variant-copy` terdaftar; salinan kurasi tetap 0 kemunculan di chunk client); **mutation-test 10 putaran, semua MEMERAH di test yang tepat** (guard sidik jari dibuang, suntingan diabaikan storefront, kolom mentah admin ikut terkirim, teks sama-otomatis dibekukan, simpan form produk mencap ulang sidik jari, badge tidak menghitung, tombol tinjau terkunci, guard 0042 dibuang, judul bagian editor diabaikan, catatan jadi langkah); D1 produksi dibaca read-only (tidak ada CapCut non-WR; deskripsi Canva = hasil 0040 persis, jadi guard 0042 akan berlaku); dev GET / 200 + CSS 200 + /admin 200 + /produk 200, `/api/admin/variant-copy` tanpa login 401; login admin dev (password sementara) + agent-browser dengan respons API tiruan dari resolver asli: badge daftar produk, editor varian status dijeda + peringatan + teks asli WR + tombol tinjau terverifikasi visual. PDP Canva tidak di-screenshot karena PDP kini di-seed server (dev tanpa D1) — rendernya dikunci test perilaku))

- 2026-09-24 — **Loading storefront tidak lagi "bisu" di jaringan lambat (permintaan owner).** Audit read-only menemukan layar diam justru di titik transisi: (1) route dinamis (`/`, PDP, artikel, pesanan) tanpa prefetch & tanpa loading boundary → halaman lama diam sampai server menjawab, dan `RouteLoading` baru menyala SETELAH halaman baru tampil; (2) setelah Bayar sukses tombol aktif lagi dan keranjang dikosongkan sebelum pindah → sempat "Keranjang kosong · ← Kembali belanja"; (3) `/pesanan` menampilkan salinan lokal apa adanya ("Pesanan Diterima! Admin akan memverifikasi bukti…" tanpa QR) dan QR tanpa placeholder; (4) sticky bar PDP mobile aktif sebelum varian termuat → klik cepat = checkout TANPA varian (buntu); (5) tombol Checkout/Beli Langsung/modal varian tanpa umpan balik (modal malah langsung tertutup); fetch tanpa batas waktu. Perbaikan: `NavigationProgress` (ganti `RouteLoading`) menangkap klik Link internal di fase capture + `startNavigation()` untuk `router.push` → setelah 120 ms bar cyan merayap + skeleton rute tujuan (`Skeletons.tsx`, satu bentuk dipakai overlay & loading halaman), 8 dtk pil "Koneksi lambat", 20 dtk "Coba lagi"/"Batal"; sengaja TANPA `loading.tsx` (streaming akan mengubah 404 produk nonaktif jadi 200+noindex dan memindah katalog SSR ke `<div hidden>`). `usePendingNavigation`: CTA berputar "Membuka checkout…" sampai checkout tampil; modal varian tetap terbuka. Checkout: produk+varian `?buy=` diambil paralel (dulu 3 round-trip berurutan), label Bayar bertahap (Membuat pesanan → Menyiapkan QRIS → Koneksi lambat), layar "Pesanan dibuat · Membuka halaman pembayaran" (scroll ke atas, 8 dtk → tautan manual) sampai `/pesanan` tampil, putus jaringan → "Tekan Bayar lagi — pesanan yang sama dilanjutkan, tidak dibuat dobel" (idempoten via `orders.quote_id`), `qris` respons create disimpan ke salinan lokal (tetap tanpa WA/email). `/pesanan`: salinan lokal hanya dipakai bila ≤10 mnt + ada QR (QR tampil tanpa menunggu GET), selain itu skeleton; QR ber-placeholder ukuran tetap + "Muat ulang QRIS"; poll tidak menumpuk. PDP: server memanggil handler `/api/products?slug=` + `/api/catalog?slug=` → PDP lengkap di HTML/respons navigasi (tanpa skeleton kedua), varian loading netral + "Coba lagi", sticky mobile nonaktif "Memuat varian…", varian gagal → Beli membuka modal, skeleton "Produk Serupa". `fetchWithTimeout` di quote (25 dtk), order (60 dtk), lookup/pesanan/varian. Reduced motion dihormati — src/components/ui/NavigationProgress.tsx (baru, hapus RouteLoading.tsx), src/components/storefront/{Skeletons,ProductCard,QuickVariantModal}.tsx, src/hooks/{useLoadingStage,usePendingNavigation}.ts, src/stores/navigation.ts, src/lib/fetch-timeout.ts, src/app/{layout.tsx,globals.css,checkout/page.tsx,pesanan/[code]/page.tsx,produk/[slug]/page.tsx,produk/[slug]/product-detail-client.tsx,lacak-pesanan/lacak-pesanan-client.tsx}, tests/{navigation-feedback,payment-loading,pdp-loading}.behavior.test.tsx (23 baru), tests/buyer-silence-and-atomicity.regression.test.ts (anchor `fetchWithTimeout("/api/orders"`), docs/{DESIGN §6.1,ARCHITECTURE §3/§3.1/§6,PRD FR-S28},README — (verifikasi: **1484/1484 test [114 file] hijau**, tsc bersih, eslint 0 error; dev GET / 200 + CSS 200, `/produk/tidak-ada-produk` tetap 404, HTML PDP kini memuat konten lengkap (1 h1 visual); visual Chromium headless mobile 390px dengan jaringan ditahan (Obscura tidak tersedia di sandbox Linux): klik kartu → skeleton PDP + bar ≤0,6 dtk, 8 dtk pil koneksi lambat, overlay hilang saat PDP tampil; Bayar → "Menyiapkan QRIS…" → layar "Pesanan dibuat" → `/pesanan` dengan QR placeholder lalu "Muat ulang QRIS" saat gambar gagal (quote dev di-mock menambah metode QRIS karena data dev tanpa QRIS); satu perbaikan dari verifikasi visual: layar "Pesanan dibuat" awalnya di luar viewport karena scroll tertinggal di dasar form)

- 2026-09-24 — **Deskripsi, S&K, dan cara aktivasi versi Axvara (permintaan owner): satu format untuk produk WR & non-WR, S&K + cara aktivasi terlipat di mobile, kalimat berulang dibuang, ketegasan pemasok utuh.** Diukur dari D1 produksi (read-only): 48 deskripsi WR + 2 non-WR, 82 teks S&K dan 29 teks cara aktivasi yang berbeda-beda (83 pasangan unik) — penuh HURUF BESAR berteriak, emoji, huruf tebal Unicode (tidak terbaca pembaca layar), bahasa gaul reseller ("bl4ckmarket", "gada yg tau"), dan aturan yang ditulis dua kali (Netflix Legal: 8 baris S&K diulang persis di cara aktivasi); PDP merender semuanya mentah (`whitespace-pre-line`) dan di mobile S&K selalu terbuka panjang. (1) **S&K + cara aktivasi WR** tetap bersumber `wr_variants` (sync tidak disentuh): `src/lib/product-copy/resolve.ts` memilih salinan Axvara di `curated.ts` (83 entri) lewat `supplierFingerprint()` (cyrb53 atas kata+angka per baris — kebal perubahan kosmetik, berubah bila WR mengubah kata/angka); bila tidak cocok, `supplierVariantCopy()` menampilkan teks WR yang dirapikan (NFKC, tanpa emoji/teriakan/baris ganda, aturan di teks aktivasi pindah ke S&K) sehingga aturan baru pemasok tidak pernah tertutup salinan lama. S&K dikelompokkan Detail paket / Proses & pengiriman / Aturan pakai / Garansi, cara aktivasi bernomor (boleh berkelompok, mis. login aplikasi vs website). `/api/catalog?slug=` kini mengirim `variants[].copy` dan mengosongkan `terms`/`delivery_terms` mentah; resolver sengaja dipanggil route, bukan `catalog.ts` (ikut diimpor komponen client). (2) **Deskripsi**: migrasi 0040 (data-only, idempoten) mengisi versi Axvara ke `admin_description_override` 47 produk WR hanya bila masih kosong (teks WR di `description` utuh), dan ke `description` Canva/GSuite hanya bila isinya masih persis teks lama; format = paragraf pembuka + daftar keunggulan (+ `Syarat & Ketentuan:` untuk GSuite). Klaim deskripsi pemasok yang bertentangan dengan S&K varian dibuang (Claude "coding canggih" padahal S&K melarang coding, Netflix "multi-perangkat", Prime "tanpa iklan" padahal varian Garansi with ads); catatan harga internal reseller tidak ditampilkan. api-testing dilewati. (3) **PDP**: `ProductCopy.tsx` (DescriptionBody/TermsBody/ActivationBody/MobileCollapsible); mobile: S&K dan Cara Aktivasi panel terlipat (`aria-expanded`, meta label varian / "N langkah"), deskripsi dilipat hanya bila melebihi ±6 baris; S&K dari deskripsi non-WR digabung dengan S&K varian tanpa baris ganda (`mergeProductCopy`). Meta SEO/OG memakai paragraf pembuka saja (`descriptionSummary`). Poin PDP & halaman /garansi-replace kini merujuk "Syarat & Ketentuan" (bukan "deskripsi produk") sebagai tempat ketentuan garansi. Editor admin menjelaskan format deskripsi. Ditemukan saat verifikasi visual: label panel `truncate` (nowrap) melebarkan grid PDP mobile (scrollWidth 484 vs 390) — kini membungkus; URL panjang (tutorial Office) memakai `[overflow-wrap:anywhere]` — src/lib/product-copy/{text,format,curated,resolve}.ts, src/components/storefront/ProductCopy.tsx, src/app/api/catalog/route.ts, src/app/produk/[slug]/product-detail-client.tsx, src/lib/product-seo.ts, src/app/garansi-replace/page.tsx, src/components/admin/ProductEditorModal.tsx, src/lib/warung-rebahan/ownership.ts, drizzle/migrations/0040_axvara_product_copy.sql, tests/fixtures/product-copy-snapshot.json, tests/product-copy{,-content}.test.ts, tests/product-copy-ui.behavior.test.tsx, tests/warung-rebahan/ownership.regression.test.ts, docs/{ARCHITECTURE,DESIGN,PRD}.md, README.md — (verifikasi: **1461/1461 test [111 file] hijau** (sebelumnya 1046/108), tsc bersih; test integritas membandingkan tiap salinan dengan snapshot teks WR produksi (semua angka, URL, dan keluarga aturan: larangan, kewajiban, tanpa garansi, refund, perangkat, sanksi, platform, OTP/VPN) — test ini menangkap 2 larangan yang sempat saya lunakkan (Zoom "jangan typo", Office "jangan sampai lupa password") dan sudah dikembalikan; **mutation-test 11 putaran, semua MEMERAH di test yang tepat** (panel mobile terbuka default, teks mentah WR tetap dikirim, kurasi diabaikan, sidik jari buta angka, angka garansi dibuang, larangan dilunakkan, guard override migrasi dibuang, S&K deskripsi tampil ganda, gabungan tanpa dedupe, meta SEO mentah, fallback membuang aturan); migrasi 0040 diuji terhadap ekspor D1 produksi (47 override + 2 non-WR, jalan ulang tanpa perubahan); `next build` sukses dan salinan kurasi hanya ada di bundle server `/api/catalog` (0 kemunculan di chunk client); dev GET / 200 + CSS 200 + /produk 200 + /garansi-replace 200, log dev tanpa error; Chromium headless (agent-browser, obscura tidak tersedia di sandbox Linux) dengan respons API tiruan berisi output resolver asli: mobile 390px S&K/aktivasi tertutup→terbuka, desktop 1440px, produk non-WR GSuite, produk URL panjang Office; scrollWidth 390=390)

- 2026-09-24 — **Storefront & Telegram (permintaan owner): pengingat pesanan belum dibayar, SEO & GEO, katalog Telegram hanya stok tersedia, paginasi tanpa pesan ganda.** (1) **Pengingat melayang** `PendingOrderReminder` (layout root): pembeli yang tak sengaja keluar dari halaman QRIS kini melihat kartu "Pesanan belum dibayar · Bayar dalam MM:SS" di seluruh storefront (kecuali checkout/pesanan/admin) dan kembali ke `/pesanan/[code]` dengan satu ketukan, tanpa checkout ulang; QR hangus tapi pesanan masih hidup → "QRIS hangus · Sisa waktu · Perpanjang". Status & tenggat SELALU dari `GET /api/orders?code=` (salinan lokal checkout tidak pernah diperbarui), cek ulang 30 dtk (limit lookup 20/mnt), tombol ✕ per pesanan per sesi. Salinan lokal checkout tidak lagi menyimpan WA/email pembeli. (2) **SEO & GEO** — diukur dari produksi: HTML beranda berisi "Katalog Premium 0 produk" tanpa satu link `/produk/` (katalog dimuat di browser → crawler tanpa JS & crawler AI melihat toko kosong), tanpa canonical/JSON-LD/og:image, tanpa `llms.txt`; JSON-LD produk memakai URL gambar RELATIF + `brand: AXVARA`; **semua artikel berjudul & berdeskripsi beranda** (tanpa `generateMetadata`); `/checkout` & `/pesanan/[code]` terindeks. Perbaikan: beranda dirender server (`page.tsx` memanggil handler `/api/products` yang sama → `HomeClient` di `home-client.tsx`), JSON-LD Organization/WebSite/ItemList, OG/Twitter default + `public/og/axvara-og.png` 1200×630 (tanpa canonical di root agar tidak diwarisi semua halaman), JSON-LD produk gambar absolut + `offers.seller` AXVARA, `generateMetadata` artikel (judul, deskripsi, canonical, gambar, `article:published_time` ISO), canonical halaman statis, noindex via `checkout/layout.tsx` + `pesanan/layout.tsx`, `robots.txt` menutup halaman transaksi + membuka `/api/products|categories|store-settings` untuk renderer + crawler AI eksplisit, `/llms.txt` baru. (3) **Katalog Telegram hanya produk yang bisa dibeli** — produksi: 48 produk tampil, hanya 20 tersedia (28 jalan buntu). Satu aturan `purchasableStockSql` (`src/lib/catalog-availability.ts`: varian aktif, stok -1 atau ≥ `min_qty`) kini dipakai katalog/kategori/cari/bestseller Telegram, kartu web `/api/products`, dan JSON-LD PDP, dibaca ulang dari D1 tiap dibuka → habis/ready di web langsung berlaku di Telegram; **web tetap menampilkan produk habis** (keputusan owner, badge "Stok Habis" di belakang — aturan ini hanya menentukan label, harga kartu, dan urutan; dikunci test agar tidak ikut disembunyikan); harga = varian tersedia termurah; pencarian yang hanya cocok produk habis dijawab "sedang habis"; halaman dari tombol lama dibatasi ke halaman terakhir. (4) **Paginasi katalog Telegram tidak lagi membuat pesan baru** — akar: `safeEditOrSend` mengirim pesan baru untuk kegagalan edit APA PUN; ketuk ▶️ dua kali = dua callback halaman yang sama → edit kedua ditolak `message is not modified` → pesan katalog baru (screenshot owner). Kini "not modified" = berhasil; fallback pesan baru hanya untuk HTTP 400 (pesan foto/terhapus), tidak untuk 429/5xx/timeout. Tombol produk nonaktif dijawab, bukan diam — src/components/storefront/PendingOrderReminder.tsx, src/app/{layout,page,home-client}.tsx, src/app/{checkout,pesanan}/layout.tsx, src/app/llms.txt/route.ts, src/app/robots.ts, src/app/artikel/{page,[slug]/page}.tsx, src/app/{cara-order,garansi-replace,lacak-pesanan}/page.tsx, src/app/produk/[slug]/page.tsx, src/app/checkout/page.tsx, src/app/api/products/route.ts, src/lib/{catalog-availability,site-seo,product-seo}.ts, src/lib/telegram/{api,keyboards}.ts, src/lib/telegram/handlers/{catalog,discovery,shared}.ts, src/lib/telegram/messages/catalog.ts, public/og/axvara-og.png, tests/{telegram-catalog-stock,telegram-safe-edit,seo-geo}.regression.test.ts, tests/{home-ssr-catalog,pending-order-reminder}.behavior.test.tsx (31 baru), tests/{storefront-catalog-ux.behavior,checkout-integrity.regression}.test.* (rujukan ke home-client), docs/{ARCHITECTURE,PRD FR-S14/FR-S27/FR-SYS6,DESIGN §5.7d/§5.7e},README — (verifikasi: **1046/1046 test [108 file] hijau**, tsc bersih, `next build` sukses (`/` kini dinamis ƒ, `/llms.txt` terdaftar); **mutation-test 18 putaran, masing-masing MEMERAH di test yang TEPAT**; dev GET / 200 + CSS 200, HTML server beranda kini memuat 12 link produk + harga + 1 JSON-LD valid (Organization/WebSite/ItemList 24 item), `/robots.txt` `/sitemap.xml` `/llms.txt` 200, `/checkout` & `/pesanan/*` `noindex, nofollow`, canonical di halaman statis; bukti produksi read-only: 48 produk Telegram vs 20 tersedia; visual Chromium headless (Obscura tidak tersedia di sandbox Linux) dengan API pesanan di-mock: kartu melayang mobile tidak menutupi nav bawah (jarak 13px), desktop kanan bawah, hitung mundur berjalan, mode perpanjang; dua perbaikan dari verifikasi visual: panel kaca 45% → hampir solid (ikon hero menembus teks) dan timer dipindah keluar area `truncate` (versi awal menyembunyikan hitung mundur di layar 390px))

- 2026-09-24 — **Audit ronde 4 ditelaah: 10 temuan terbukti & diperbaiki, 4 ditolak, sisanya ditunda — kabar kegagalan akhirnya sampai ke pembeli web + Telegram.** Temuan terpenting justru di luar laporan: **semua kabar pembeli kanal WEB masuk outbox WhatsApp**, padahal bot WA mati (outbox produksi 18–19 Sep: 3 baris `dead`, `whatsapp_not_connected`), jadi serah terima, bukti ditolak, bukti menunggu Hook, dan gagal kirim (termasuk F5 ronde 3) **tidak pernah sampai** ke pembeli web. Kini web → email Resend ke `customer_email` (wajib sejak revamp checkout), dengan fallback WA hanya untuk order web lama tanpa email; email + DM Telegram diberi kunci idempoten di tabel baru `buyer_notice_log` (migrasi 0039; `failed`/`sending` basi boleh diklaim ulang). **Terbukti & diperbaiki:** (T-H1) kegagalan Warung Rebahan (webhook `order.failed`, reconciler, percobaan habis 3x) dulu hanya mem-ping admin karena item WR `return false` sebelum `notifyBuyerDeliveryFailed` → kini `refreshOrderAggregate` mengabari pembeli saat agregat pertama kali `failed` + jalur percobaan-habis ikut memanggilnya; (W-H1) `/pesanan/[code]` merayakan "Pembayaran Dikonfirmasi 🎉 … 5–15 menit" untuk order gagal kirim karena `GET /api/orders?code=` tidak mengembalikan `fulfillment_status` → kini dikembalikan (juga `/api/orders/[code]`) dan halaman tampil merah "Pengiriman Produk Bermasalah"; (B-H1) pembeli web kini menerima email tanda terima pembayaran (idempoten, tanpa fallback WA); (B-H2/B-H3) antrean kedaluwarsa memakai jalur per jenis (`ROW_NUMBER() OVER (PARTITION BY status='pending')`, dicek didukung D1 produksi) + `hasTime(10 dtk)` per kiriman + timeout email 8 dtk; (T-M5) handover mengembalikan `buyer_notified` dan panel admin memperingatkan bila kabar gagal; (T-M4) `wa_after_paid` hanya menangkap teks mirip nomor telepon sehingga tombol menu tidak lagi dibalas "Nomor WA tidak valid"; (T-M3) label harga tombol Telegram tidak lagi membulatkan (Rp7.500 dulu "Rp8rb"); (W-M3) S&K belum dicentang tidak lagi mematikan CTA tanpa penjelasan — klik menampilkan pesan + menggulir ke checkbox; (W-M2) catatan keranjang "QRIS / DANA / SeaBank" → "QRIS — semua e-wallet & m-banking". **Ditolak setelah diverifikasi:** T-H3 email-gate "fail-open saat DB timeout" (setiap query sudah `.catch(() => null)` dan `wr_type` dimuat dari varian → timeout justru membuat bot MEMINTA email); W-M1 hero search mati (`Hero.tsx` tidak dirender di mana pun, homepage memakai `OrbitHero`); W-M4 `ctaDisabled` hardcode QRIS (`submit()` sendiri meng-hardcode `payMethod="qris"`, checkout memang QRIS-only); T-H2 sebagai High (klaim "ketik canva → Jumlah tidak valid" salah: handler qty hanya jalan untuk input angka; last-write-wins satu kolom adalah desain, dampaknya edge-case ringan). **Ditunda:** alur refund (butuh migrasi + keputusan produk, sama dengan F3), tombol perpanjang QRIS di keyboard status Telegram (T-M2), tombol basi tanpa jawaban (T-M1), reminder ke chat id grup legacy (T-M6), Cache-Control endpoint admin fulfillment (B-M1), audit confirm_match (B-M2), kabar pembeli untuk uang tanpa pasangan, W-M5/W-M6 dan temuan Low — drizzle/migrations/0039_buyer_notice_log.sql, drizzle/schema.sql, src/lib/notify-buyer.ts, src/lib/warung-rebahan/{deliver,order,forward-sender}.ts, src/lib/fulfillment/delivery/ensure.ts, src/lib/payments/qris-expiry-notifications.ts, src/app/api/cron/operations/route.ts, src/app/api/orders/route.ts, src/app/api/orders/[code]/route.ts, src/app/pesanan/[code]/page.tsx, src/app/api/admin/orders/[code]/handover/route.ts, src/components/admin/OrdersManager.tsx, src/lib/telegram/{keyboards.ts,handlers/discovery.ts}, src/app/checkout/page.tsx, src/components/storefront/CartDrawer.tsx, tests/round4-buyer-notices.regression.test.ts (11 baru), tests/round4-telegram.regression.test.ts (5 baru), tests/order-page-delivery-failed.behavior.test.tsx (2 baru), tests/qris-expiry-queue-blocking.regression.test.ts (+2), docs/{ARCHITECTURE §15,PRD FR-S20/FR-S26,DESIGN §5.0/§5.7c},README — (verifikasi: **1015/1015 test [103 file] hijau**, tsc bersih; **mutation-test 15 putaran, masing-masing MEMERAH di test yang TEPAT** (email web dikembalikan ke WA, ledger dimatikan, notifikasi agregat/percobaan-habis/tanda terima dibuang, `buyer_notified` dibuang, `fulfillment_status` dibuang dari API, halaman mengabaikan failed, jalur antrean dikembalikan, guard waktu dibuang, timeout 20 dtk dikembalikan ×2, `wa_after_paid` menelan semua teks, label harga membulatkan lagi, CTA dimatikan `!agreed` lagi); dev GET / 200 + CSS 200 + /checkout 200 + /lacak-pesanan 200 + /pesanan/[code] 200 + /admin 200 tanpa error compile; bukti produksi read-only: outbox WA `dead`/`whatsapp_not_connected`, 6 order lunas semuanya kanal web; visual Chromium headless (Obscura tidak tersedia di sandbox Linux) dengan respons API di-mock: status gagal kirim merah + ikon ✕ + panel langkah tampil, estimasi 5–15 menit hilang, kontrol `queued` tidak berubah)

- 2026-09-23 — **Regresi F7 diperbaiki: antrean notifikasi kedaluwarsa QRIS tersumbat permanen di produksi (termasuk Telegram).** Ditemukan saat memeriksa apakah kredensial Resend dibutuhkan, dan jawabannya **tidak**: `RESEND_API_KEY`, `FORWARD_FROM_EMAIL`, dan `WR_EMAIL_WEBHOOK_SECRET` sudah ada di Pages production sebagai `secret_text`, domain `axvara.tech` verified (Tokyo), dan `wr_email_forward_log` mencatat 9 email terkirim tanpa error sampai 18 Sep. Tidak ada secret yang ditulis ulang. **Akar:** migrasi 0026 hanya menandai riwayat `terminal` saat kolom dibuat. F7 (live 12:58 UTC) memasukkan kanal web tanpa backfill setara, sehingga 8 order web yang expired 15–23 Sep langsung ikut antre. Dua baris terdepan (urutan terlama-dulu, `LIMIT 2`) tidak punya email dan dilewati tanpa menandai, jadi terpilih ulang setiap cron dan tidak ada notifikasi kedaluwarsa lain (Telegram/web) yang bisa lewat. Kalau sumbatannya lepas, pembeli malah akan menerima kabar "pesanan kedaluwarsa" yang telat 3–8 hari. **Perbaikan** (semua di `QRIS_EXPIRY_NOTICE_WHERE`, jadi hitungan `qris_notice` cron ikut akurat): (1) antrean hanya berisi baris yang punya tujuan kirim (web: email, Telegram: chat id, WA: conversation); (2) cabang terminal hanya untuk order yang expired <24 jam; (3) urutan renewable dulu, lalu yang terbaru dulu, supaya baris yang gagal terus (bot diblokir, email ditolak) tenggelam ke belakang dan tidak menahan kabar baru. **Upaya pertama ditolak:** menandai baris tanpa tujuan memakan 1 query per baris dari kuota fase expiry, dan `cron-budget.integration.test.ts` memerah (run 2 hanya meng-expire 3, bukan 4). Filter di WHERE tidak memakan query sama sekali dan mempertahankan niat F7: bila email terisi kemudian, kabarnya tetap terkirim — src/lib/payments/qris-expiry-notifications.ts, tests/qris-expiry-queue-blocking.regression.test.ts (4 baru), docs/ARCHITECTURE.md §15 — (verifikasi: **995/995 test [100 file] hijau**, tsc bersih, dev GET / 200 + CSS 200 + /checkout 200 + /lacak-pesanan 200 + route cron ter-compile & tetap 401 tanpa token; WHERE baru dievaluasi read-only ke D1 produksi: antrean **8 → 2 baris** (hanya order web hari ini yang punya email), 3 baris tanpa email dan 3 riwayat basi tersaring; **mutation-test 4 putaran, masing-masing MEMERAH di tepat 1 test**: filter tujuan dibuang, urutan dikembalikan ke terlama-dulu, prioritas renewable dibuang, batas 24 jam dibuang; kode F7 lama memerah di 3 test; Obscura tidak tersedia di sandbox Linux ini, dan perubahan ini murni backend tanpa UI)

- 2026-09-23 — **F7 dikerjakan: pengingat kedaluwarsa QRIS akhirnya sampai ke pembeli WEB lewat email.** Koreksi atas alasan penundaan saya sendiri di entri sebelumnya ("web tidak punya jalur push selain WA, dan bot WA mati") — setelah diperiksa, jalur **email Resend sudah hidup dan dipakai** (`sendForwardEmail`, dipakai mengirim kredensial ke pembeli web), dan sejak revamp checkout `3d83dd9` email **wajib** untuk semua order web. Jadi bahannya sudah lengkap. Masalahnya: `QRIS_EXPIRY_NOTICE_WHERE` memfilter `sales_channel IN ('telegram','whatsapp')` sehingga kanal web tidak pernah masuk hasil query — padahal justru pembeli WEB yang BOLEH memperpanjang QRIS (`qris_reissue_allowed` + gerbang `reissueDanaQrisInvoice` hanya mengecualikan WhatsApp). Akibatnya pembeli web tidak pernah tahu QR-nya hangus dan hanya menemukannya bila kebetulan membuka halaman lagi; tab yang sudah ditutup = tidak tahu sama sekali (kebalikan WhatsApp yang hanya menerima kabar terminal, tanpa pernah dapat kabar "masih bisa diselamatkan"). Perbaikan: kanal `web` masuk filter, dan pesannya dikirim sebagai email dua-nada — **renewable** = ajakan menekan "Perpanjang QRIS" di `/pesanan/[code]` (perpanjangan tersedia 1x, `MAX_QRIS_REISSUES`), **terminal** = pesanan sudah mati, arahkan pesan ulang. Dua sifat aman dijaga eksplisit: order web lama tanpa email dilewati **tanpa** menulis `expiry_notice_state`, dan kegagalan kirim juga tidak menandai — keduanya agar cron berikutnya mencoba lagi alih-alih kehilangan kabar diam-diam. Prioritas kanal tetap: web + Telegram, WhatsApp tidak disentuh (bot mati, grup dikunci admin-only) — src/lib/payments/qris-expiry-notifications.ts, tests/qris-expiry-web-email.integration.test.ts (4 baru), docs/ARCHITECTURE.md §15 — (verifikasi: **991/991 test [99 file] hijau**, tsc bersih, dev GET / 200 + CSS 200 + /checkout 200 + /lacak-pesanan 200 + /admin 200 (diverifikasi di port 3001 karena port 3000 dipegang proses dev zombie di luar namespace sandbox — 500-nya ENOENT `routes-manifest.json`, bukan regresi kode; satu-satunya `⨯` di log adalah gagal unduh Google Fonts karena sandbox offline); 11 test `qris-channel-policy` yang sudah ada TETAP hijau sehingga kontrak Telegram/WA tidak bergeser; **mutation-test 2 putaran, masing-masing MEMERAH di test yang TEPAT**: `web` dibuang dari filter → 3 test F7 memerah; penanda ditulis walau email gagal → test retry memerah; diuji sebagai PERILAKU dengan fixture D1 nyata + mock Resend, bukan pencocokan string)

- 2026-09-23 — **Audit ronde 3 ditelaah: 5 temuan terbukti & diperbaiki, 1 DITOLAK karena usulannya justru merusak, sisanya ditunda sesuai prioritas kanal.** **Konteks prioritas (keputusan owner):** WEB + TELEGRAM wajib sempurna; WhatsApp bukan kanal utama 2-5 bulan ke depan (bot sedang mati, grup akan dikunci admin-only), jadi temuan khusus-WA (F1 jendela QRIS 15 mnt, F4 handover `.d`, F8 session sunyi, F6 sisi-WA) sengaja TIDAK dikerjakan — bukan karena tidak valid. (a) **Regresi revamp checkout `3d83dd9` (R1-R3, kanal WEB)**: field nama dihapus dan `customer_name` diambil dari `email.split("@")[0]` MENTAH — prefix email bukan nama: `budi123@…` tersimpan "budi123", `first.last+promo@…` tersimpan "first.last+promo", lalu merembes ke sapaan halaman pesanan ("Terima kasih, budi123!"), notifikasi Telegram admin (`👤 budi123`), prefill tombol WA follow-up ("Halo budi123"), pencarian admin (LIKE), dan ekspor CSV. Kini `deriveNameFromEmail` membuang plus-addressing + angka, mengubah `._-` jadi spasi, lalu mengapitalkan (`budi123`→"Budi", `first.last+promo`→"First Last", `x@`/`12345@`→"Pembeli Axvara"); **UX revamp dipertahankan** (form tetap tanpa field nama) dan nama yang diisi pembeli sendiri tidak pernah disentuh. (b) **F2 — uang masuk tanpa pasangan invoice berakhir sunyi total**: cabang `unmatched` webhook DANA hanya menulis `status='ignored'` lalu balas `ok:true` — pembeli menunggu sampai order kedaluwarsa (lalu menuduh toko menipu) dan admin hanya tahu bila mengintip tabel `dana_webhook_events`. Kini admin di-ping Telegram berisi nominal + sebab; ack tetap 2xx agar DANA tidak retry. (c) **F5 — pembeli tidak pernah tahu saat pengiriman gagal**: kegagalan terminal dulu hanya membunyikan Telegram ADMIN; kini `notifyBuyerDeliveryFailed` mengabari pembeli lewat kanal asal ordernya. (d) **F9 — halaman lacak pesanan buta terhadap kegagalan**: `/api/orders/lookup` SUDAH mengembalikan `fulfillment_status`, tetapi halaman hanya membaca status pembayaran sehingga order lunas-tetapi-gagal-kirim tampil hijau "Lunas — pembayaran diterima" selamanya; pembeli yang menghapus chat atau ganti HP jadi buta total karena semua kabar ronde 2 lewat chat. Kini ada status "Lunas — pengiriman bermasalah" + panel langkah lanjutan. (e) **B1 DITOLAK** — klaim "retry spam-klik = amplifikasi upstream" dengan usulan `attempt_count=attempt_count+1` di route retry: usulan itu justru **menghitung ganda** karena penaikan counter adalah milik worker saat mengklaim (`order.ts` `status='claimed', attempt_count=?`) dan `handleInsufficientBalance` SENGAJA mengembalikannya (`attempt_count-1`) agar saldo habis tidak memakan jatah transport — menerapkannya memangkas separuh kuota retry. Amplifikasinya sendiri sudah tertutup: klik bersamaan disaring CAS (dikunci `admin-retry-race.regression.test.ts` yang MEMERAH saat saya sempat mencoba debounce) dan klik berurutan menemukan status sudah `claimed` yang bukan anggota `RETRYABLE` → 409. **Ditunda dengan alasan**: F7 (expiry notice tak cover web) terblokir karena web tidak punya jalur push selain WA/email sementara bot WA mati; F3 (alur refund) butuh migrasi kolom + keputusan produk; B3/B4/B5 (CSV PII, CSRF tanpa Origin, revoke tanpa scope) — khusus B4, usulan "tolak mutasi tanpa Origin" akan MEMATIKAN seluruh webhook DANA/Telegram/WA dan cron yang memang tidak mengirim header Origin — src/lib/utils.ts, src/lib/notify-buyer.ts, src/lib/fulfillment/delivery/send.ts, src/app/api/orders/route.ts, src/app/api/webhook/dana/route.ts, src/app/checkout/page.tsx, src/app/lacak-pesanan/lacak-pesanan-client.tsx, tests/round3-name-and-money-silence.regression.test.ts (8 baru), docs/ARCHITECTURE.md §15 — (verifikasi: **987/987 test [98 file] hijau**, tsc bersih, dev GET / 200 + CSS 200 + /checkout 200 + /lacak-pesanan 200 + /admin 200 tanpa error compile, screenshot /lacak-pesanan render normal; **mutation-test 3 putaran, masing-masing MEMERAH di test yang TEPAT**: sanitizer nama dilemahkan → test nama memerah; ping admin unmatched dibuang → test F2 memerah; `isDeliveryFailed` dijadikan konstanta → test F9 memerah setelah assertion diperkuat dari "mengandung identifier" menjadi mencocokkan kondisi utuh)

- 2026-09-23 — **Checkout revamp ala Sekalipay: ① Metode dulu, ② Data minimal (WA + Email wajib, tanpa Nama), ringkasan 1x, CTA 1x per viewport.** (1) Metode pindah ke kolom kiri paling atas + QRIS auto-select selama maintenance (hemat 1 klik, hilangkan error pilih-metode; hook sebelum early return — aturan hooks). (2) Field Nama dihapus; `customer_name` opsional di API dengan fallback prefix email (kolom DB NOT NULL tetap terisi, notif admin tetap bernama); `customer_email` kini SELALU wajib di zod + form (defense-in-depth 422 invite/Link WR dipertahankan). (3) Rail kanan jadi DESKTOP ONLY (`hidden lg:block`, hanya ringkasan + S&K + CTA) — mobile tidak lagi duplikat: accordion ringkasan `lg:hidden` + S&K kiri + sticky CTA; S&K satu fungsi `renderAgreeBlock(id)` (1 definisi, 1 render per viewport, 1 state `agreed`). (4) Copy hint WA/Email ala Sekalipay; box Verifikasi Otomatis jadi sub-hint di bawah metode; subtitle "Pilih pembayaran, isi data…". Hook auto-select sempat kepasang setelah early return → crash "Rendered more hooks" di direct-buy, diperbaiki dengan memindahkannya sebelum early return (tertangkap verifikasi visual, bukan test). — src/app/checkout/page.tsx, src/app/api/orders/route.ts, tests/checkout-integrity.regression.test.ts (guard layout diperbarui: rail `hidden lg:block`, `renderAgreeBlock`, tanpa `checkout-name`/"Nama lengkap", ①/②, "Auto-select QRIS"), issue/checkout-revamp-sekalipay-2026-09-23.md, docs/{PRD (FR-S5),DESIGN (§5.0 + §5.6),ARCHITECTURE (§flow + struktur route),README} — (verifikasi: **970/970 test [96 file] hijau**, tsc bersih, dev GET / 200 + CSS 200 + /checkout 200 + /checkout?buy=slug 200 tanpa error compile; visual browser: desktop accordion `none` + rail `block` + CTA terlihat 1 + tanpa field Nama + urutan ① Metode → ② Data; catatan: QRIS tak muncul di dev karena `isDanaQrisConfigured()` false tanpa env DANA — perilaku dev yang benar, bukan regresi; auto-select aktif di prod)
- 2026-09-23 — **Audit ronde 2 divalidasi: 9 dari 10 temuan High terbukti, lalu diperbaiki — pembeli berhenti menunggu dalam sunyi + jalur uang dibuat atomik.** Ronde 1 menutup kebohongan status di sisi ADMIN; ronde 2 menemukan pola yang sama di sisi PEMBELI. (a) **P0 uang — `handleOrderCancel` non-atomik**: stok dipulihkan LEBIH DULU, lalu `UPDATE orders` bergerbang `status='pending'`, lalu `UPDATE payment_transactions` **tanpa gerbang sama sekali**. Webhook DANA yang melunasi order tepat di sela itu menghasilkan kerusakan permanen: stok menggelembung, order tetap sah `lunas`, tetapi ledger ditimpa `cancelled` → order lunas tanpa transaksi `paid` + fulfillment yatim. Kini pembatalan MENGKLAIM dulu (CAS `status='pending' AND payment_status IN ('unpaid','pending')`); kalah balapan = berhenti tanpa menyentuh stok/ledger, dan UPDATE ledger diberi gerbang `status IN ('pending','unpaid','expired')`. (b) **P0 uang — harga basi**: token quote berlaku **1 jam** sementara sync WR menulis ulang harga tiap ~5 menit; `sameItems` hanya menyamakan identitas+qty dan `createOrderWithStock` **nol guard harga** (0 kemunculan `price` di file itu), jadi order + invoice QRIS bisa terbit dari `quote.subtotal` usang — harga naik = toko rugi, turun = pembeli dirugikan. Kini harga DB dibanding harga quote → `409 price_changed`, checkout memuat ulang quote agar pembeli melihat nominal baru (bukan buntu di pesan error mentah). (c) **Kabar ke pembeli** via modul baru `src/lib/notify-buyer.ts` — kanal dipilih dari `orders.sales_channel`, Telegram HANYA ke chat pribadi terverifikasi (tidak pernah id grup), web/WA lewat outbox durable yang idempoten: serah terima manual dulu menandai `delivered` tanpa satu pun pesan **padahal pesan lunas Telegram sudah berjanji** "Produk akan dikirim admin melalui DM Telegram pribadi ini"; penolakan bukti hanya menulis `status='rejected'` sehingga pembeli melihat "Pending" selamanya tanpa alasan dan tanpa tahu harus bayar ulang; persetujuan screenshot QRIS membalas `payment_updated:false` (Hook otoritatif — keputusan benar, komunikasinya nol). Semua kabar best-effort dan tidak pernah melempar. (d) **Guard order ganda tidak konsisten**: jalur keranjang menegakkan satu pending per chat, tetapi beli-langsung + katalog hanya mencocokkan `variant_id` → satu chat bisa pegang 2 order pending + 2 QRIS aktif + stok tertahan ganda; kini ketiganya per-chat, varian sama tetap diprioritaskan agar kirim-ulang invoice (RR3-05) tak bergeser. (e) `items LIKE '%"product_id":1%'` juga cocok dengan `10`/`11`/`100` karena JSON tersimpan tanpa spasi (dibuktikan langsung pada data produksi) — pembeli produk #10 ditolak `alreadyPendingMessage` gara-gara pending produk #1; kini LIKE berpembatas. **DITOLAK setelah diperiksa**: N2 "GET fulfillment membocorkan ≤100 secret plaintext yang tak dirender" — premisnya salah, agen memeriksa `FulfillmentInventoryPanel` yang DEAD CODE; panel hidup (`NonWrFulfillmentPanel`) memang merender kredensial itu secara sengaja (baris 135 & 149) sebagai fitur reveal admin. **BELUM dikerjakan (sengaja, butuh keputusan/scope lebih besar)**: C-H2 jendela QRIS WA 15 mnt tanpa reissue vs 60 mnt + 1 reissue di web/Telegram (keputusan produk, bukan bug), dan N1 retry WR tanpa verifikasi sisi-vendor (butuh panggilan API WR baru) — src/lib/telegram/handlers/{orders,invoice,catalog,discovery}.ts, src/lib/notify-buyer.ts (baru), src/app/api/orders/route.ts, src/app/checkout/page.tsx, src/app/api/admin/proofs/[id]/route.ts, src/app/api/admin/orders/[code]/handover/route.ts, tests/buyer-silence-and-atomicity.regression.test.ts (9 baru), docs/ARCHITECTURE.md §15 — (verifikasi: **979/979 test [97 file] hijau**, tsc bersih, dev GET / 200 + CSS 200 + /admin 200 + /checkout 200 + /lacak-pesanan 200 tanpa error compile; **mutation-test 3 putaran, masing-masing MEMERAH di test yang TEPAT**: gerbang ledger dibuang → test atomisitas memerah; `price_changed` dibuang → test harga memerah; guard per-varian dikembalikan → test satu-pending-per-chat memerah; skenario balapan diuji sebagai PERILAKU dengan fixture D1 nyata: order `lunas`+`paid` → klaim pembatalan mengembalikan `changes=0`, ledger tetap `paid`, stok tidak berubah)

- 2026-09-22 — **Audit UX admin: 8 temuan High divalidasi lalu diperbaiki — status pengiriman berhenti berbohong dan order gagal punya jalan keluar.** Validasi 3 laporan agen (19 + 33 klaim); pola akarnya satu: **server sudah tahu jawabannya, UI membuangnya**. (a) **P0 kebohongan status**: `deliverWhatsAppCredential` `return` diam saat kill-switch `WHATSAPP_CREDENTIAL_DM_ENABLED` mati → `processCredentialDelivery` tetap menulis `delivery_status='delivered'` walau NOL pesan terkirim; pembeli kanal WhatsApp melihat "Selesai" dan menerima nihil, admin mengira beres. Kini throw `whatsapp_credential_dm_disabled` → retry → `failed` → antrean handover. Kanal WEB sengaja TETAP boleh skip diam (punya token capability + panel + email sebagai jalur ambil); kanal WhatsApp tidak punya jalur lain — pembedaan itu yang diuji. (b) **P0 jalan buntu**: `fulfillment_status='failed'` tak punya tombol apa pun padahal `recordManualHandoverDetailed` menerimanya (`["manual_required","retry","queued","failed"]`) — order lunas yang gagal kirim hanya bisa dipulihkan lewat operasi DB; `needsHandover` kini mencakup `failed`, dan cabang `'retry'` yang **tidak pernah ditulis kode mana pun** ditinggalkan sebagai kode mati. (c) **P0 gerbang retry WR TERBALIK**: UI `["pending","retry","failed"]` vs API `RETRYABLE` `[...,"blocked_balance"]` — `blocked_balance` (retryable, `attempt_count=0`, status order `AXV-20260918-BE018D96` yang tertahan di produksi) tak pernah dapat tombol, sedangkan `failed` selalu dapat tombol padahal DIJAMIN ditolak `max_attempts_reached` (status itu hanya ditulis saat `attempt >= max_attempts`). Kini `RETRYABLE_WR_STATUS` + cek kuota percobaan + badge "Percobaan habis". (d) Status `claimed`/`submitted`/`blocked_balance` akhirnya punya label (dulu teks mentah `blocked_balance` berwarna kuning generik) + bisa difilter (`allowed` di `admin/warung/orders`). (e) Jalur fulfillment modern gagal **dalam diam**: `delivery/send.ts:processItem` membuang `adminChatId` dengan `void` padahal jalur legacy mengirim ping Telegram; `scheduleItemRetry` kini mengembalikan `true` HANYA pada transisi terminal (berbasis `meta.changes`, jadi fence yang kalah race tidak alert ganda). (f) Toast palsu: approve screenshot QRIS membalas `payment_updated:false` (QRIS Hook otoritatif, order tetap pending) tapi admin selalu melihat "dikonfirmasi lunas" — risiko kirim barang sebelum uang masuk. (g) `proofRejectionReason` + `fulfillment_status` sudah di-parse tapi tak pernah dirender; modal Detail juga kehilangan tombol Tolak + handover. (h) Produk WR: blok harga/stok mode **single** tidak terkunci seperti Nama/Slug (toggle Variasi OFF → edit yang dijamin 409), dan `field` pada 409 dibuang sehingga admin tak tahu field pemicunya — kini disebut namanya + pesan ownership menegaskan yang MASIH milik admin (foto, badge, harga coret, min. beli, override deskripsi). **DITOLAK setelah diuji** (klaim agen lain yang tidak terbukti): "sync membangkitkan varian WR yang dinonaktifkan" (dibuktikan lewat sweep nyata: `is_active` tetap 0 — sync tak pernah menulis kolom itu), "DELETE kurang guard WR" (`is_active` milik admin), "stock=-1 merusak low-stock" (`BETWEEN 0 AND 5` sudah mengecualikan, UI merender ∞), "`WR_SUBMITTED_STALE_MINUTES` tak dipakai" (dipakai `order.ts:540`, `submitted` basi direkonsiliasi `:707` + di-alert `:756`), "handover multi-item error membingungkan" (sudah memfilter `pending` + jalur pemulihan RR4-03), "banner override tak konsisten" (field-nya memang WR-only), dan `VariantEditor.tsx` **dead code** (nol import — sumber 3 temuan palsu) — src/lib/warung-rebahan/deliver.ts, src/lib/fulfillment/delivery/send.ts, src/lib/warung-rebahan/ownership.ts, src/components/admin/{OrdersManager,WarungRebahanManager,ProductEditorModal,useProductManager}, src/app/api/admin/warung/orders/route.ts, tests/{admin-ux-dead-end.regression,admin-failed-order-actionable.behavior,warung-rebahan/wa-credential-killswitch.regression} (11 baru), docs/ARCHITECTURE.md §15 — (verifikasi: **970/970 test [96 file] hijau**, tsc bersih, dev GET / 200 + CSS 200 + /admin 200 + /lacak-pesanan 200 tanpa error di /tmp/axvara-dev.log; **mutation-test 5 putaran, masing-masing MEMERAH di test yang TEPAT**: kill-switch dikembalikan `return` diam → delivery jadi `delivered` palsu; `failed` dibuang dari `needsHandover` → test berkas DAN test perilaku render memerah ("Serahkan manual" hilang); `blocked_balance` dibuang dari `RETRYABLE_WR_STATUS` → test selaras-API memerah; label `blocked_balance` dihapus → test kelengkapan status memerah; **bukti visual**: panel Pesanan dirender sungguhan lalu di-screenshot — badge merah "Kirim GAGAL" + tombol "Serahkan manual", badge kuning "Perlu handover", dan baris "Bukti ditolak: Nominal tidak cocok dengan mutasi" tampil benar)

- 2026-09-22 — **Sweep katalog WR di-batch: 642 → 117 round-trip D1 (−82%).** Jawaban atas "kenapa D1 lambat / apakah batas waktu dinaikkan?" — diukur, bukan diperkirakan, dan hasilnya membalik dugaan. "D1 lambat" BUKAN D1-nya: `sql_duration_ms` produksi hanya **0,12–0,19 ms**, tetapi sweep memakai **~197 ms per query**, jadi **99,9% durasi sweep adalah menunggu jaringan**. Sebabnya D1 primary di **SIN** (`served_by_colo: SIN`, `read_replication: null`) sementara Pages Function jalan di colo terdekat pemanggil → tiap round-trip menyeberang benua; bukti beban IDENTIK 48 produk/87 varian: **8.551 ms (178 ms/produk) vs 122.473 ms (2.552 ms/produk)**, selisih 14x tanpa perubahan pekerjaan, dan tren harian memburuk 243 → 2.372 ms/produk (11 → 22 Sep). Perbaikan: tulis satu produk dikumpulkan sebagai rencana (`SqlWrite`) lalu dikirim SATU `d1.batch()`, plus prefetch massal baris pembanding (`wr_variants` + registry `wr_products` + verifikasi tautan katalog) via `IN (...)` dipotong per 50 id (D1 menolak >100 bound parameter). **Cakupan batch SENGAJA per produk, bukan per sweep** — `batch()` adalah transaksi, jadi satu produk bermasalah tidak boleh membatalkan produk lain; varian/produk BARU tetap berurutan karena butuh `lastInsertRowid`, dan bila batch gagal tulis diulang satu per satu agar statement `optional` (dulu `.catch()`) kembali toleran. **KEPUTUSAN: `RUN_DEADLINE_MS` TIDAK dinaikkan** — pengikat sebenarnya CPU, bukan deadline: produksi mencatat **270 invocation `exceededResources` dengan `cpuTimeP50` mentok 10.000 µs** (plafon CPU Workers Free) pada irama cron 5 menit, jadi menaikkan budget waktu justru memperbanyak run yang dibunuh sebelum menulis log — mode kegagalan yang PR #3 tutup. Kolom prefetch dijaga PERSIS sama dengan SELECT per-varian lama (sengaja TIDAK menambah `wr_product_name` walau JOIN-nya mudah: SELECT lama juga tidak mengambilnya, jadi menambahkannya akan mengubah kelas pengiriman yang ditebak `guessDeliveryClass` — itu perubahan perilaku, bukan performa) — src/lib/warung-rebahan/sync.ts, tests/warung-rebahan/sync-roundtrip.regression.test.ts (3 baru), docs/ARCHITECTURE.md §15+§16.6, README.md — (verifikasi: **959/959 test [93 file] hijau**, tsc bersih, dev GET / 200 + CSS 200 + /admin 200; **mutation-test 2 putaran, masing-masing memerah di test yang TEPAT**: (1) batching dimatikan → round-trip MEMERAH 474 > 200; (2) cakupan batch dilebarkan ke seluruh sweep → test isolasi MEMERAH karena sabotase 1 varian menelan perubahan 86 varian lain; angka 642/117 diukur dengan menghitung ROUND-TRIP (batch = 1 perjalanan) pada fixture 48 produk/87 varian, dan baris tertulis tetap **12** sehingga guard kuota PR #4 tidak bergeser; perubahan stok nyata 10→3 diuji tetap merambat penuh ke `wr_variants` + `product_variants` + agregat induk; **ekuivalensi dibuktikan eksplisit**: skenario yang sama dijalankan di jalur batch dan jalur berurutan lalu isi `wr_variants`/`product_variants`/`products`/`wr_products` dibandingkan — IDENTIK, jadi batching murni optimasi; screenshot home + /admin render normal via Chromium headless karena `obscura` tidak tersedia di sandbox Linux ini)

- 2026-09-20 — Temuan review eksternal (2 valid + 1 ditolak): (a) varian stok<min (mis. stok 3/min 50) diperlakukan tak tersedia di PDP/modal/cart/checkout-direct — qty berapa pun pasti gagal di quote, jadi tolak sejak awal (label "STOK < MIN"/"Stok < min", cart menolak add + mengeluarkan baris basi); (b) GET /api/products/:id memuat SEMUA varian termasuk nonaktif — admin bisa reaktivasi + tak ada lagi risiko nonaktif permanen via `id NOT IN`; (c) DITOLAK: QRIS paid "bisa diunduh selamanya" — endpoint image sengaja melayani paid (riwayat), payload QR tetap terekspos via /api/orders/[code], proteksi anti-enumerasi sudah di rate-limit + masking — src/app/produk/[slug]/product-detail-client.tsx, src/components/storefront/QuickVariantModal.tsx, src/stores/cart.ts, src/app/checkout/page.tsx, src/app/api/products/[id]/route.ts, tests/{variant-min-qty,cart-store,quick-variant-modal,admin-variant-load-failure} — (verifikasi: 956/956 test, tsc bersih, build:pages sukses, dev GET / + CSS + /admin 200)

- 2026-09-20 — **Kuota tulis D1 dipangkas 97%: sync WR berhenti menulis ulang katalog yang tidak berubah.** Jawaban atas pertanyaan "aman tidak CF Free untuk WR + SekaliPay?" — diukur, bukan diperkirakan. Sweep katalog berjalan tiap 5 menit dan SELURUH `UPDATE`-nya tanpa syarat, sehingga sweep yang menemukan nol perubahan tetap menulis **398 baris** (96 `wr_variants` + 96 `product_variants` + 96 agregat `products` + 48 `wr_products` + 48 deskripsi + state/log) hanya untuk menyegarkan `last_synced_at`/`updated_at` — padahal `last_synced_at` **tidak dibaca kode mana pun** (kesegaran sync dibaca dari `wr_sync_log`; diverifikasi `rg` di `src/`, `tests/`, `mcp-worker/` = nol pembaca). Dikali ~56 sweep/hari = ~28k rows-written/hari, **cocok dengan puncak produksi nyata 28.518 (18 Sep) = 28,5% kuota Free 100k**; headroom hanya 3,5x padahal sejak 1 Sep 2026 kuota tulis habis = query D1 DIBLOKIR (toko mati sampai tengah malam UTC), bukan lagi sekadar peringatan. Perbaikan: setiap `UPDATE` sync memakai guard null-safe `IS NOT` (bukan `<>`, yang diam-diam gagal pada kolom NULL seperti `wr_terms`/`duration_label`), termasuk agregat induk yang kini membandingkan hasil subquery-nya sendiri, dan `writeSyncState` yang melewati tulis bila nilainya sama. Terukur: sweep tanpa perubahan **398 → 12 baris (−97%)**, proyeksi ~28k → ~1k writes/hari sehingga WR + SekaliPay muat di Free tanpa upgrade. Perubahan NYATA tetap merambat penuh ke tiga lapis (varian 10→3, katalog 10→3, agregat induk ikut turun) — sengaja diuji karena inilah satu-satunya cara guard ini bisa berbahaya — src/lib/warung-rebahan/sync.ts, tests/warung-rebahan/sync-write-quota.regression.test.ts (3 baru), docs/ARCHITECTURE.md §16.6 — (verifikasi: **950/950 test [92 file] hijau**, tsc bersih, dev GET / 200 + CSS 200 + /admin 200 + /lacak-pesanan 200, tanpa error di /tmp/axvara-dev.log, screenshot home render normal tanpa console error; **mutation-test: guard `wr_variants` dibatalkan → test kuota MEMERAH** (28 > 15) lalu dipulihkan hijau; angka 398 & 12 diukur dengan menghitung `meta.changes` tiap statement pada fixture 48 produk/96 varian, bukan menghitung statement)

- 2026-09-20 — **Celah lanjutan ditutup: sweep parsial tidak lagi menunggu interval 30 menit.** Ditemukan saat menguji pertanyaan "apakah ke depan sudah aman?" — bukan dari laporan gejala. Dengan budget waktu, sweep yang berhenti karena WAKTU punya `errors` kosong sehingga `logSync` mencatatnya **`success`**; gerbang interval lalu membacanya sebagai "baru saja sukses" dan menahan lanjutannya setengah jam. Efeknya katalog 48 produk butuh **~90 menit** (4 potongan × 30 mnt) padahal kerjanya hanya ~2 menit CPU, dan selama itu harga/stok separuh katalog basi — gejalanya mirip "sync tersendat" walau mekanismenya beda. Perbaikan: cron melanjutkan SEGERA bila `products_cursor > 0`. Sinyalnya sengaja **cursor**, bukan `products_snapshot_complete` yang ambigu (migrasi 0029 menyeednya '0', jadi DB yang belum pernah sync tak bisa dibedakan dari sweep parsial tertunda — ketahuan karena test `skipped=interval` memerah); cursor ditulis `0` tepat saat sweep mencapai ujung daftar. `syncProducts` juga kini me-reset `products_snapshot_complete` ke '0' saat sweep berhenti di tengah (sebelumnya hanya pernah dinaikkan ke '1', tak pernah diturunkan) — src/app/api/cron/operations/route.ts, src/lib/warung-rebahan/sync.ts, tests/warung-rebahan/sync-time-budget.regression.test.ts (3 baru) — (verifikasi: 947/947 test [91 file] hijau, tsc bersih, dev GET / 200 + CSS 200 + /lacak-pesanan 200 + /admin 200; **mutation-test 2 putaran**: putaran pertama membongkar test LOLOS PALSU — assertion penanda '0' tetap hijau walau reset dibatalkan, karena fixture sudah berisi '0' dari migrasi; test diperbaiki dengan menyeed '1' lebih dulu, lalu mutation-test kedua benar MEMERAH; simulasi D1 lambat 314 ms/query: katalog tuntas dalam 4 run ≈ 20 menit, maju konsisten, tidak macet)

- 2026-09-20 — **Admission proporsional `duration_ms × 1,5` DIBUANG: ia mematikan sync WR secara PERMANEN.** Review perbaikan agent lokal (`570c13e`) lawan data produksi: gerbang `if (!hasTime(estimateMs)) skip` diukur terhadap `RUN_DEADLINE_MS` = 45 dtk, sehingga estimasi apa pun di atas 45 dtk **mustahil** terpenuhi — ambang matinya `duration_ms > 30.000 ms`. Pada `wr_sync_log` produksi **69 dari 104 sweep (66%) melewati ambang itu**, termasuk SELURUH blok 00:12–07:07 UTC (114–116 dtk) dan blok 15:06–18:26 UTC (50–54 dtk). Yang membuatnya fatal: jalur skip TIDAK menulis `wr_sync_log`, jadi `duration_ms` terakhir **membeku selamanya** — satu-satunya penulis nilai itu adalah sweep, dan sweep tak pernah diizinkan jalan lagi. Deadlock tertutup; pemulihan hanya lewat Force Sync manual (route admin tidak memakai gerbang ini). Ironisnya gerbang itu juga **tidak perlu**: begitu sweep punya budget waktu internal, durasi tercatat selalu ≤ budget sehingga tidak ada nilai beku yang bisa mengunci. **Yang DIPERTAHANKAN dari agent lokal:** checkpoint cursor tiap 8 produk (`WR_SYNC_CHECKPOINT_EVERY`) — itu perbaikan nyata dan saling melengkapi: checkpoint membuat run yang mati mendadak (deploy/recycle) tetap melanjutkan, budget waktu membuat run tidak mati mendadak sejak awal. Test upstream yang mengunci perilaku deadlock (`admission proporsional … → deadline SEBELUM fetch`, meng-assert `wr_sweep_estimate_ms >= 100000`) diganti test anti-deadlock yang meng-assert sweep TIDAK ditolak hanya karena sweep sebelumnya lambat — src/app/api/cron/operations/route.ts, tests/cron-deadline-poison.integration.test.ts, docs/ARCHITECTURE.md — (verifikasi: 940/940 test [91 file] hijau termasuk 22 test sync upstream, tsc bersih; **mutation-test: gerbang deadlock dikembalikan → test anti-deadlock MEMERAH**; deadlock disimulasikan angka-per-angka dari baris `wr_sync_log` nyata 115.448 ms → estimasi 173.172 ms > 45.000 ms selamanya)

- 2026-09-20 — Sweep WR resumable + admission proporsional (AKAR ketiga gap misterius: sweep cron 50–116 dtk vs gerbang 14 dtk + deadline 45 dtk — dimulai, kepotong tengah, NOL jejak, ulang, kepotong lagi; bukti: fetch 200/5mnt di log proxy + `duration_ms` 115448 vs manual 12184): (1) checkpoint cursor + `products_progress_at` tiap 8 produk (`WR_SYNC_CHECKPOINT_EVERY`) — run berikut melanjutkan; (2) admission = durasi sukses terakhir ×1,5, fallback = `TIME_WR_NETWORK` (fail-open; pelajaran: fallback 60 dtk mustahil lolos deadline 45 dtk — ditangkap 2 test cron lama); tak cukup → `deadline` SEBELUM fetch + `wr_sweep_estimate_ms`; (3) partial log + `budget_yielded` saat yield berkemajuan. RED→GREEN: checkpoint + admission gagal dulu tanpa kode — src/lib/warung-rebahan/sync.ts, src/app/api/cron/operations/route.ts, tests/warung-rebahan/sync.test.ts + tests/cron-deadline-poison.integration.test.ts, issue/wr-sync-resumable-jujur.md, docs/ARCHITECTURE.md (§16) — (verifikasi: 941/941 test hijau, tsc bersih, dev GET / 200 + CSS 200, Obscura OK + PNG nonblank)

- 2026-09-20 — **AKAR sync WR tersendat berminggu-minggu DITEMUKAN & DIPERBAIKI: sweep katalog tidak punya gerbang WAKTU.** Diagnosa dari data produksi (`wr_sync_log`), bukan pembacaan kode: sweep penuh = 48 produk / 87 varian = **~366 query D1 BERURUTAN**, dan biayanya ditentukan latensi D1 yang tidak kita kendalikan — beban IDENTIK terukur **12.184 ms saat D1 sehat (~33 ms/query) vs 115.448 ms saat D1 lambat (~314 ms/query)**; **68 dari 391 sweep (17%) melewati deadline cron 45 dtk**, 35 di antaranya >100 dtk (maks 122.473 ms). Akibat berantai: run dibunuh platform SEBELUM ekor handler menulis `wr_sync_log` + penanda fase → sweep tak tercatat, fase terkunci di `warung_rebahan`, dan jeda sync melonjak dari normal 39-40 mnt jadi **163 dan 305 mnt** (terverifikasi: selama jeda 18:26→23:32 UTC tidak ada satu pun baris cron, sementara saldo-sync manual dari UI tetap jalan — jadi bukan pemicu yang mati). Ini menjelaskan kenapa perbaikan-perbaikan sebelumnya tidak pernah menyelesaikan: semuanya mengatur KAPAN sweep dimulai (interval, rotasi fase, anti-starvation, watchdog), tak satu pun memastikan sweep BERHENTI sebelum invocation mati — loop hanya punya gerbang budget query (`canSpend`), tidak pernah gerbang waktu. Perbaikan: `SyncOptions.timeBudgetMs` + admission waktu ADAPTIF per iterasi (memakai latensi terukur run itu sendiri, bukan konstanta optimistis) yang berhenti di produk utuh terakhir, menyimpan cursor, dan melapor `budgetYielded` — jalur aman yang sudah terbukti dipakai saat budget query habis; cron mengoper `timeLeftMs() - TIME_WR_SWEEP_RESERVE` (8 dtk cadangan untuk reconcile/saldo/delivery + ekor penulis log). Force Sync admin tanpa `timeBudgetMs` = perilaku lama (tanpa batas) — src/lib/warung-rebahan/sync.ts, src/app/api/cron/operations/route.ts, tests/warung-rebahan/sync-time-budget.regression.test.ts (3 baru), docs/ARCHITECTURE.md — (verifikasi: **940/940 test [91 file] hijau**, tsc bersih, dev GET / 200 + CSS 200 + /lacak-pesanan 200 + /admin 200; **mutation-test: gerbang waktu dibatalkan → test D1-lambat MEMERAH**, jadi bukan hijau palsu; test memakai D1 lambat tiruan 300 ms/query + fake timer agar kondisi "D1 buruk" deterministik tanpa menunggu insiden nyata)

- 2026-09-20 — Susulan WIB-SKEW: halaman /artikel (list + detail) ketinggalan dari audit — `published_at` diformat langsung via `new Date(...)`; kini memakai `formatWibDateTime` + masuk daftar guard test — src/app/artikel/page.tsx, src/app/artikel/[slug]/page.tsx, tests/audit-2026-09-20.regression.test.ts — (verifikasi: 937/937 test [90 file], tsc bersih, GET /artikel 200)

- 2026-09-20 — Audit menyeluruh (kode + Cloudflare prod): 3 bug diverifikasi lawan data produksi lalu diperbaiki. (1) WIB-SKEW — D1 menulis `datetime('now')` sebagai UTC berformat spasi ("2026-09-19 16:25:13") sementara UI memanggil `new Date(nilai)` yang membacanya sebagai waktu LOKAL: di perangkat WIB SELURUH timestamp tampil mundur 7 jam (order 23.25 tampil 16.25) dan tanggalnya ikut salah bila melewati tengah malam; produksi terdampak total (27/27 orders.created_at, 25/25 payment_transactions.created_at, 601/601 wr_sync_log.created_at berformat spasi). Helper kanonis baru `formatWibDateTime` di src/lib/utils.ts memakai `parseExpiry` yang sudah dipakai cron/webhook, lalu mengunci `timeZone: "Asia/Jakarta"` agar admin dari zona mana pun melihat jam operasional toko; 7 permukaan tampilan dipindah ke helper ini. (2) CRON-TIMING — `/api/cron/operations` + `/api/cron/publish-scheduled` membandingkan bearer CRON_SECRET dengan `!==` padahal DANA/Telegram/WhatsApp/WR sudah memakai `constantTimeEqual`; kini seragam (tetap fail-closed saat CRON_SECRET kosong). (3) PROOF-LEAK — `GET /api/orders/[code]` (publik, cukup kode order) mengembalikan `proof_url` = kunci objek R2 PRIVAT, sementara endpoint kembarannya `GET /api/orders?code=` sudah tidak; field dicabut + invarian "dua endpoint harus sepadan" dikunci test. Cloudflare diperiksa menyeluruh dan BERSIH: D1 integrity_check ok + foreign_key_check 0 + 38/38 migrasi sinkron dengan repo, R2 axvara-assets privat (tanpa r2.dev/custom domain), Pages env 39 var semuanya `secret_text` + Git integration nonaktif (CI satu-satunya jalur deploy), Worker axvara-mcp aktif — src/lib/utils.ts, src/app/api/cron/{operations,publish-scheduled}/route.ts, src/app/api/orders/[code]/route.ts, src/app/lacak-pesanan/lacak-pesanan-client.tsx, src/components/{admin/{OrdersManager,PaymentReconciliation,WarungRebahanManager,NewsletterSubscribers,ArticlesManager,AgentIntegration},storefront/WrCredentialsPanel}.tsx, tests/audit-2026-09-20.regression.test.ts (12 baru), docs/{ARCHITECTURE,PRD,DESIGN}.md + README — (verifikasi: 937/937 test [90 file] hijau, tsc bersih, dev GET / 200 + CSS 200 + /lacak-pesanan 200 + /admin 200; mutation-test: ketiga perbaikan dibatalkan satu per satu dan test yang sesuai MEMERAH, jadi bukan test hijau palsu; browser zona Asia/Jakarta merender "Dibuat 6 Sep 2026, 20.50" — sebelumnya 13.50)

- 2026-09-20 — Urutan nonaktif dibalik: komparator `byActive` tertulis `Number(!b) - Number(!a)` sehingga produk yang dimatikan (API Testing, Canva WR di screenshot owner) nangkring di baris 1-2; kini `Number(!a) - Number(!b)` — nonaktif PALING belakang, sesuai komentar kode yang sudah benar sejak awal — src/components/admin/useProductManager.ts, tests/product-variant-save.integration.test.ts — (verifikasi: test sort 8/8, tsc bersih)

- 2026-09-20 — Merge PR #2 upstream (fork marylynnsigala#1 → Hasbi1605:main): audit panel admin + 8 bug + 4 butir approved owner; resolusi konflik CHANGELOG (append-only) + blok `filtered` useProductManager (gabung sort upstream + filter onlyLowStock + dep array) — src/components/admin/useProductManager.ts, CHANGELOG.md — (verifikasi: tsc bersih, 924/924 test [89 file], build:pages sukses, dev GET / 200 + CSS 200 + /admin 200)

- 2026-09-20 — Watchdog konteks tak-pernah-kosong (bukti prod: ping 06:32 "Sebab terakhir: -"): (1) `alertStaleWrSync` mencatat konteks awal (`sync_stale_alert_context`, "pre_phase" bila buta) saat menandai episode; (2) `refreshStaleWrSyncContext` dilonggarkan — syarat fase-WR-aktif dicabut, setiap run ber-sebab presisi boleh koreksi 1x/episode (run basi jarang pegang fase WR); RED→GREEN: test konteks gagal dulu di kode lama — src/lib/warung-rebahan/order.ts, tests/cron-deadline-poison.integration.test.ts — (verifikasi: 900/900 test [84 file] hijau, tsc bersih, dev GET / 200 + CSS 200, Obscura OK + PNG nonblank)

- 2026-09-20 — Review gate PR #1: keluarkan workflow sync fork dari branch + verifikasi merge sungguhan lawan upstream `8ffc7a0`:

- 2026-09-19 — Daftar produk admin meniru storefront (screenshot owner): ready dulu, stok habis belakangan, nonaktif PALING belakang (sebelumnya urutan DB mentah — Netflix/Capcut/Claude stok 0 nangkring di atas); kunci sort_order admin + id sebagai tiebreak, pola sama dengan page.tsx — src/components/admin/useProductManager.ts, tests/product-variant-save.integration.test.ts — (verifikasi: 897/897 test [85 file] hijau, tsc bersih)

- 2026-09-19 — formatVariantLabel khusus WR (insiden Canva screenshot owner): helper B2 menggabung durasi untuk SEMUA produk → non-WR Canva tampil "Invite Lifetime - 6 Bulan" + "Head 1 Bulan - 14 Hari" (DB: duration 6 month/14 day = duplikat kolom garansi, bukan display); kini append hanya bila wr_variant_id terisi (Meitu dkk tetap "- 7 Hari"), non-WR label admin final apa adanya; DB tak tersentuh (tak perlu edit manual, hilang sendiri setelah deploy) — src/lib/catalog.ts, tests/whatsapp-variants.regression.test.ts — (verifikasi: 896/896 test [85 file] hijau, tsc bersih)

- 2026-09-19 — Hint WA checkout: "Untuk pengiriman detail produk — tidak untuk spam." → "Untuk pengiriman detail produk, lacak pesanan, dan klaim garansi / reffund" (permintaan owner) — src/app/checkout/page.tsx — (verifikasi: 896/896 test [84 file] hijau)

 - 2026-09-19 — Anti-macet struktural sync WR (4 insiden berulang → tak perlu forensik manual lagi): (1) watchdog `alertStaleWrSync` — sweep >90 mnt (3x interval) → ping Telegram admin 1x/episode (umur + waktu terakhir + sebab + arahan Force Sync), idempoten via `wr_sync_state`, reset saat sweep sukses, best-effort ≤4 query; (2) `wr_sync_skipped` penuh — `budget_yielded`/`attempted_failed` (+ `wr_sync_errors` 3 pertama), tak ada lagi attempted-tanpa-jejak; (3) audit Force Sync: SUDAH BENAR (bypass interval + status jujur + dashboard bedakan success/partial/failed) — pemulihan mandiri pemilik, tanpa perubahan; RED→GREEN: 3 test watchdog gagal tanpa kode — src/app/api/cron/operations/route.ts, src/lib/warung-rebahan/order.ts, tests/cron-deadline-poison.integration.test.ts (4 baru), issue/wr-sync-anti-macet-struktural.md, docs/ARCHITECTURE.md (§16) — (verifikasi: 896/896 test [84 file] hijau, tsc bersih, dev GET / 200 + CSS 200, Obscura home OK + PNG nonblank)
- 2026-09-20 — Review gate PR #1: keluarkan workflow sync fork dari branch + verifikasi merge sungguhan lawan upstream `8ffc7a0`: (1) `docs/workflows/sync-upstream.yml` + testnya + §sync di ARCHITECTURE/README/CHANGELOG DIREVERT dari branch ini (reviewer benar: guard `github.repository == fork` membuatnya jadi dokumen mati bila ikut ter-merge ke upstream) — kerjanya utuh dipindah ke branch `hoplite/leukas-d42cd0bf--fork-sync` untuk PR terpisah khusus fork; branch ini kini murni perubahan panel admin; (2) angka 909/924 lama memang dihitung dari base lama — diulang penuh melawan upstream terkini `8ffc7a0`: 2 konflik (CHANGELOG append-only + blok `filtered` di useProductManager) diselesaikan dengan MENGGABUNG sort upstream + filter onlyLowStock (bukan memilih salah satu, `onlyLowStock` masuk dependency array), lalu KETIGA gate CI dijalankan di hasil gabungan — `tsc` bersih, **924/924 test [89 file]**, dan `npm run build:pages` SUKSES 17,36 dtk (gate ketiga ini belum pernah dijalankan siapa pun di hasil merge); `outbox.ts` tetap auto-merge berkat konvergensi ke `fbc7235`; (3) `issue/sync-upstream-hasbi-main.md` diperbarui dengan angka terverifikasi + perintah 3-gate — issue/sync-upstream-hasbi-main.md, CHANGELOG.md (+ revert 5c02f8d) — (verifikasi: branch ini 915/915 [89 file] hijau + tsc bersih; hasil merge vs 8ffc7a0 924/924 + tsc bersih + build:pages sukses; kekhawatiran reviewer soal `node:sqlite` di runner CI DIJAWAB — dijalankan di Node 22 persis seperti ci.yml: test baru 4/4 dan suite penuh 915/915 hijau, lagipula fixture D1 ini sudah dipakai 44 file test yang SUDAH ADA sebelum PR ini (koreksi dari "33" yang pernah saya tulis — glob `tests/*.ts` tidak menjangkau subdirektori seperti `tests/warung-rebahan/`; hitung ulang rekursif tanpa helper pendefinisi = 44); merge percobaan di-abort, tidak ada commit upstream di branch PR)

- 2026-09-20 — Panel admin: 4 butir yang disetujui owner (1/1b/3/4 dari audit; butir 2 DIBATALKAN setelah isinya dibaca — WR = urusan supplier, Kanal = urusan kanal, digabung justru jadi satu halaman dua konteks): (1+1b) modal produk dipecah jadi tab Produk/Varian/Foto dengan header tetap + badan scroll + footer Simpan `shrink-0` — terukur di browser untuk produk 5 varian: tombol Simpan dulu 2597px di bawah area kerja, kini SELALU terlihat; scroll per tab Produk 388px & Foto 239px (dulu satu kolom 3126px/46 kontrol), tab Varian tetap 2370px karena memang berisi 5 baris; (3) `Integrasi Agent` + `Subscriber Email` turun jadi tab di Pengaturan Toko via komponen baru `SystemTabs` (sidebar 12 → 10 menu); `?section=agent`/`?section=subscribers` TETAP sah + judul fallback untuk header mobile, diverifikasi browser membuka tab yang benar; (4) BUG: query stats `/api/admin/orders` berjalan TANPA `where` sementara query daftar memakainya — filter ke Pending tetap menampilkan "Total pesanan 26" (kartu dan daftar di layar sama bercerita beda); stats kini memakai `${where}` + bindings yang sama, kartu diberi penanda "hasil filter", dan tab kanal sengaja TETAP global (ia pemilih) sehingga "Semua" dihitung dari counts kanal, bukan stats.total yang kini terfilter — src/app/api/admin/orders/route.ts, src/app/admin/page.tsx, src/components/admin/{SystemTabs (baru),AdminShell,ProductEditorModal,OrdersManager}.tsx, tests/{admin-orders-stats-filter.integration (4 test, RED→GREEN: 2 test filter memerah di kode lama sementara test tanpa-filter & counts-kanal tetap hijau),admin-approved-ui.regression (8 guard)}, docs/{PRD (FR-SYS12),DESIGN (§5.8 form panjang = tab + aksi sticky),ARCHITECTURE (kontrak navigasi + catatan stats vs counts)} + README + audit — (verifikasi: 915/915 test [89 file] hijau di branch ini, tsc bersih, dev GET / 200 + CSS 200, browser: tab modal + Simpan sticky terlihat tanpa scroll, sidebar 10 menu, ?section=agent & subscribers membuka tab benar; DIVERIFIKASI ULANG 2026-09-20 di hasil merge melawan upstream `8ffc7a0` — 924/924 test [89 file], tsc bersih, `npm run build:pages` sukses 17,36 dtk)

- 2026-09-19 — Audit panel admin + perbaikan bug navigasi/konsistensi (laporan owner "acak-acakan, fitur mati"): (1) AUDIT `issue/audit-admin-panel-2026-09-19.md` — 6 bug terkonfirmasi + 8 temuan UX, tiap butir berbukti file:baris atau eval DOM; satu dugaan (thumbnail bukti kosong) DIPERIKSA dan dicoret karena `ProofThumbnail.tsx:54` sudah menanganinya; (2) kartu "Stok menipis" Ringkasan dulu hanya pindah tab (`params: {}` dan ProductsSection tak baca URL sama sekali) — sekarang mengirim `?low_stock=1`, diterapkan di `navigateAdmin` (pushState tak memicu popstate) dan disinkronkan ulang saat back/forward; (3) satuan "Stok menipis" disamakan: Ringkasan hitung varian (66) vs Produk hitung produk (33) untuk label yang sama — `/api/products` kini mengirim `lowStockVariants` per produk (varian aktif stok 0–5, hanya mode D1 agar fallback dev tidak tertindih); (4) filter `proof` di Pesanan sebelumnya menyaring diam-diam tanpa satu pun kontrol di layar (setProof cuma dipakai clearFilters) — semua filter aktif kini jadi chip yang bisa dilepas satu per satu; (5) section Produk satu-satunya tanpa heading (eval DOM: `[]`) — ditambah h2 + deskripsi; (6) `confirm()`/`alert()` bawaan browser di 5 tempat (kategori, banner, artikel, 2 upload) diganti ConfirmDialog/toast bertema; (7) modal Kategori jadi dialog sah: `role=dialog` + `aria-modal` + Escape + scroll-lock; (8) RISIKO DATA: `openEdit` menelan kegagalan KEDUA sumber varian lalu me-reset form jadi kosong tanpa pesan — dari keadaan itu Simpan mengirim `variants: []` yang memicu `UPDATE product_variants SET is_active=0 WHERE product_id=?` (products/[id]/route.ts:270), jadi satu hiccup jaringan bisa menonaktifkan SELURUH varian produk; kini melempar + formError + toast "jangan simpan sebelum varian tampil"; (9) DICATAT (belum dieksekusi): `VariantEditor.tsx` + `FulfillmentInventoryPanel.tsx` tak pernah diimpor siapa pun (~360 baris mati) tetapi DIPAKUKAN test payment-proofs/variant-min-qty — suite hijau ikut "membuktikan" file yang tak pernah dirender, akar historis panel kredensial yang hilang; menghapusnya harus sekalian mengalihkan test ke ProductVariantRows — (10) BONUS di luar admin: flake CI `wa-outbox-lease` (5009 ms vs batas 5 dtk Vitest) berakar pada jitter outbox WA yang dibaca dari konstanta modul sehingga `WHATSAPP_OUTBOX_PACE_MS=0` tetap menyisakan tidur acak 0-3 dtk per kirim; kini base 0 berarti TANPA jeda sama sekali termasuk jitter (guard `if (base === 0) return 0`) — durasi 2174 ms → 8 ms; implementasi sengaja DISELARASKAN dengan perbaikan upstream `Hasbi1605:main` fbc7235 yang menambal flake yang sama, supaya sync berikutnya tidak bentrok di file ini (env tambahan dibatalkan) — src/lib/whatsapp/outbox.ts, tests/wa-outbox-lease.integration.test.ts, .env.example, src/app/api/products/route.ts, src/app/admin/page.tsx, src/components/admin/{AdminOverview,OrdersManager,CategoryManager,BannerManager,ArticlesManager,ArticleEditor,ImageDropzone,useProductManager,product-types,sections/ProductsSection}, tests/{admin-low-stock-filter.behavior,admin-native-dialogs.regression,admin-variant-load-failure.behavior} (12 test baru, RED→GREEN terkonfirmasi untuk kartu low-stock dan gagal-muat-varian), docs/{PRD (FR-SYS11),DESIGN (§5.8),ARCHITECTURE (kontrak navigasi admin)} + README — (verifikasi: 903/903 test [87 file] hijau, flake wa-outbox dinetralkan, tsc bersih, lint tanpa error baru, dev GET / 200 + CSS 200, browser: heading Produk muncul, chip proof tampil lalu terhapus dari URL, Escape menutup modal kategori + scroll-lock lepas, dialog hapus artikel bertema tanpa confirm() native)

- 2026-09-19 — Badge shared menipu "Tersedia 0" (bug screenshot owner): badge counts (Tersedia/Terpakai/Dipesan) membaca tabel fulfillment_inventory yang memang 0/0 untuk mode shared — padahal datanya ada di kolom shared_secret_ciphertext (kotak "ISI SAAT INI: hasbii" tampil benar). Mode shared kini badge-nya status pesan ("Pesan bersama aktif" / "Belum ada isi"), counts inventory hanya untuk unique — src/components/admin/sections/ProductVariantRows.tsx, tests/variant-min-qty.test.ts — (verifikasi: 891/891 test [84 file] hijau, tsc bersih)

- 2026-09-19 — Observability sync WR: skip jujur + heartbeat (akar gap 07:12→10:24 UTC tak terlihat — `synced:0 + skipped:null` ambigu untuk 4 kondisi): (1) `wr_sync_skipped` kini terisi di SEMUA jalur — `disabled` (switch mati/tabel belum siap), `phase_inactive` (fase tak aktif, rotasi normal), `interval` (sweep <30 mnt, tersering), `sync_disabled` (saklar sync off) + lama `deadline`/`query_budget`; (2) `wr_last_sync_at` di respons (sweep terakhir tanpa query D1); (3) heartbeat `store_settings.cron_last_hit_at` tiap hit (1 stmt best-effort — baca: heartbeat segar + sync basi = run kepotong deploy, keduanya basi = pemicu mati); perilaku sync TAK DIUBAH (ambang/budget/guard sama) — src/app/api/cron/operations/route.ts, tests/cron-deadline-poison.integration.test.ts (5 baru, RED→GREEN terkonfirmasi), issue/wr-sync-observability-skip-jujur-heartbeat.md, docs/ARCHITECTURE.md (§16) + README.md — (verifikasi: 891/891 test [84 file] hijau, tsc bersih, dev GET / 200 + CSS 200, Obscura home OK + PNG nonblank)

- 2026-09-19 — Perbaiki CI failure prefer-const `inventory` di route fulfillment admin (`let` → `const`, tak pernah di-reassign; hanya push) — src/app/api/admin/fulfillment/route.ts — (verifikasi: 886/886 test [84 file] hijau, tsc bersih, dev GET / 200 + CSS 200, Obscura home OK + PNG nonblank)

- 2026-09-19 — Panel fulfillment tampilkan isi + auto-load (keputusan owner: admin MELIHAT kredensial): (1) akar counts tetap 0: counts hanya di-load manual (tanpa auto-load) + endpoint GET tak balikan isi + DB tulis ke kolom ciphertext (D1 prod: Canva Invite 1 Bulan shared=1) sementara counts baca inventory (0/0 untuk semua non-WR) — pantau dua sumber, bukan satu; (2) GET /api/admin/fulfillment me-reveal plaintext server-side (shared_secret + inventory available ≤100, requireAdmin, tanpa ke pembeli); panel shared tampilkan "Isi saat ini" + tombol Ganti, unique tampilkan daftar stok + Hapus per baris, auto-load via useEffect saat dibuka/mode berubah; (3) CATATAN DATA: simpanmu kemarin sukses tulis ciphertext (bukan hilang) — tapi mode dropdown baru belum ada saat itu sehingga varian tetap manual; buka Edit → Cara Pengiriman = shared/unique → Simpan produk → isi tampil — src/app/api/admin/fulfillment/route.ts, src/components/admin/sections/ProductVariantRows.tsx, tests/variant-min-qty.test.ts (reveal integration) — (verifikasi: 886/886 test [84 file] hijau, tsc bersih)

- 2026-09-19 — Hapus kalimat "Made By Order — dikerjakan sesuai antrean…" dari QuickVariantModal (keputusan owner): badge per kartu varian tetap, ekspektasi detail hanya di blok checkout "Made By Order" — src/components/storefront/QuickVariantModal.tsx, tests/wr-queued-delivery.regression.test.ts (guard: modal wajib tidak mengandung deliveryEtaForBuyer) — (verifikasi: 885/885 test [84 file] hijau, tsc bersih)

- 2026-09-19 — Opsi pengiriman non-WR di admin + kembalikan panel fulfillment yang hilang: (1) audit D1 prod: 10/10 varian non-WR (Canva, Gsuite) fulfillment_mode=manual — display buyer lama (cek wr_delivery_class saja) membuat semuanya jatuh ke MBO + estimasi supplier 6–12 jam yang bukan miliknya; (2) helper baru buyerDeliveryKind/Badge/EtaNonWr di catalog.ts (WR ikut wr_delivery_class, non-WR ikut fulfillment_mode; ETA non-WR tanpa angka supplier); PDP + QuickVariantModal fulfillment-aware; query catalog membawa wr_variant_id; (3) admin ProductVariantRows: dropdown "Cara Pengiriman" (Made By Order / Kirim otomatis pesan bersama / Kirim otomatis stok unik, sinkron 1:1 fulfillment_mode + preview badge) + panel konten NonWrFulfillmentPanel (shared/unique via /api/admin/fulfillment; varian baru diminta simpan dulu); useProductManager memetakan + mengirim fulfillment_mode (PUT /api/products/:id sudah dukung); AKAR HILANG: satu-satunya panel isi kredensial (FulfillmentInventoryPanel) hanya dirender VariantEditor yang tak dipakai halaman mana pun — produk non-WR jalur resmi tak pernah bisa diisi stok — src/lib/catalog.ts, src/app/produk/[slug]/product-detail-client.tsx, src/components/storefront/QuickVariantModal.tsx, src/components/admin/{sections/ProductVariantRows,useProductManager,product-types,ProductEditorModal}.tsx, src/app/admin/page.tsx, tests/{warung-rebahan/delivery-class,variant-min-qty} — (verifikasi: 885/885 test [84 file] hijau, tsc bersih)

- 2026-09-19 — Copy Made By Order checkout di-reframe positif (keputusan owner): "{produk} dibuat setelah pembayaran terkonfirmasi. Umumnya terkirim cepat, maksimal 12 jam pada jam layanan." — kata antre/ramai/sabar + rentang 6–12 jam dibuang; komentar kode diselaraskan — src/app/checkout/page.tsx, tests/wr-queued-delivery.regression.test.ts (guard: wajib ada kalimat baru, wajib tidak ada frasa lama) — (verifikasi: 881/881 test [84 file] hijau, tsc bersih)

- 2026-09-19 — Mikrocopy WA checkout: hint "Untuk pengiriman detail produk — tidak untuk spam." di bawah input No WA (aria-describedby) agar pembeli tak ragu memberi nomor — src/app/checkout/page.tsx — (verifikasi: 881/881 test [84 file] hijau, tsc bersih)

- 2026-09-19 — Potong "Biarkan halaman terbuka;" dari ② Verifikasi Otomatis checkout — src/app/checkout/page.tsx — (verifikasi: 881/881 test [84 file] hijau, tsc bersih)

- 2026-09-19 — Potong catatan mikro CTA checkout → "QRIS diverifikasi otomatis." (tanpa embel-embel upload bukti) — src/app/checkout/page.tsx — (verifikasi: 881/881 test [84 file] hijau, tsc bersih)

- 2026-09-19 — Hapus blok detail "QRIS dinamis dibuat setelah pesanan…" dari paymentBlock checkout (redundan dengan ② Verifikasi Otomatis di kiri) — src/app/checkout/page.tsx — (verifikasi: 881/881 test [84 file] hijau, tsc bersih)

- 2026-09-19 — Batch C revisi anti-redundan (feedback owner + screenshot): (1) mini-blok MBO + trust sebaris "QRIS otomatis • Garansi replace • Support WA admin" DIHAPUS dari rail kanan (estimasi sudah di kiri, trust sudah di hero); (2) gaya WR penuh — Metode Pembayaran + S&K agreement + error + CTA pindah ke rail kanan (variabel paymentBlock/agreeBlock satu definisi, state tetap satu sumber); kiri hanya ① Data Pembeli + ② Verifikasi Otomatis + blok Made By Order (+ error/sticky-mobile); 2 tombol submit berbagi handler ctaDisabled/ctaLabel — src/app/checkout/page.tsx, tests/checkout-integrity.regression.test.ts (guard: paymentBlock+agreeBlock di rail, tanpa Trust rail, 2× onClick submit) — (verifikasi: 881/881 test [84 file] hijau, tsc bersih)

- 2026-09-19 — Batch C layout checkout action-rail (analisa WR vs Sekalipay): kiri = form 3 step bernomor yang sudah ada (① Data, ② Metode QRIS pre-selected, ③ Verifikasi Otomatis + blok Made By Order + agreement + CTA); kanan = aside sticky "Ringkasan dan pembayaran" (ringkasan + total + mini-blok Made By Order + trust QRIS otomatis/Garansi replace/Support WA + CTA duplikat desktop + catatan tanpa-upload-bukti); mobile = accordion "Ringkasan Pesanan" (buka default, total selalu terlihat di header-nya) + CTA sticky bottom fixed + spacer 68px; 3 tombol submit berbagi satu handler ctaDisabled/ctaLabel (tanpa logika ganda); tanpa field/metode/voucher/logic quote baru; MobileBottomNav tetap hidden di /checkout — src/app/checkout/page.tsx, tests/checkout-integrity.regression.test.ts (guard struktur baru: aside sticky + 3× onClick submit + accordion + sticky CTA) — (verifikasi: 881/881 test [84 file] hijau, tsc bersih)

- 2026-09-19 — Batch B2 audit durasi varian: audit D1 prod membuktikan BUKAN cuma Meitu — 88/97 varian aktif kehilangan durasi di label (sinkron WR menulis label=nama verbatim, durasi di kolom duration_*; PDP web hanya render label; web WR menggabung "Meitu VIP - 7 Hari"; Telegram sudah gabung via duration_label). Fix display global: helper baru `formatVariantLabel()` di src/lib/catalog.ts (append " - durasi" hanya bila belum terkandung, case-insensitive — "GSuite 1 Hari"/"Invite 1 Bulan" tidak ganda) dipakai PDP + QuickVariantModal + displayName quote checkout (ringkasan + blok Made By Order ikut "Meitu VIP - 7 Hari"); DB tak tersentuh (sync aman menimpa) — src/lib/catalog.ts, src/app/{api/checkout/quote/route,produk/[slug]/product-detail-client}.ts(x), src/components/storefront/QuickVariantModal.tsx, tests/{whatsapp-variants,checkout-integrity}.regression — (verifikasi: 880/880 test [84 file] hijau, tsc bersih)

- 2026-09-19 — Revisi copy antrean (keputusan owner): (1) template checkout → "{produk} dibuat setelah orderan masuk — dikerjakan sesuai antrean, estimasi 6–12 jam jika ramai, biasanya lebih cepat, mohon bersabar." (rentang atas permintaan owner; risiko dibaca janji minimum dicatat, plafon internal 12 jam tak berubah); (2) badge PDP + modal + badge pengiriman PDP → "Made By Order" polos (tanpa "· maks 12 jam"); impor WR_QUEUED_MAX_HOURS yang tak terpakai dibersihkan di 3 file — src/app/{checkout/page,produk/[slug]/product-detail-client}.tsx, src/components/storefront/QuickVariantModal.tsx, tests/wr-queued-delivery.regression.test.ts — (verifikasi: 878/878 test [84 file] hijau, tsc bersih)

- 2026-09-19 — Batch B1 badge keranjang = jumlah baris varian: selector baru `lineCount()` di `src/stores/cart.ts` (count() sum-qty utuh untuk subtotal); Navbar + judul CartDrawer pakai lineCount — GSuite min-50 1 baris tampil "1", Canva 10 + GSuite 50 tampil "2", kosong badge hilang; qty tetap per baris + ringkasan checkout — src/stores/cart.ts, src/components/storefront/{Navbar,CartDrawer}.tsx, tests/cart-store.behavior.test.ts (2 guard baru) — (verifikasi: 878/878 test [84 file] hijau)

- 2026-09-19 — Batch A copy+badge: (1) hero social proof vanity (1.200+ aktivasi/Rata-rata 8 menit/4.9/5) → trust signal verifikabel (QRIS otomatis/Garansi replace/Support WA admin); (2) "Diproses antrean" → "Made By Order" di sumber kanonis `deliveryLabelForBuyer`/`deliveryEtaForBuyer` (merambat PDP+modal+checkout+Telegram+WA) + badge kartu varian + blok checkout + halaman pesanan + garansi-replace; (3) feature list PDP baris pengiriman jadi badge tunggal (tanpa ikon checklist ganda); (4) checkout: judul "Waktu pengerjaan" → "Made By Order", template "dibuat setelah orderan masuk — dikerjakan sesuai antrean, maksimal 12 jam, mohon bersabar", paragraf "Detail akun dikirim ke WhatsApp…Tidak perlu menunggu" dihapus; (5) "DYOR, DWYOR." dihapus dari checkbox checkout (warranty-policy utuh) — src/app/{page,checkout/page,pesanan/[code]/page,produk/[slug]/product-detail-client,garansi-replace/page}.tsx, src/components/storefront/QuickVariantModal.tsx, src/lib/warung-rebahan/delivery-class.ts, tests/{warung-rebahan/delivery-class,wr-queued-delivery.regression,checkout-integrity.regression,order-status-delivery.behavior} — (verifikasi: 876/876 test [84 file] hijau)

- 2026-09-19 — Subheadline hero "hemat dari official" → "murah dibanding official" — src/app/page.tsx — (verifikasi: 876/876 test [84 file] hijau, dev GET / 200, Obscura sub OK + PNG nonblank)

- 2026-09-19 — Kill-switch auto-DM kredensial WA + hardening umur nomor BOT (pasca-restriction ~3,5 jam, "tidak bisa memulai chat baru"): (1) flag baru `WHATSAPP_CREDENTIAL_DM_ENABLED` default MATI — gate di `notifyWebBuyerCredentialsReady` + `deliverWebCredentialViaWhatsApp` + `deliverWhatsAppCredential` + legacy `sendToRecipient`/`processLegacyJob` (skip diam → settled via email + panel; order whatsapp → `manual_required`); KODE + MEKANISME UTUH, nyalakan lagi = 1 env + deploy (cara di issue); (2) gateway `axvara-wa-gateway/src/index.ts`: 401 = TIDAK reconnect (hammer tiap 3 dtk = signature bot pemicu restriction), 515 reconnect 1 dtk, sisanya backoff+jitter ≤60 dtk + teardown anti-zombie + `cachedGroupMetadata` 5 mnt + jeda baca 1-2,5 dtk + antrean outbound 4-8 dtk + cooldown 15 mnt pasca-3 sinyal bahaya + `/health` jujur (`lastDisconnectCode/At/reconnectCount`) — DEPLOY GATEWAY DITUNDA sampai timer nol (restart saat dibatasi = memperpanjang); (3) outbox Pages dipacing (`WHATSAPP_OUTBOX_PACE_MS` 6 dtk + jitter ≤3 dtk, cooldown 60 dtk/destinasi, auto-pause lunak → `whatsapp_outbox_paused` di hasil cron + `paused` di return); (4) issue `issue/disable-wa-credential-dm-dan-hardening-umur-nomor.md` (latar, risiko, langkah, kriteria, cara nyalakan lagi); docs PRD (FR-S24) + ARCHITECTURE + README + `.env.example` — (verifikasi: tsc bersih, 876/876 test [84 file] hijau)

- 2026-09-19 — Footer /garansi dihapus dari SEMUA DM kredensial WA + rencana pemantauan sepekan (keputusan owner pasca-verifikasi web): pesan `wr-web-ready`, `wr-delivery` (DM web) + delivery fulfillment WA/Telegram channel whatsapp/telegram tidak lagi menyuruh "Ketik *garansi*" — perintah itu hanya hidup di grup (handler webhook `/garansi` tak tersentuh); diagnosis bot grup mati via Heroku CLI: sesi auth Postgres SEHAT (5731 kunci, `registered:false` + `me.id` terisi = bukan sesi mati melainkan minta pairing ulang di HP yang sama), akar = loop 401 `Connection Failure`→reconnect tiap ~5 dtk sejak dyno restart 17:03 WIB (Baileys rc14 vs server menolak noise handshake — sesi ditendang dari sisi WA/phone companion, bukan bug kode deploy v20/14 Sep); pemantauan wajib 19–26 Sep dicatat (sweep cron ~30 mnt, outbox tanpa `whatsapp_not_connected`, delivery tanpa antrean menggantung, pairing ulang tanpa hapus kunci) — src/lib/warung-rebahan/deliver.ts (3 call-site + Telegram), src/lib/fulfillment/delivery/{send,process}.ts (3 call-site), tests/wr-queued-delivery.regression.test.ts (2 guard), docs/{PRD.md (FR-S24),ARCHITECTURE.md (§ pengawasan antrean),README.md} — (verifikasi: tsc bersih, 870/870 test [84 file] hijau)

- 2026-09-18 — Kredensial rapi + WA JID fix (bukti prod sore): (1) panel web tampil JSON mentah `{"product":..,"details":"email:..\r\npassword:.."}` karena cabang string formatter mengembalikan apa pun mentah + `\r` tak dinormalisasi — kini `normalizeAccountDetailsForDisplay` (envelope kupas 3 lapis, `\r\n` sebelum split, pembungkus `{product,details}` hanya pakai details, label ganda dibuang) dipakai di SEMUA titik baca (processCredentialDelivery, readVerifiedDetails, getDecryptedAccountDetails) tanpa migrasi data; (2) 2 baris `wr-web-ready` gagal 4x (`jidDecode(...)` undefined) karena outbox menyimpan nomor mentah tanpa domain — `normalizeWhatsAppTarget` di adapter (pola DM ketat, grup/`@` lolos apa adanya) — src/lib/warung-rebahan/deliver.ts, src/lib/whatsapp/gateway.ts, src/app/api/orders/[code]/credentials/route.ts, tests/warung-rebahan/{order,credentials}.regression.test.ts + tests/whatsapp-variants.regression.test.ts, docs/{ARCHITECTURE.md,DESIGN.md} — (verifikasi: RED→GREEN — formatter JSON prod + JID grup-digit + label ganda gagal dulu; full: 870/870 test [84 file], tsc bersih)

- 2026-09-18 — Perbaiki flaky RNG `qris-lifecycle` ("rejects an event observed..."): test checkout tanpa mock sehingga nominal dasar acak dan ekspektasi hardcoded 10100 kadang meleset (CI: 10101); mock `chooseCode(42)` dipasang sebelum checkout seperti test saudara — tests/qris-lifecycle.integration.test.ts — (verifikasi: 6x run lokal hijau; file + `dana-qris.ts` tidak tersentuh commit Fase B, jadi bukan regresi)

- 2026-09-18 — Fase B: isi kredensial WR dikirim LANGSUNG 3 jalur (keputusan owner): (1) WA via outbox durable (`deliverWebCredentialViaWhatsApp`, kunci `wr-delivery:<code>[:pN]`, potong 1500 char/bagian, hanya DM) + kabar `wr-web-ready` yang sudah ada; (2) email Resend template baru "Detail Akun Siap" (`buildCredentialReadyTemplate`, isi + peringatan jangan bagikan, idempoten `wr-cred-email:<code>`) + migrasi 0038 (kind `credential_ready` + channel `email-credential` — CHECK lama menolak INSERT diam-diam sehingga retry mengirim ganda, tertangkap test sebelum live; tanpa email dilewati diam); (3) `WrCredentialsPanel` dipakai ulang di hasil `/lacak-pesanan` (`prefillWa` + auto-verify sekali, verifikasi tetap server-side; lookup sudah constantTimeEqual penuh) + flag `credentials_ready` di `/api/orders/lookup`; formatter key map normalisasi (`akses otp`/URL tampil rapi, bukan JSON); aturan settled: dua gagal = retry, satu gagal = parsial tercatat — src/lib/warung-rebahan/{deliver,email-forward}.ts, src/app/{api/orders/lookup,lacak-pesanan}/*, src/components/storefront/WrCredentialsPanel.tsx, drizzle/{schema.sql,migrations/0038_credential_email_log.sql}, tests/wr-queued-delivery.regression.test.ts (5 baru) + tests/warung-rebahan/wr-migrations.regression.test.ts (0038), docs/PRD.md (FR-S24) + ARCHITECTURE.md (§15) + README.md — (verifikasi: MERAH→HIJAU — 3 test gagal dulu: typo ekspektasi, LIKE prefix `wa:text:`, CHECK menolak INSERT; bug kode asli tertangkap: kolom VALUES bocor; full: 867/867 test [84 file], tsc bersih)

- 2026-09-18 — Fase A: akar sync mati ternyata ENV bukan kode — `WARUNG_REBAHAN_ENABLED`/`SYNC_ENABLED` tidak bernilai `"true"` di Pages sehingga `isWrEnabled()` false dan seluruh fase WR no-op total tanpa jejak (semua jalur skip kode menandai deferred, deferred selalu kosong → return awal; "bukti kerja WR" 08:22–09:13 UTC ternyata refresh saldo manual dashboard). Diagnosis: 7 skenario fixture membuktikan jalur sync bekerja (gerbang lolos, CALLS=1, log tertulis) + tembakan manual 401 membuktikan CRON_SECRET lokal ≠ Pages. Perbaikan: set 3 kunci WR ke `true` + rotasi CRON_SECRET (Pages + Worker AXVARA_CRON_SECRET + lokal disamakan) via `secret put` per kunci (hitungan tetap 38, tidak ada hilang) + commit plan sebagai pemicu deploy (Pages secret baru terbaca deployment berikutnya). Verifikasi: tembakan pasca-deploy `wr_products_synced: 48`, baris `trigger='cron'` 13:20 UTC, reconcile menarik Meitu (completed/TERISI/delivered), kabar WA `wr-web-ready` mengantre — tanpa perubahan kode, tanpa Force Sync — docs/ARCHITECTURE.md (§16) — (verifikasi: D1 prod + respons cron 200 langsung)

- 2026-09-18 — Perbaiki guard anti-starvation sync WR yang memveto dirinya sendiri: sync otomatis tetap mati 2,5 jam SETELAH perbaikan deadline 18 Sep (dua order Meitu menggantung 04:38–07:14 UTC) karena syarat `pendingWrDue === 0 && pendingWrDelivery === 0` — padahal fase WR menangani order DAN sync, jadi slot paksa tidak pernah diberikan selama ada order due. Syarat itu DIBUANG; fase WR didahulukan ke depan urutan eksekusi saat sync basi >45 menit (admission sync memakai budget baseline 40 + deadline 45 dtk, sehingga bila jalan belakangan sweep sering di-skip diam-diam); skip sweep karena budget/waktu WAJIB ditandai (`cron_deferred` + `results.wr_sync_skipped = "deadline"|"query_budget"`) agar tidak hilang diam-diam dan run berikutnya mendorong fase WR ke depan — src/app/api/cron/operations/route.ts, tests/cron-deadline-poison.integration.test.ts (2 test baru: slot paksa walau ada order menggantung + komposisi tak berubah tanpa histori), docs/ARCHITECTURE.md (§16.6) + README.md — (verifikasi: MERAH terkonfirmasi — guard dimatikan sementara → test "sync basi mendapat slot" gagal `expected [ 'warung_rebahan', 'cleanup' ] to not include 'warung_rebahan'`; dikembalikan → 6/6 hijau di file ini; full: 861/861 test [84 file], tsc bersih, dev GET / 200 + CSS 200, Obscura home 12 kartu OK)

- 2026-09-18 — Set env Pages produksi untuk mengaktifkan auto-order kelas antrean: `WARUNG_REBAHAN_AUTO_ORDER_ENABLED=true`, `WARUNG_REBAHAN_AUTO_ORDER_MBO=true` (baru, saklar mundur), `WARUNG_REBAHAN_SALDO_ALERT_THRESHOLD=250000` (baru — sebelumnya absen sehingga memakai default kode); semua `secret_text` via `wrangler pages secret put` satu per satu (BUKAN PATCH env_vars, yang berisiko menimpa seluruh peta env). Total secret prod 36 → 38, 36 lama utuh dan terverifikasi lewat `pages secret list`. Nilai baru baru terbaca deployment berikutnya, karena itu commit ini sekaligus memicu CI — docs/ARCHITECTURE.md §16.2 — (verifikasi: `pages secret list` = 38 entri termasuk 3 kunci di atas; 859/859 test + tsc tetap hijau dari commit cff1b25, tidak ada perubahan kode)

- 2026-09-18 — Produk kelas ANTREAN ikut auto-order + ekspektasi waktu jujur sebelum bayar (owner: estimasi supplier 6–12 jam, <1 jam bila lancar): gate `processWrPendingOrders` dibuka untuk seluruh kelas (`made_by_order`/NULL) dengan saklar mundur `WARUNG_REBAHAN_AUTO_ORDER_MBO=false`; satu sumber copy+ambang di `delivery-class.ts` (`WR_QUEUED_MAX_HOURS=12` plafon publik, `WR_QUEUED_ALERT_HOURS=13` ambang internal, `deliveryEtaForBuyer`, `isQueuedFulfillment`); disclosure SEBELUM bayar di PDP (badge "Diproses antrean · maks 12 jam" + baris fitur) + QuickVariantModal (kalimat di atas CTA) + checkout (blok "Waktu pengerjaan pesanan ini" dari flag `queued_delivery` per baris di quote); `GET /api/orders?code=` menambah `queued_delivery` (satu query gabungan dengan `credentials_ready` via `json_each(items)`), halaman pesanan membedakan teks + memperpendek polling antrean ke 3 percobaan; kabar WA otomatis ke pembeli web saat detail siap (`wr-web-ready:`, isi kabar+tautan invoice saja, bukan kredensial); `alertAgingWrOrders()` + migrasi 0037 (`aging_alerted_at`, ditandai sebelum kirim) memberi tahu admin bila order melewati 13 jam; panel admin menampilkan umur baris antrean (merah ≥13 jam); ambang alert saldo default 50.000 → 250.000 (modal termahal 200.000); garansi kelas antrean dinyatakan mulai saat detail akun diserahkan; label "Dikirim admin" dibuang (samar). Nama supplier nol jejak di copy pembeli, status third-party independen DIPERTAHANKAN di /garansi-replace (dasar klaim + posisi sengketa) — src/lib/warung-rebahan/{order,client,deliver,saldo,delivery-class}.ts, src/app/api/{orders,checkout/quote,cron/operations}/route.ts, src/app/{checkout,pesanan/[code],produk/[slug],garansi-replace}/*, src/components/storefront/QuickVariantModal.tsx, src/components/admin/WarungRebahanManager.tsx, drizzle/{schema.sql,migrations/0037_wr_aging_alert.sql}, .env.example, tests/{wr-queued-delivery.regression,order-status-delivery.behavior,checkout-integrity.regression}.* + tests/warung-rebahan/{order,saldo,delivery-class}.test.ts, docs/PRD.md (FR-S22/S23) + ARCHITECTURE.md §15 + DESIGN.md §5.3c + README.md — (verifikasi: 859/859 test [84 file], tsc bersih, dev GET / 200 + CSS 200 + PDP/checkout/garansi 200, Obscura PDP menampilkan "maksimal 12 jam pada jam layanan" tanpa "Dikirim admin" + /garansi-replace third-party & garansi-mulai-saat-diserahkan terbaca; 2 test gate lama dibalik + 1 test saklar mundur baru)

- 2026-09-18 — Perbaiki penyebab sync otomatis WR mati ~16 jam (17 Sep 13:06 UTC → 18 Sep 04:55 UTC): (1) poison-pill guard — `writeCronPhase` dipanggil di AWAL run (fase maju + deferred dikosongkan) sehingga run yang dipotong platform tidak mengunci rotasi di `warung_rebahan`; (2) deadline wall-clock `RUN_DEADLINE_MS=45s` + ambang per unit kerja jaringan (notify/WA/fulfillment/tiap langkah WR), respons cron menyertakan `run_duration_ms`/`run_deadline_ms`; (3) `WR_API_TIMEOUT_MS` 30s → 12s; (4) lease pengiriman kredensial disimpan (`delivery_next_attempt_at`, 2 mnt) + `recoverStaleCredentialDeliveries()` mengangkat baris `sending` basi dan hitungan antrean cron ikut memasukkannya; (5) delivery web tidak lagi menganggap `issueCredentialToken()=null` sebagai gagal saat token valid sudah ada (`hasValidCredentialToken()`), yang sebelumnya membuat setiap retry web pasti gagal; (6) zero-missing stok hanya jalan bila sweep dimulai dari awal daftar (`startAt===0`) — tanpa ini run lanjutan-cursor me-nol-kan stok varian yang tidak dilihatnya (terbukti 4 varian) — src/app/api/cron/operations/route.ts, src/lib/warung-rebahan/{client,deliver,sync}.ts, tests/cron-deadline-poison.integration.test.ts (baru), tests/warung-rebahan/{credentials.regression,sync}.test.ts, docs/ARCHITECTURE.md (§batch cron) + PRD.md (FR-S21) + README.md — (verifikasi: 845/845 test [83 file], tsc bersih, dev GET / 200 + CSS 200 + POST /api/cron/operations 401 tanpa secret, Obscura home 12 kartu OK; diagnosis dari D1 prod + analytics GraphQL + `wrangler pages deployment tail` yang menangkap 2 run `canceled` pada wallTime 125.003/125.014 ms)

- 2026-09-18 — Panel "Detail Akun Digital" tidak lagi jadi form mati di semua order lunas: `GET /api/orders?code=` mengirim flag `credentials_ready` (EXISTS `wr_order_links` completed + ada ciphertext, dievaluasi hanya untuk status `lunas`, `.catch` → false untuk D1 pra-0027, tanpa membocorkan isi kredensial), halaman pesanan merender panel retrieval HANYA saat flag true dan sisanya menampilkan blok statis "Pengiriman Produk" (tujuan WA + email checkout tersamar + estimasi 5–15 menit); polling terbatas 20 dtk × maks 30 saat `lunas && !credentials_ready` supaya panel muncul sendiri tanpa reload — src/app/api/orders/route.ts, src/app/pesanan/[code]/page.tsx, tests/order-status-delivery.behavior.test.tsx (baru), tests/warung-rebahan/credentials.regression.test.ts, tests/checkout-integrity.regression.test.ts, docs/PRD.md (FR-S20) + DESIGN.md (§5.7) + ARCHITECTURE.md (§API + §15) + README.md — (verifikasi: 838/838 test [82 file], tsc bersih, dev GET / 200 + CSS 200 + /pesanan/[code] 200, Obscura dua cabang: `credentials_ready=false` → "Pengiriman Produk" tanpa tombol Tampilkan; `=true` → panel + input WA)

- 2026-09-18 — Revert S&K ala Axvara kembali ke pure WR (permintaan owner: hasil rewrite terasa aneh): revert commit 62f10e1 (hapus terms-display.ts + TermsHighlight.tsx + testnya, PDP kembali render mentah per varian, docs kembali) + NULL-kan 3 admin_description_override pilot (netflix-premium/capcut-pro/loklok) di D1 prod sehingga deskripsi kembali teks WR — git revert + D1 prod — (verifikasi: 829/829 test, tsc bersih)

- 2026-09-18 — Rapikan blok tombol pesanan 2-tier (anti wrap/gepeng): Tier 1 navigasi grid 2 kolom h-11 text-sm nowrap, Tier 2 bantuan pill outline h-11 text-13px + ikon brand + label pendek WA Admin/Telegram + konteks "Butuh bantuan?", hapus solid hijau/biru full-bleed; berlaku di /pesanan/[code] + /lacak-pesanan (kondisional cyan col-span-2) + guard regresi — src/app/pesanan/[code]/page.tsx, src/app/lacak-pesanan/lacak-pesanan-client.tsx, tests/checkout-integrity.regression.test.ts, docs/DESIGN.md (§5.7/§5.7a) — (verifikasi: 829/829 test)

- 2026-09-18 — Lebarkan QuickVariantModal di desktop (mobile dikunci): panel `max-w-[480px]` → `sm:max-w-[620px] lg:max-w-[660px]` + `sm:p-7`, grid tetap 2 kolom `sm:gap-3` + kartu `sm:p-4`, list `sm:max-h-[340px]` agar 4-6 varian tidak langsung scroll internal, CTA `sm:h-12` + thumb header `sm:h-16`; mobile tetap bottom-sheet 480px; test regresi kelas responsif — src/components/storefront/QuickVariantModal.tsx, tests/quick-variant-modal.behavior.test.tsx, docs/DESIGN.md (§5.3d) — (verifikasi: 828/828 test, tsc bersih, dev GET / 200 + CSS 200, Obscura home OK)

- 2026-09-18 — Perbaiki ikon Deskripsi Produk PDP yang hancur: path SVG terpotong (`2-2V8z` → `2 2h12a2 2 0 0 0 2-2V8z`, kontur dokumen utuh, ikon rill sama tidak diganti) — src/app/produk/[slug]/product-detail-client.tsx — (verifikasi: 827/827 test, tsc bersih, dev GET / 200 + CSS 200 + PDP 200, Obscura PDP ikon dokumen utuh)

- 2026-09-17 — Maintenance jalur manual (E-Wallet/Bank) QRIS saja di semua platform: WEB tampilkan disabled + badge Maintenance (tidak bisa diklik) + upload bukti disembunyikan total, `POST /api/proof/upload` 503, `POST /api/orders` tolak non-QRIS/proof 503 + tolak quote tanpa QRIS; WA hilangkan opsi manual (variantSelected/paymentChoice hanya QRIS, handlePay + proof SEABANK/EWALLET → pesan maintenance), TELE sudah QRIS-only (hanya teks notif admin disesuaikan); cara-order langkah 3 jadi QRIS; 8 test disesuaikan (security/checkout-integrity/dependency-audit/whatsapp-variants) — src/app/checkout/page.tsx, src/app/api/orders/route.ts, src/app/api/proof/upload/route.ts, src/lib/whatsapp/messages.ts + handlers/payment.ts + handlers/proof.ts, src/lib/telegram/messages/admin.ts, src/app/cara-order/page.tsx, tests/security.regression.test.ts + checkout-integrity.regression.test.ts + dependency-audit.regression.test.ts + whatsapp-variants.regression.test.ts, docs/PRD.md (FR-S5/S6/S7/S9/S15) + DESIGN.md (§5.6) + ARCHITECTURE.md (§5–6) + README.md — (verifikasi: 827/827 test, tsc bersih)

- 2026-09-17 — Revisi template email buyer (approval): BG terang penuh (blok hitam Gmail hilang), logo Prism PNG asli navbar (path sama persis, stroke putih; Gmail strip SVG inline), footer "Butuh bantuan? Tim kami siap membantu:" + tombol hijau wa.me klikable + "Axvara" link web; logo di public/brand/axvara-email-mark.png — src/lib/warung-rebahan/email-forward.ts, public/brand/axvara-email-mark.png, tests/warung-rebahan/wr-email-forward.test.ts — (verifikasi: 825/825 test, tsc bersih, render invite+update OK di Obscura)

- 2026-09-17 — Template email Axvara style final (logo Prism center + CTA Cyan #00E5FF + badge status cyan-outline, body midnight): header email = logo Prism wireframe inline SVG cyan + wordmark tracking lebar ala Navbar, CTA bukan biru generik, "Axvara" penutup midnight bold, footer terang di atas midnight — src/lib/warung-rebahan/email-forward.ts — (verifikasi: 825/825 test, tsc bersih, dev GET / 200 + CSS 200, Obscura home OK, render invite+update bersih WR-leak)

- 2026-09-17 — Bot email WR→buyer white-label (fase 2): Apps Script forwarder label WR-INGEST→WR-SENT trigger per-menit (docs/WR-EMAIL-FORWARDER.gs.js) POST ke /api/webhook/wr-email (secret WR_EMAIL_WEBHOOK_SECRET, rate-limit webhook:warung) → parser invoice/status/produk/target-email (src/lib/warung-rebahan/email-forward.ts) → join wr_order_links → 2 template Axvara nol-jejak-WR → kirim Resend (FORWARD_FROM_EMAIL, src/lib/warung-rebahan/forward-sender.ts) / fallback WA outbox idempoten / held-202 bila env belum diisi → log idempoten wr_email_forward_log (migrasi 0036 + schema.sql) + 11 test + docs PRD (FR-SYS10)/ARCHITECTURE (§15)/README/.env.example — (verifikasi: 825/825 test, tsc bersih, dev GET / 200 + CSS 200, Obscura home OK)

- 2026-09-17 — Cari manual invoice WR di admin (jembatan forward email WR→buyer): GET /api/admin/warung/orders dukung ?q= (#RBHN-… atau kode Axvara, potong 40 char, LIKE di-escape) + kembalikan buyer (nama/WA/email/channel) + tab Warung Rebahan ada kolom cari debounce 400ms + baris buyer per order; mapping order Axvara↔invoice WR sudah ada (wr_order_links.wr_order_id) jadi tanpa migrasi — src/app/api/admin/warung/orders/route.ts, src/components/admin/WarungRebahanManager.tsx, tests/warung-rebahan/admin-apis.test.ts, docs/ARCHITECTURE.md + README.md — (verifikasi: 814/814 test, tsc bersih, dev GET / 200 + CSS 200, Obscura home OK)

- 2026-09-17 — Halaman Lacak Pesanan mandiri (kode + no WA, tanpa login): POST /api/orders/lookup verifikasi pasangan kode+WA (normalisasi 08/+62/62, constantTimeEqual, 404 generik anti-enumerasi, WA/email mask, rate-limit orders:lookup) + halaman /lacak-pesanan (form validasi inline, timeline Dibuat→Pembayaran→Diproses, badge Pending/Lunas/Batal/Kedaluwarsa, auto-refresh Pending 10 dtk, deep-link ?code=&wa=, riwayat lokal 5 terakhir, FAQ + kartu cara, tombol Salin/Lacak-pesanan-lain, CTA pembayaran/pesan-baru) + tautan navbar/footer/bottom-nav/cara-order/pesanan + 7 test baru + docs PRD (FR-S11a)/DESIGN (§5.7a)/ARCHITECTURE/README — src/app/api/orders/lookup/route.ts, src/app/lacak-pesanan/, src/components/storefront/Navbar.tsx + Footer.tsx + MobileBottomNav.tsx, src/app/cara-order/page.tsx, src/app/pesanan/[code]/page.tsx, tests/order-tracking.test.ts — (verifikasi: 813/813 test, tsc bersih, dev GET / 200 + CSS 200 + /lacak-pesanan 200, Obscura form+FAQ OK)

- 2026-09-17 — Social proof sold_count: migrasi 0035 set angka terjual random pada semua produk (non-WR 50–350, WR 30–200) via MAX agar angka riil yang sudah tinggi tidak turun; pembelian baru tetap increment normal (sold_count+=qty) — drizzle/migrations/0035_sold_count_social_proof.sql — (verifikasi: 806/806 test, dev GET / 200 + CSS 200)

- 2026-09-17 — Stepper Jumlah bisa + dan ketik manual (PDP + modal): angka jadi input ketik (digit, maks 3) dengan draft-commit ala Shopee — bebas ketik lalu clamp [min, 100] saat blur/Enter, tidak bisa di bawah min (GSuite: ketik 5 → kembali 50); tombol + naik (50→51, total ikut), tombol aksi/total pakai qty ter-commit; test +/ketik/clamp di modal — src/app/produk/[slug]/product-detail-client.tsx, src/components/storefront/QuickVariantModal.tsx, tests/quick-variant-modal.behavior.test.tsx + variant-min-qty.test.ts, docs/DESIGN.md (§5.3d) — (verifikasi: 806/806 test, tsc bersih, dev GET / 200, Obscura PDP OK)

- 2026-09-17 — Revisi UX min pembelian ala marketplace (Shopee/Tokopedia): hapus badge "Min. 50" di tiap kartu varian (menumpuk) → blok Jumlah tunggal (stepper floor=min, plafon 100, teks "Min. pembelian N" + total) di PDP desktop+mobile; Beli Langsung bawa ?qty= (tanpa dead-end); QuickVariantModal panel solid #0B1025 konsisten modal admin + stepper + tombol total; checkout dialog "Sesuaikan ke minimum" 1-klik (bukan "kembali belanja"); fix Rules-of-Hooks (clamp render-time safePdpQty); test modal stepper min-50 — src/app/produk/[slug]/product-detail-client.tsx, src/components/storefront/QuickVariantModal.tsx, src/app/checkout/page.tsx, src/components/storefront/CartDrawer.tsx, tests/quick-variant-modal.behavior.test.tsx + variant-min-qty.test.ts, docs/DESIGN.md (§5.3d) — (verifikasi: 805/805 test, tsc bersih, dev GET / 200, Obscura PDP tanpa hooks-error)

- 2026-09-17 — Mode minimum pembelian generik per-varian (kasus pertama GSuite min 50): migrasi 0034 product_variants.min_qty (default 1, CHECK ≥1, milik admin, UPDATE GSuite→50 via slug/nama) + schema.sql + strip-snippet test + catalog.ts + quote 409 below_minimum (agregat/baris, plafon web 20→100) + orders 409 (hitung ulang DB) + guard atomik COALESCE(min_qty,1) di orders-create + commerce (atomic+legacy) + auth verifier ≤100 + Telegram (stepper dibuka di min, tolak ketik/invoice/keranjang/checkout di bawah min, keyboard minQty) + WA (varian min>1 ditolak jelas sejak pilih + saat bayar, arah ke web/Telegram) + web (badge Min. PDP/modal/drawer, cart dibuka di min + floor min + plafon 100, checkout dialog below_minimum, Beli Langsung min>1 diarahkan bulk) + admin (input Min. Beli VariantEditor + ProductVariantRows + API variants/products + ownership catat admin-owned) + 14 test baru (migrasi/CHECK/GSuite/guard SQLite/SQL-string/admin) — drizzle/migrations/0034_variant_min_qty.sql, drizzle/schema.sql, src/lib/catalog.ts, src/app/api/checkout/quote/route.ts, src/app/api/orders/route.ts, src/lib/db/orders-create.ts, src/lib/commerce.ts, src/lib/auth.ts, src/lib/telegram/cart.ts + handlers/catalog.ts + invoice.ts + discovery.ts + cart.ts + keyboards.ts + messages/purchase.ts, src/lib/whatsapp/handlers/catalog.ts + payment.ts + messages.ts, src/stores/cart.ts, src/app/produk/[slug]/product-detail-client.tsx, src/components/storefront/QuickVariantModal.tsx + CartDrawer.tsx, src/app/checkout/page.tsx, src/components/admin/VariantEditor.tsx + sections/ProductVariantRows.tsx + product-types.ts + useProductManager.ts, src/app/api/admin/variants/route.ts, src/app/api/products/[id]/route.ts + route.ts, tests/variant-min-qty.test.ts + cart-store + wr-migrations + whatsapp-variants, docs/PRD.md (FR-S19/SYS9) + DESIGN.md (§5.3d) + ARCHITECTURE.md (§14) + README.md — (verifikasi: 804/804 test, tsc bersih, dev GET / 200 + CSS 200, Obscura home OK)

- 2026-09-17 — Harga coret WR bisa diedit admin + diskon/badge live 87 varian: compare_price keluar dari WR_OWNED (milik admin, sync tak pernah tulis) → guard 409 hanya key body mentah (fix bug zod .partial()+default bikin edit coret selalu 409 stock) → PUT /api/products/:id tulis varian walau fields produk kosong (fix edit coret saja 200 tapi null) → kartu fallback coret ke varian termurah bila semua habis (fix 26/50 kartu tanpa diskon) → input Harga Coret modal edit selalu terbuka (✎ bisa diedit) → seed 87 varian coret wajar (<10rb=3x, 10-30rb=2.5x, 30-60rb=2x, >60rb=1.8x, bulat 500; diskon 44-67%) + 9 badge (Terlaris: Netflix/Apple Music; Hemat: Viu/Zoom/PicsArt/Remini; Baru: Canva WR/Gemini WR/Bstation) — src/lib/warung-rebahan/ownership.ts, src/app/api/admin/variants/route.ts, src/app/api/products/[id]/route.ts, src/app/api/products/route.ts, src/components/admin/sections/ProductVariantRows.tsx + ProductEditorModal.tsx + product-types.ts, tests/warung-rebahan/ownership.regression.test.ts + sync.test.ts + catalog-available-price.regression.test.ts, docs/ARCHITECTURE.md + README.md, D1 prod — (verifikasi: 790/790 test, tsc bersih, dev GET / 200 + CSS 200, Obscura home OK, D1 87/87 coret valid + 9 badge, API prod 24/50 kartu diskon pre-fallback-fix)

- 2026-09-17 — Bersih antrean tes: 2 event QRIS tes koneksi 14 Sep + 7 job yatim (order sudah dihapus) + order Ferdi (tes teman, manual_required) + anaknya — docs/ARCHITECTURE.md tak perlu ubah (data tes, bukan perilaku) — (verifikasi: kartu QRIS 0 + Fulfillment 0 di D1 prod)

- 2026-09-17 — Koreksi vonis cron + hapus 14 order test lama: cron_phase=notify basi BUKAN bukti macet (itu giliran berikutnya dalam rotasi 5 fase) — bukti hidup = wr_sync_log trigger=cron tiap ~30 mnt penuh 12 jam (48p/87v success) + phase berpindah; hapus 14 order test 6–8 Sep + anaknya (items/jobs/transactions/history/dana events), sisa lunas tinggal Ferdi (asli) + AB12 (uji WR) — docs/ARCHITECTURE.md (§16.6 koreksi cara baca indikator) — (verifikasi: D1 sisa lunas 2, sync cron ritmis per jam)

- 2026-09-17 — Badge pengiriman tanpa emoji (anti AI slop) + sinkron docs: pill "Kirim otomatis" (emerald) / "Dikirim admin" (gold) per kartu varian PDP + QuickVariantModal, teks polos per baris di Telegram/WA, tombol admin AUTO/MANUAL, docs ARCHITECTURE §16.7 + DESIGN §5.3c diselaraskan dari label emoji lama — docs/ARCHITECTURE.md, docs/DESIGN.md — (verifikasi: 785/785 test, dev GET / 200 + CSS 200, Obscura PDP BADGE-OK)

- 2026-09-16 — Matriks sandbox WR 7 order (saldo utuh Rp6.500): 3 restock (Prime Video Private, I Love PDF Sharing, Canva Edu Link) → processing TEST-ORD + 3 MBO (Picsart, Express VPN, Zoom 14D) → gate menolak, link pending tanpa API call + Remini Link tanpa email → pending + quote apple-music emailRequired:true. Sandbox ON→OFF via secret + deploy. 7 order/link/job/item test dibersihkan dari D1 (payment tables 0 karena PATCH admin tanpa ledger). Catatan: TEST-ORD-* fiktif (WR tidak mencatat order test) — bukti validitas = validasi lolos, bukan delivery — src/lib/warung-rebahan/order.ts (gate), client.ts (is_test) — (verifikasi: D1 link statuses + saldo log + proxy log, cleanup sisa 0)

- 2026-09-16 — Email wajib SEBELUM bayar untuk Invite/Link WR + toggle per produk: quote hitung emailRequired (JOIN wr_type + require_email, 1 query IN) → form web label/wajib dinamis → API orders 422 guard DB → Telegram minta email via pending email_for:/emailcart: + simpan buyer_email + teruskan ke WR → WA tolak jelas + arahkan web/Telegram → toggle "Wajib email pembeli" di editor + migrasi 0033 (products.require_email, telegram_users.buyer_email) + schema.sql — src/app/api/checkout/quote/route.ts, src/app/api/orders/route.ts, src/app/checkout/page.tsx, src/lib/telegram/handlers/invoice.ts + cart-invoice.ts + callback.ts + discovery.ts(handlePendingEmailInput) + command.ts, src/lib/whatsapp/handlers/payment.ts, src/lib/warung-rebahan/delivery-class.ts(needsEmailForVariant), src/lib/catalog.ts(wr_type/require_email), src/app/api/products/[id]/route.ts + useProductManager + product-types + ProductEditorModal, drizzle/migrations/0033_product_require_email.sql, tests/warung-rebahan/email-required.test.ts + wr-migrations(0033) — (verifikasi: 785/785 test, tsc bersih, dev 200+CSS 200, quote non-invite emailRequired:false, Obscura checkout EMAIL-FIELD-OK)

- 2026-09-16 — Uji live auto-order WR #1 SUKSES + fix email_invite: order test Apple Music (modal Rp3.500) lunas → link pending → WR 422 "email_invite required for invite-based" (Apple tipe Invite, order tanpa email) → processOneLink teruskan customer_email sebagai email_invite + test kirim-invite + koreksi seed 0032 ke JOIN wr_products (versi awal cocok varian generik, cuma 4) + reset link + email dummy + retry admin → processing RBHN-20260916-DCB922, saldo 10.000→6.500. Invite/Link tetap default MBO (tanpa email, retry tak sembuh) — src/lib/warung-rebahan/order.ts, delivery-class.ts, drizzle/migrations/0032_wr_delivery_class.sql, tests/warung-rebahan/order.test.ts — (verifikasi: link processing + wr_order_id + order_deduct di D1 prod, proxy log 200; saldo asli terpakai Rp3.500)

- 2026-09-16 — Health jujur anti-false-alarm + kelas pengiriman WR restock/MBO + label pembeli singkat + kunci FF/AUTO: (1) query tgQueue/fulfillment/item JOIN orders lunas+paid — 7 failed order final tak seret Telegram degraded +1 test; (2) FULFILLMENT_ENCRYPTION_KEY baru + AUTO_FULFILLMENT_ENABLED=true via deployment_configs (secret_text); (3) migrasi 0032 wr_delivery_class/source + seed 17 screenshot + guessDeliveryClass + sync tak timpa kunci + gate auto-order hanya restock (MBO/NULL tetap pending) + PUT markup delivery_class kunci admin; (4) PDP "⚡ Kirim otomatis / ✋ Dikirim admin", Telegram/WA ⚡/✋ per varian, badge admin RESTOK/MBO + tombol kunci — src/app/api/admin/overview/route.ts, src/lib/warung-rebahan/delivery-class.ts, sync.ts, order.ts, src/lib/catalog.ts, src/app/produk/[slug]/product-detail-client.tsx, src/lib/telegram/messages/catalog.ts + handlers/catalog.ts, src/lib/whatsapp/messages.ts, src/app/api/admin/warung/markup/route.ts, src/components/admin/WarungRebahanManager.tsx, drizzle/migrations/0032_wr_delivery_class.sql + schema.sql, tests/warung-rebahan/delivery-class.test.ts + order.test.ts(gate MBO/NULL) + wr-migrations(0032) + overview-health(false-alarm) + cron.test + monitoring-health + whatsapp-variants — (verifikasi: 779/779 test, tsc bersih, dev 200+CSS 200, Obscura PDP LABEL-OK)

- 2026-09-16 — Framing QRIS di /pesanan/[code]: label "Scan QRIS" di atas QR + lockup resmi (logo QRIS BI mono putih + teks lengkap "National Payment Standard") di bawah QR; QR + generator PNG tidak disentuh; +1 regression test; mockup visual terverifikasi — public/brand/qris.svg, src/app/pesanan/[code]/page.tsx, tests/checkout-integrity.regression.test.ts, docs/DESIGN.md (§5.7b) — (verifikasi: 765/765 test, tsc bersih, dev GET / 200 + CSS 200 + logo 200, Obscura screenshot framing OK)

- 2026-09-16 — Garansi varian PDP pindah ke baris sendiri di bawah nama varian (block, bukan inline di sebelah label) — src/app/produk/[slug]/product-detail-client.tsx, docs/DESIGN.md (§5.3c) — (verifikasi: 764/764 test, tsc bersih, dev GET / 200 + CSS 200, Obscura PDP OK)

- 2026-09-16 — PDP tampilkan S&K + cara aktivasi per varian WR (JOIN wr_variants via wr_variant_id, tanpa migrasi; section terikat varian terpilih + fallback varian aktif pertama, desktop + mobile) + label garansi kanonis "Garansi N Hari" (formatWarranty limited abaikan label mentah "12 Hari" yang ambigu) + ikon shield — src/lib/catalog.ts, src/app/produk/[slug]/product-detail-client.tsx, src/components/storefront/QuickVariantModal.tsx, src/lib/warung-rebahan/ownership.ts, tests/warung-rebahan/ownership.regression.test.ts, tests/whatsapp-variants.regression.test.ts, docs/PRD.md (FR-S18), docs/DESIGN.md (§5.3c), docs/ARCHITECTURE.md (S&K tanpa migrasi) — (verifikasi: 764/764 test, tsc bersih, dev GET / 200 + CSS 200, Obscura PDP OK)

- 2026-09-15 — Redo kiro-ai (hantu putih + bg ungu e97e136a) + getcontact-premium (pin biru + telepon 8bfba635) dari referensi logo user — /tmp/axvara-compose/*-userref2.webp, R2 products/ — (verifikasi: 2/2 R2 live 200, 2/2 catalog API terisi, 759/759 test, Obscura homepage OK)

- 2026-09-15 — Revisi bg drakor-id (badge merah + play putih aeb595ab) + iqiyi (badge hijau + iQIYI putih 98aaa094) sesuai referensi user — /tmp/axvara-compose/*-bgfix.webp, R2 products/ — (verifikasi: 2/2 R2 live 200, 2/2 catalog API terisi, 759/759 test, Obscura homepage OK)

- 2026-09-15 — Redo 7 tile dari referensi logo milik user (compose canva+gsuite+Referensi/1-7.png): leonardo-ai→da Vinci geometris 15569c51, drakor-id→play origami 20d1a26c, iqiyi→iQIYI frame 28b610f8, adobe→infinity pelangi 41131d84, devin→3-hexagon 8b2a8418, copilot→robot ungu 14251c3d, office365→loop M365 040646b3 — /tmp/axvara-compose/*-userref.webp, R2 products/ — (verifikasi: 7/7 R2 live 200, 7/7 catalog API terisi, 759/759 test, Obscura homepage OK)

- 2026-09-15 — Redo 15 tile ke logo asli terbaru via compose 3-referensi (ikon Play Store asli + gaya canva+gsuite): disney-hotstar→Disney+ 19970cc8, grok-ai→black-hole G c4418092, surfshark→sirip hiu 62292fdf, leonardo→LEO ungu fd064c67, office365→M365 Copilot fluid 74543134, youku→biru-oranye 84829e65, scribd→[S] 2025 a24144ef, wink→senyum 79e4e7ad, remini→merah familiar cf49fe81, prime-video→wordmark 2024 079d2e6c, copilot→knot+octocat 0e976247, adobe→infinity 10a30168, iqiyi→iQIYI putih 8ad57a37, devin-ai-windsurf→devin 1eacb5d9, drakor-id→badge merah 546f9039; Antigravity dicoret (tetap sparkle Gemini sesuai pemakaian), generik C tidak diubah — /tmp/axvara-compose/*-redo2.webp, R2 products/ — (verifikasi: 15/15 R2 live 200, 15/15 catalog API terisi, 759/759 test, Obscura homepage OK)

- 2026-09-15 — Foto massal 33 sisa produk aktif via muse compose (referensi canva+gsuite terkunci; prime-video 40133698, tradingview d8a8a4fa, scribd 49f29432, hma-vpn 035521e8, express-vpn a0905284, vcc 1eb96012, office365 0c9e8a13, windows 95e51c8d, picsart 879a1064, wink 27eccf99, meitu 95dc09ac, remini da55d3d0, bstation 3a6073f6, wetv f294bb56, crunchyroll 8046571f, youku ef5aa3f7, i-love-pdf e1163181, alight 9b01a45f, hotstar 234980a1, grok 693de0cd, surfshark cd0ac26f, kiro fafdc1f3, windsurf 07ebec13, hbo 86a34b31, iqiyi 95f1c2f3, drakor a860a568, camscanner 3f76f4f8, domain c00443b3, upcloud 1edce82b, adobe 301c1452, copilot c149d5aa, student 10b5bdd9, api-testing 187dda6f) — 50/50 produk aktif berfoto — /tmp/axvara-compose/*-mass.webp, R2 products/ — (verifikasi: 6/6 R2 spot 200, 13/13 catalog API terisi, 759/759 test, dev GET / 200 + CSS 200, Obscura homepage OK)

- 2026-09-15 — Redo loklok (5e4880a9) + vidio-platinum (b967e0ff) ke logo asli terbaru via compose 3-referensi (gaya canva+gsuite + logo asli Play Store/seeklogo; loklok wordmark putih miring, vidio script merah #FF0040; revisi loklok v1 neon didrop) 1600x900 WebP — /tmp/axvara-compose/*-v2.webp, R2 products/ — (verifikasi: 2/2 R2 live 200, catalog API 2/2 terisi, 759/759 test, Obscura PNG diperiksa)

- 2026-09-15 — Redo 12 foto katalog via muse compose (referensi canva+gsuite pemilik, tight close-up sejiwa: netflix fd15f96a, spotify e8f664ac, chatgpt f87f491e, loklok c113eff8, capcut 27082073, claude 6b3418cf, leonardo 23ae5893, getcontact 8ce220b4, apple-music 38e02cf8, vidio 570e5cb2, viu 3c76e50d, zoom d33cab69; YT skip manual pemilik) 1600x900 WebP — /tmp/axvara-compose/, R2 products/, docs/DESIGN.md §9 — (verifikasi: 12/12 R2 live 200, catalog API image 12/12 terisi, 759/759 test, dev GET / 200, Obscura PNG diperiksa)

- 2026-09-15 — Foto katalog batch2 (10 produk: loklok, capcut-pro, claude-pro, youtube-premium, leonardo-ai, getcontact-premium, apple-music, vidio-platinum, viu-premium, zoom-premium) 1600x900 WebP tile 3D napak live R2/D1 — /tmp/axvara-16x9/batch2/, R2 products/ — (verifikasi: 10/10 R2 live 200, D1 15 berfoto, catalog API image terisi 10/10, Obscura loklok+claude tampil sempurna, test 759/759)

> Riwayat perubahan project `axvara/` — terbaru di atas. Append-only, jangan hapus entri lama.
> Format: `- YYYY-MM-DD — <ringkas perubahan> — <file/area utama> — (verifikasi: <hasil>)`
> Aturan lengkap: `axvara/AGENTS.md` → Aturan Changelog & Verifikasi WAJIB.

- 2026-09-16 — Cron WR hidup lagi: CRON_SECRET Pages vs AXVARA_CRON_SECRET Worker tidak sama (401 diam-diam, fase basi sejak 14 Sep); rotasi keduanya + verifikasi cron manual 200 — Cloudflare Pages env + Worker secret — (verifikasi: fase notify→fulfillment, sync cron 48 produk tercatat trigger=cron)

- 2026-09-16 — Cron WR anti-starvation: bersihkan 7 job sampah order final (D1 prod) + pendingJobs hanya order lunas/paid + jaminan slot warung_rebahan bila sync basi >45 mnt (guard histori agar R12 deterministik) + 2 test — src/app/api/cron/operations/route.ts, tests/warung-rebahan/cron.test.ts — (verifikasi: 767/767 test, tsc bersih)

- 2026-09-16 — Kartu sync bedakan manual vs cron: migrasi 0031 kolom trigger + SyncOptions + kartu dua-baris (🔵 manual / 🟢 otomatis) + 2 test — drizzle/migrations/0031_wr_sync_trigger.sql, drizzle/schema.sql, src/lib/warung-rebahan/sync.ts, src/app/api/cron/operations/route.ts, src/app/api/admin/warung/sync/route.ts, src/app/api/admin/warung/sync-log/route.ts, src/components/admin/WarungRebahanManager.tsx, tests/warung-rebahan/admin-apis.test.ts, tests/warung-rebahan/wr-migrations.regression.test.ts — (verifikasi: 761/761 test, tsc bersih, dev GET / 200 + CSS 200)

- 2026-09-15 — Foto katalog 3 produk via Muse Image 3D tile (squircle hitam glossy + logo resmi 3D akurat, navy #0a1230, rim cyan/magenta, tile napak; 1600×900 WebP q82 ~17–22 KB): Netflix + ChatGPT + Spotify(v3 revisi melayang→napak) live di R2 + D1 prod; pelajaran: wrangler R2 WAJIB --jurisdiction default + generate landscape 16:9-safe — docs/DESIGN.md (standar foto produk), D1 products image_url — (verifikasi: R2 live 200 ketiga file, catalog API image terisi, Obscura katalog visual OK)

- 2026-09-15 — Aturan dokumentasi lintas-repo: setiap perubahan WAJIB update SEMUA docs relevan (tabel PRD/DESIGN/ARCHITECTURE/README/AGENTS per jenis perubahan) + checklist docs + pengingat di 4 AGENTS folder lain — axvara/AGENTS.md, axvara-wa-gateway/AGENTS.md, axvara-qris-gateway/AGENTS.md, axvara-wr-proxy/AGENTS.md, axvara-tg-bot/AGENTS.md — (verifikasi: 759/759 test, tsc bersih, dev GET / 200)

- 2026-09-15 — Sinkron docs vs prod 90%: PRD v1.1 (status live, glossary WR/proxy/gateway/bot/varian, FR-S14–S17/SYS7–SYS8, seed→data produksi) + DESIGN v1.1 (font cross-platform, kartu varian-termurah, load-more, foto welcome Telegram) + ARCHITECTURE §16.6 (sync satu sweep, produk baru otomatis, sync-log filter, throttle saldo) — docs/PRD.md, docs/DESIGN.md, docs/ARCHITECTURE.md — (verifikasi: 759/759 test, tsc bersih, dev GET / 200)

- 2026-09-14 — Sync WR satu sweep (Opsi A): budget khusus katalog +800 query (produk baru ~15 query, bukan estimasi 8,6) + cap 12→48; cursor tetap fallback; jalur order/fulfillment tetap budget 40 — src/lib/db-access.ts, src/lib/warung-rebahan/sync.ts, tests/warung-rebahan/sync.test.ts — (verifikasi: 758/758 test, tsc bersih)

- 2026-09-14 — Kartu sync tidak 0/0/0 setelah login ulang: endpoint sync-log memfilter baris saldo (products_synced NULL) + UI pakai sync produk terakhir + test regresi — src/app/api/admin/warung/sync-log/route.ts, src/components/admin/WarungRebahanManager.tsx, tests/warung-rebahan/admin-apis.test.ts — (verifikasi: 758/758 test, tsc bersih)

- 2026-09-14 — Rangkuman insiden 14 Sep + aturan anti-ulang: ARCHITECTURE §16 (proxy WR terpisah 2 akun, env wajib secret_text, larang redeploy wrangler tanpa direktori, SITE_URL absolut, ekosistem 5 folder) + README handoff + `.heroku-credentials` 2 akun (git-ignored) — docs/ARCHITECTURE.md, README.md, .heroku-credentials, .gitignore, AGENTS.md — (verifikasi: 757/757 test, tsc bersih, dev GET / 200 + CSS 200, akses Heroku akun1+akun2 via file OK)

- 2026-09-14 — Foto /start Telegram: SITE_URL kosong di worker → URL foto relatif → Telegram "URL host is empty"; paksa absolut https + buang pesan keyboard ekstra (tanpa "Menu Cepat"/"—") + hapus endpoint debug — src/lib/telegram/handlers/command.ts — (verifikasi: telegram 69/69, tsc bersih)

- 2026-09-14 — Foto welcome /start Telegram pulih: PNG R2 2,2 MB (7,8 dtk, timeout edge 10 dtk → sendPhoto gagal diam-diam) diganti WebP 31 KB di public/banners + retry 1x + fallback teks; hapus teks "Menu Cepat" (ganti "Pilih menu di bawah 👇") — public/banners/tg-welcome.webp, src/lib/telegram/handlers/command.ts, tests/telegram-bot.regression.test.ts — (verifikasi: telegram 69/69, tsc bersih)

- 2026-09-14 — Telegram aktif + anti-spam notif saldo: set TELEGRAM_BOT_TOKEN/SECRET/ADMIN_CHAT_ID (grup Axvara_Notif) + webhook terdaftar (pending 0, no error); throttle low_saldo via wr_sync_state (maks 1 pesan/6 jam, ulang bila turun ≥Rp5rb) + test regresi — src/lib/warung-rebahan/saldo.ts, tests/warung-rebahan/saldo.test.ts — (verifikasi: 757/757 test, tsc bersih)

- 2026-09-14 — Login admin prod pulih: ADMIN_EMAIL tertulis nama-key bukan alamat (challenge JWT mengikat email salah → proof 401 walau password benar); perbaiki + reset hash PBKDF2; juga: env Pages wajib secret_text, DANA QRIS + WA tersambung, folder axvara-wr-proxy permanen + axvara-tg-bot — Cloudflare Pages env — (verifikasi: login 200 adamimin@axvara.tech, /me authed:true, QRIS muncul di quote, WR balance via proxy 200)

- 2026-09-14 — Pulihkan prod pasca-rollback redeploy: env Pages wajib `secret_text` (plain_text tidak terbawa deploy — terbukti FLAG hilang tiap deploy), set DANA QRIS (payload+secret dari axvara-qris-gateway/.env) + sambung WA (salin AXVARA_WEBHOOK_TOKEN gateway→WHATSAPP_WEBHOOK_TOKEN + URL+nomor bot) + PRODUCT_VARIANTS_READ + 2 var proxy WR sebagai secret; catat ekosistem 5 folder di AGENTS.md (tambah axvara-tg-bot untuk kredensial Telegram) + folder permanen axvara-wr-proxy (pindah dari /tmp) — AGENTS.md (5 folder), Cloudflare Pages env — (verifikasi: CI success; live varian Canva 3 baris + coret, badge, load-more, login pbkdf2-proof 200)

- 2026-09-14 — Tuntaskan audit UX admin + kesiapan Warung Rebahan (sisa pekerjaan pasca-hardening `da74204`, digabung dari 5 commit audit berurutan): (a) **kepemilikan field WR ditegakkan di API, bukan cuma UI** — helper `src/lib/warung-rebahan/ownership.ts` (`isWrManaged` + `findWrOwnedViolation`) dipasang sebagai guard 409 `{error,field}` di **dua jalur tulis varian** yang ada: `PUT /api/products/:id` (nama/slug/deskripsi/harga/stok master + label/harga/coret/stok/durasi/garansi varian) dan `/api/admin/variants` PUT tunggal + POST batch — jalur kedua ini panel varian lama `VariantEditor` dan sempat lolos putaran pertama (terbukti 200, bukan 409), sehingga edit admin hilang diam-diam pada sweep sync berikutnya; nilai identik tetap 200 karena form admin mengirim payload utuh, produk manual tidak terpengaruh; (b) **migrasi 0030 `products.admin_description_override`** — teks milik admin, TIDAK PERNAH ditulis sync, NULL = pakai deskripsi WR; resolver bersama `displayDescription()` di `src/lib/catalog.ts` membuat override berlaku di SEMUA kanal, bukan cuma web: storefront, `GET /api/products`, `/api/catalog`, bot Telegram, bot WhatsApp, PDP `/produk/[slug]` (meta description + Open Graph/Twitter + JSON-LD Product), dan `/api/agent/context` (`COALESCE(admin_description_override, description)`) — tanpa ini pembeli di bot dan hasil pencarian Google melihat teks berbeda dari storefront; editor memisahkan "Deskripsi (dari WR)" read-only vs "Deskripsi khusus (override)", kosong = kembali ke teks WR; header migrasi dikoreksi: SQLite tak punya `ADD COLUMN IF NOT EXISTS` sehingga apply manual ulang gagal `duplicate column name` (diverifikasi) — jalur CI `d1 migrations apply` aman, backfill tidak diperlukan, bootstrap D1 baru pakai `schema.sql`; (c) badge `WR • dikelola otomatis` + input read-only **hanya** di editor admin, storefront nol penanda WR; (d) **stabilitas admin**: context toast tidak lagi membuat identitas baru tiap toast (akar rantai toast→rerender→refetch: loader WR terbukti 7x fetch), login memuat data tepat sekali (callback ganda dihapus), hanya `401` yang mengakhiri sesi (5xx/network tidak logout), fallback JWT dev dibuat deterministik — secret acak per-isolate adalah akar "login lalu keluar lagi" — dengan production tetap fail-closed (melempar) bila `ADMIN_JWT_SECRET` kosong; (e) **Force Sync jujur** — route balas `status` success/partial/failed (budget-yield = partial) dan UI berhenti menampilkan "Sync selesai" palsu; (f) **UX katalog storefront**: 12 produk + tombol "Tampilkan N produk lagi" menggantikan nomor halaman, produk ready di depan & stok habis tetap tampil di belakang dengan urutan stabil, harga kartu + harga coret diambil dari varian termurah yang MASIH TERSEDIA; (g) docs handoff Heroku/Cloudflare (rotasi IP egress dyno, key WR hanya di gateway) — drizzle/migrations/0030_wr_admin_ownership.sql, drizzle/schema.sql, src/lib/warung-rebahan/{ownership.ts,sync.ts}, src/lib/{catalog.ts,auth.ts}, src/app/api/products/{route.ts,[id]/route.ts}, src/app/api/admin/variants/route.ts, src/app/api/admin/warung/sync/route.ts, src/app/api/agent/context/route.ts, src/app/{page.tsx,produk/[slug]/page.tsx,admin/page.tsx}, src/components/ui/{Toast.tsx,MoneyInput.tsx}, src/components/admin/{useAdminAuth.ts,useProductManager.ts,ProductEditorModal.tsx,WarungRebahanManager.tsx,product-types.ts,sections/ProductVariantRows.tsx}, docs/ARCHITECTURE.md §15, README.md, tests/{warung-rebahan/ownership.regression,warung-rebahan/sync-honesty.regression,admin-stability.behavior,storefront-catalog-ux.behavior,catalog-available-price.regression,auth-dev-secret.regression} — (verifikasi: 756/756 test hijau (+37 dari 719, 76 file), `tsc --noEmit` bersih, `next lint` 0 error; tiap suite baru dibuktikan MERAH pada kode pra-perbaikan: variants PUT/POST `expected 200 to be 409`, kanal bot `expected 'Deskripsi dari WR' to be 'Teks AXVARA'`, token dev lintas-modul `expected null not to be null`, refetch 7x→1x, produk dimuat 2x→1x, 502 logout→tetap authed, harga kartu 2000→5000, 8-per-halaman→12+load-more; dev GET / 200 + CSS 200, /admin 200, /api/catalog 200, /produk/[slug] 200 dengan meta+OG+JSON-LD ter-render, /api/agent/context 401 tanpa token, PUT /api/products/:id 401 tanpa token, storefront 24 produk • tampil 12 tanpa penanda WR, tanpa error di log dev/console; CATATAN DEPLOY: migrasi 0029 + 0030 belum pernah diterapkan ke D1 produksi)

- 2026-09-13 — Perbaiki CI #131 (webhook 429): scope "webhook:warung" dipakai route tapi kelewat tidak ikut commit di RATE_LIMITS — max undefined → request pertama per isolate lolos, sisanya 429 selamanya (self-DoS webhook); daftarkan scope 60 + checkRateLimit fail-open berisik untuk scope tak dikenal + test regresi tests/rate-limit-scopes.test.ts (scan semua literal scope src/ + burst 3x webhook bukan 429) — src/lib/rateLimit.ts, tests/rate-limit-scopes.test.ts — (verifikasi: 719/719 test, tsc + eslint bersih, build:pages pass)
- 2026-09-13 — Hardening WR pasca-Ralph #8: migrasi 0029 exactly-once (state machine pending/claimed/submitted/ordering/processing/completed/failed/retry/blocked_balance + idempotency UNIQUE + lease 5 mnt + delivery durable + wr_credential_tokens + wr_webhook_events + backfill, terminal lama dipertahankan), klaim atomik + fencing anti-resend, sync budget-aware cursor (generation/snapshot/sub-budget + guard harga/stok/nama), fulfillment per-item wr_link_id + agregat order dari item, webhook monotonik + event log, delivery durable retry holdings, web retrieval via verifikasi WA + capability token (halaman pesanan WrCredentialsPanel + admin retrieval/resend), admin retry CAS→409, schema.sql bootstrap final WR, https-only outbound + kontrak proxy test, 15+ test regresi wajib hijau — drizzle/migrations/0029_wr_exactly_once.sql, src/lib/warung-rebahan/*, src/app/api/orders/[code]/credentials, src/app/api/admin/warung/credentials, src/app/api/admin/warung/orders/[id]/retry, tests/warung-rebahan/*, docs/ARCHITECTURE.md §15 — (verifikasi: 717/717 test, tsc + eslint bersih)
- 2026-09-11 — Perbaiki 4 UPDATE registry/varian kurang satu `now` (last_synced_at+updated_at butuh 2 values): di D1 asli melempar binding error, di fixture diam-diam changes:0 — inilah kenapa registry Canva/Gemini tetap NULL walau produk dipakai ulang (duplikat semu di screenshot) dan wr_variants update gagal; audit eksekusi semua query insert/update/zero-missing kini nol mismatch — src/lib/warung-rebahan/sync.ts — (verifikasi: 688/688 test, tsc + lint bersih; perlu sync ulang prod setelah deploy)

- 2026-09-11 — Perbaiki akar stok-0 + tanpa varian Canva/Gemini WR (temuan dari API mentah vs D1 prod): bug placeholder INSERT product_variants — 19 kolom tapi 16 `?` (literal fulfillment_mode/is_active/sort_order/wr_auto_managed menempati slot values) sehingga SEMUA insert varian baru gagal D1_ERROR dan produk tercatat error/stok 0; tulis 19 `?` eksplisit + SKU varian dari UUID penuh (24 head + 6 tail) karena 24-char awal UUID WR bisa sama (2x "Member Pro" Canva, 3x Gemini) yang sebelumnya tabrakan SKU; +1 test UUID asli Canva (3 varian unik, stok 4 terbaca); test canva-registry juga jalankan migrasi 0028 agar setara prod — src/lib/warung-rebahan/sync.ts, tests/warung-rebahan/sync.test.ts — (verifikasi: 687/687 test, tsc + lint bersih; API mentah via proxy: Canva 3 varian/stok 4, Gemini 3 varian/stok 1 — data ada, yang rusak sync-nya; perlu sync ulang prod + bersihkan 2 produk WR gagal)

- 2026-09-11 — Urungkan exclusion Canva/Gemini WR (permintaan pemilik): migrasi 0028 hapus rule %canva%/%gemini% + buka flag registry lama; sync kini memasangkan katalog untuk registry excluded-tanpa-pasangan; anti-bentrok produk manual sendiri: slug WR suffix -wr (loop 5x, UniqueError bila tetap bentrok) + nama katalog selalu "<nama> (WR)" (mis. "Canva Premium (WR)" vs manual "Canva Pro / Premium" slug canva-premium); manual tidak tersentuh (nama/slug/harga/varian); +2 test (suffix+nama, skenario Canva end-to-end: varian WR menempel ke produk WR, harga 8000→12000) — src/lib/warung-rebahan/sync.ts (refactor createAxvaraCatalogForWr), drizzle/migrations/0028_unexclude_canva_gemini.sql, tests/warung-rebahan/sync.test.ts — (verifikasi: 686/686 test, tsc bersih, lint bersih; migrasi disimulasikan SQLite: exclusions 0, registry terbuka, manual utuh)

- 2026-09-11 — Perbaiki bug parah toast sesi admin menumpuk belasan (laporan screenshot): checkAuth di useAdminAuth kini dijaga sessionEndedRef — satu sesi berakhir memberitahu tepat sekali walau render/remount/StrictMode berulang, flag ditandai sebelum toast (anti-race panggilan bersamaan), dibuka lagi saat login sukses/cek ok; cek focus window dibuat diam (pemberitahu tunggal tetap checkAuth); tambah test regresi jsdom (5x rerender → 1 toast; terbukti merah tanpa fix) — src/components/admin/useAdminAuth.ts, tests/admin-session-toast.behavior.test.tsx — (verifikasi: 684/684 test, tsc bersih, lint bersih; dev /admin 200; Obscura admin diperiksa)

- 2026-09-11 — Opsi A lawan IP whitelist WR: proxy Heroku di app yang sama (POST /wr/:endpoint balance/products/order/transactions + GET /wr/health + /wr/egress-ip; auth x-proxy-token timing-safe; api_key disuntik server-side); client.ts Axvara mode proxy (WARUNG_REBAHAN_PROXY_URL/TOKEN; tanpa api_key asli di Pages); key WR permanen di Heroku config vars; sync LIVE pertama via proxy sukses (48 produk, 46 synced, 80 varian, 2 excluded Canva+Gemini, 0 error; saldo Rp 10.000) — axvara-wa-gateway/src/{wr-proxy,index}.ts, src/lib/warung-rebahan/client.ts, .env.example, tests/warung-rebahan/client.test.ts (+2 test proxy) — (verifikasi: 683/683 test, tsc bersih, lint bersih; live balance/products via proxy; asumsi: whitelist WR = IP dyno 3.227.251.182, dapat berubah saat restart — cek /wr/egress-ip)

- 2026-09-11 — Integrasi Warung Rebahan H2H Fase 1–4 (reseller otomatis, default mati): migrasi 0027 (wr_products/variants/order_links/sync_log/saldo_log/exclusions + kolom source/wr_* products/variants, seed %canva%/%gemini%); modul src/lib/warung-rebahan/ (client edge + HMAC webhook + klasifikasi error, sync idempoten + markup 50% + agregat induk, auto-order + retry 1/5/15mnt + saldo-habis tunda 1 jam, deliver akun terenkripsi AES-256-GCM ke Telegram/WA/Web, saldo monitor + alert); webhook POST /api/webhook/warung; fase cron warung_rebahan (sync 30mnt, order due maks 4, reconcile >1 jam, saldo 1 jam; COUNT WR terpisah agar DB pre-migrasi tidak runtuh); hook lunas 4 jalur (webhook DANA, retry admin, approve bukti, konfirmasi admin); 7 admin APIs + tab Warung Rebahan (saldo, sync, antrean+retry, exclusions, markup); env WARUNG_REBAHAN_* di .env.example; health WR di bot/health; storefront tanpa ubahan (badge Stok Habis existing); docs ARCHITECTURE §15 + README — src/lib/warung-rebahan/*, src/app/api/{webhook/warung,admin/warung/**}, src/app/api/cron/operations/route.ts, webhook DANA + payments/events + proofs + admin orders, src/components/admin/{WarungRebahanManager,AdminShell}, src/app/admin/page.tsx, drizzle/migrations/0027_warung_rebahan.sql, .env.example, tests/warung-rebahan/* (8 file, 51 test) — (verifikasi: 681/681 test di 62 file, type-check bersih; dev GET / dan CSS 200; Obscura home+admin diperiksa; asumsi: API key/secret + webhook URL WR diset manual di Pages/WR dashboard, saldo WR di-top up manual)

- 2026-09-11 — Perbaiki bug edit produk multi-varian (Canva) selalu 409 "Harga dan stok dikelola per varian": PUT /api/products/[id] yang membawa variants eksplisit kini mengabaikan kolom legacy price/stock/comparePrice untuk guard + write, master dihitung ulang dari varian aktif (MIN price, agregat stok, compare NULL multi-varian); client useProductManager mode varian tidak lagi mengirim kolom legacy; guard 409 tetap hidup untuk edit legacy tanpa variants; tambah 6 test integrasi SQLite (client baru/lama, skenario Canva 5000→2000, guard legacy, single DEFAULT-variant) — src/app/api/products/[id]/route.ts, src/components/admin/useProductManager.ts, tests/product-variant-save.integration.test.ts, docs/ARCHITECTURE.md, README.md — (verifikasi: 630/630 test di 55 file, type-check bersih; dev GET / dan CSS 200; Obscura admin diperiksa)

- 2026-09-10 — Sederhanakan Telegram varian langsung ke jumlah; tombol QRIS Baru hanya di pesan QR pertama kedaluwarsa. Batasi pembaruan QRIS Web/Telegram 1x dan pendekkan deadline order ke QR terakhir; WA hanya 1 QR selama 15 menit, tanpa pembaruan termasuk lewat API, dengan instruksi hangus/order ulang dan antrean notifikasi kedaluwarsa durable. Akar WA diam: deadline order lama 60 menit dan cron hanya mengirim pesan expiry Telegram. Migrasi 0026 menyesuaikan order pending lama, menyimpan marker notifikasi per invoice, dan mencegah replay pesan terminal historis. Halaman pesanan membaca deadline serta izin pembaruan dari kedua API lookup. Asumsi: jendela menunggu permintaan QR pengganti pertama Web/Telegram tetap maksimal 60 menit; deadline pembayaran tidak menunggu cron, notifikasi mengikuti cron 5 menit/antrean gateway — src/lib/{payments,telegram,whatsapp}, src/app/api/{cron/operations,orders,payments/qris}, src/app/pesanan/[code], drizzle/{schema.sql,migrations/0026_qris_channel_policy.sql}, tests, README.md, docs/{ARCHITECTURE,PRD}.md — (verifikasi: 624/624 test di 54 file, type-check bersih, lint 0 error; build Pages lulus; dev GET / dan CSS 200; screenshot/evaluasi Obscura atas halaman pesanan aktual dengan API fixture membuktikan tombol hanya pada QR pertama kedaluwarsa, tanpa tombol pada QR pengganti/WA yang hangus; lifecycle, migrasi, retry notifikasi, dan perintah WA diuji SQLite dengan jaringan luar dinonaktifkan)

- 2026-09-10 — Perbaiki tiga regresi review: dependency sesi admin stabil menghentikan loop fetch; riwayat nominal QRIS dipertahankan trigger dalam transaksi ledger, waktu penerbitan baru dipakai webhook/retry/guard atomik, dan nominal lama tidak pernah dipakai ulang oleh order yang sama; cron menunggu expiry ORDER 60 menit, QR baru dibatasi deadline order, serta pelepasan stok berpagar deadline. Migrasi 0025 mengisi riwayat yang masih tersedia dan menandai rentang nominal dari reissue lama yang riwayatnya sudah hilang untuk verifikasi mutasi manual, tanpa menghidupkan order terminal. Test nominal acak dibuat deterministik; fixture budget cron kini mengatur expiry order dan invoice secara eksplisit — src/app/admin/page.tsx, src/lib/payments/{dana-qris,dana-history}.ts, src/lib/db/orders-transition.ts, webhook/retry DANA, cron operations, src/lib/expiry.ts, drizzle/{schema.sql,migrations/0025_qris_invoice_history.sql}, tests, README.md, docs/{ARCHITECTURE,PRD}.md — (verifikasi: 613/613 test di 53 file; type-check bersih, lint 0 error, build Pages lulus, dev GET / + CSS 200; screenshot/evaluasi Obscura memverifikasi admin hanya 1 request per endpoint setelah pindah menu dan tombol QRIS baru 1 POST lalu nominal/gambar berubah dengan respons API fixture; migrasi/backfill/penulis lama dan lifecycle pembayaran diuji SQLite dengan jaringan luar dinonaktifkan)

- 2026-09-09 — FASE 2 refactor struktural 6 file terbesar (PURE MOVE, nol perubahan perilaku): `deliver.ts` 1752→barrel 74, `telegram/webhook/route.ts` 2175→280, `db.ts` 1044→barrel 25, `telegram/messages.ts` 1027→barrel 59, `admin/page.tsx` 986→166, `whatsapp/webhook/route.ts` 936→255. (Angka "sebelum" diukur tepat sebelum masing-masing dipecah; di baseline `510ca21` ketiga file kanal berukuran 2098/991/933 dan bertambah karena perbaikan perilaku di `f65bc5a`.) Implementasi pindah ke `src/lib/fulfillment/delivery/` (9 modul), `src/lib/telegram/{handlers,messages}/` (9+7), `src/lib/whatsapp/handlers/` (5), `src/lib/db/` (6), dan `src/components/admin/{,sections/}` (7). File terbesar sekarang 395 baris (dari 2098); `processItem` 192→47 baris dan `processLegacyJob` 158→76 dipecah per fase yang sudah tersirat di kodenya. Setiap modul lama dipertahankan sebagai barrel supaya tidak ada import di repo yang putus — dibuktikan dengan probe compiler yang mengimpor SELURUH nama ekspor dari commit sebelumnya dan lolos `tsc`: 38 simbol `deliver.ts`, 51 `messages.ts`, 20 `db.ts`. Refactor dijalankan SETELAH perbaikan perilaku sudah hijau, supaya regresi bisa dibedakan dari pemindahan kode. Kontrol keamanan diverifikasi manual, bukan dari laporan: guard kepemilikan `ownerBound` (12 aksi termasuk `qrenew`) tetap di puncak `handleCallback` sebelum switch, `route.ts` Telegram hanya mengimpor `markDone`/`handleCommand`/`handleCallback` sehingga tidak ada handler sensitif yang dapat dicapai tanpa guard, himpunan handler yang dijangkau jalur command identik dengan sebelum refactor, dan urutan webhook WhatsApp tetap content-type 415 → body size 413 → auth timing-safe 401 → feature flag → allowlist grup → `isAdminMember`. `whatsapp/gateway.ts` sengaja tidak disentuh karena memuat `isPrivateIp` dan `timingSafeEqual`. Risiko terbesar `db.ts` — state in-memory mode dev — ditangani dengan menjaga SATU definisi `getSharedMem`/`getOrderMem` di `db/client.ts` yang diimpor modul lain, bukan disalin. Test grep-source yang menunjuk file lama dialihkan ke lokasi baru tanpa satu assertion pun dihapus atau dilemahkan: total tetap 2016 `expect` dan 587 `it`, identik sebelum-sesudah refactor (metode ukur sama pada `f65bc5a` dan sekarang). Diaudit ulang reviewer independen: sidik jari SQL gabungan file baru identik dengan file lama untuk keempat modul, jadi pemindahan terbukti tidak menyentuh SQL — `src/lib/{fulfillment/delivery,telegram/handlers,telegram/messages,whatsapp/handlers,db}/**`, `src/components/admin/**`, 5 route/barrel, 16 file test — (verifikasi: 598/598 test di 50 file, `tsc` bersih, 0 error lint, `GET /` + `/admin` + `/checkout` + `/produk/[slug]` + `/pesanan/[code]` + CSS + `/api/products` + `/api/categories` semua 200, homepage 24 produk dan panel admin 12 menu sidebar diperiksa visual via CDP Obscura. CATATAN JUJUR: toggle Aktif admin di dev tidak persist PUT→GET, tetapi worktree pada commit sebelum refactor menunjukkan perilaku yang SAMA — itu isolasi memori edge runtime yang sudah didokumentasikan 2026-09-01, bukan regresi. `QuickVariantModal` tidak dapat dibuka di dev karena tanpa binding D1 `variantCount` selalu 0, sehingga a11y-nya bersandar pada 13 test komponen jsdom plus verifikasi hook yang sama di browser lewat CartDrawer)

- 2026-09-09 — QRIS dapat diterbitkan ulang oleh pembeli + masa hidup order dipisah dari masa hidup QR (migrasi 0024): AUDIT MENEMUKAN fitur "retry invoice" yang diklaim ada TERNYATA hanya mengirim ulang FOTO invoice yang sama (`src/lib/telegram/invoice-retry.ts`), dan `createDanaQrisInvoice` mengembalikan baris lama (`isExisting:true`) walau sudah kedaluwarsa — jadi TIDAK ADA satu pun jalur di mana pembeli bisa memperoleh QR baru, di kanal mana pun. Akar masalahnya: `createDanaQrisInvoice` menimpa `orders.expires_at` dengan expiry invoice 15 menit, sehingga klaim "web 24 jam" salah dan SEMUA kanal efektif mati 15 menit lalu stok dilepas. Perbaikan: `QRIS_ORDER_WINDOW_MINUTES=60` dipisah dari `DANA_QRIS_EXPIRY_MINUTES=15`; `reissueDanaQrisInvoice()` baru meng-UPDATE baris ledger di tempat (wajib, karena `payment_transactions` punya UNIQUE(order_code)) dengan nominal unik baru + expiry baru, difence pada `orders.qris_reissue_count` agar dua permintaan bersamaan hanya menghasilkan satu pemenang, maksimal 3x per order, dan HANYA saat invoice lama sudah kedaluwarsa — syarat terakhir itulah yang membuat pemegang kode order lain tidak bisa membatalkan QR yang sedang aktif dipakai. Pembeli merasakannya lewat tombol "Minta QRIS Baru" di `/pesanan/[code]` dan tombol "🔄 QRIS Baru" pada keyboard invoice Telegram (masuk `ownerBound` sehingga hanya pemesan yang bisa). Nominal LAMA sengaja gagal-tertutup ke rekonsiliasi manual, bukan melunasi order — `drizzle/migrations/0024_qris_reissue.sql`, `drizzle/schema.sql`, `src/lib/payments/dana-qris.ts`, `src/app/api/payments/qris/[code]/reissue/route.ts`, `src/app/pesanan/[code]/page.tsx`, `src/lib/telegram/{keyboards,messages}.ts`, `src/app/api/telegram/webhook/route.ts`, `src/lib/rateLimit.ts` — (verifikasi: 14 test integrasi SQLite baru `tests/qris-reissue.integration.test.ts` — nominal+expiry benar-benar berganti di DB, baris ledger tetap 1, webhook DANA melunasi nominal BARU dan MENGABAIKAN nominal lama, QR aktif tidak bisa diganti, batas 3x ditegakkan; migrasi 0024 diterapkan bersih di atas skema tanpa kolom baru)

- 2026-09-09 — Jalur order Telegram disatukan ke reservasi atomik (`createChannelOrderAtomic`): Telegram adalah jalur KETIGA pembuatan order yang menjalankan `reserveInventory()` + `UPDATE product_variants` + `INSERT INTO orders` sebagai statement TERPISAH dengan rollback manual di catch, sementara Web (`db.ts`) dan WhatsApp (`commerce.ts`) sudah satu `d1.batch()`. Rollback manual hanya berjalan untuk error yang dilempar — bila isolate dihentikan di tengah (batas CPU/eviction) stok terpotong tanpa order dan tidak ada yang memulihkannya karena cron expiry tidak menemukan order apa pun. Sekarang guard stok per baris + reservasi inventory unik per baris + INSERT order berada dalam SATU batch dengan `operation_guards CHECK(valid=1)`; ditambah verifikasi pasca-klaim (UPDATE yang tidak mengenai baris TIDAK melempar di SQLite) dan klausa `(stock=-1 OR stock>=?)` pada UPDATE sebagai defense-in-depth. `reserveInventoryForLines()` dihapus karena dua jalur reservasi berbeda adalah sumber bug divergen; `expires_at` kini ditulis ISO UTC, bukan `datetime('now','+N minutes')` yang formatnya spasi dan ditafsirkan `Date.parse` sebagai waktu LOKAL (di WIB menggeser expiry 7 jam lebih awal) — `src/lib/commerce.ts`, `src/app/api/telegram/webhook/route.ts`, `src/lib/fulfillment/inventory.ts` — (verifikasi: 598 test hijau termasuk rr3-invoice-retry + telegram-retry + fulfillment-per-item; test grep-source lama diganti menjadi pemeriksaan invarian nyata)

- 2026-09-09 — Aksesibilitas modal disatukan ke satu hook + infrastruktur test frontend pertama: `QuickVariantModal` dikirim TANPA `role="dialog"`, tanpa Escape, tanpa focus trap, tanpa scroll lock, dan tanpa `aria-label` pada tombol tutup — pengguna keyboard terjebak di belakang overlay. Tidak ada test yang bisa memerah karena `vitest.config.ts` hanya meng-include `.test.ts` di environment `node`, sehingga 538 test lama SELURUHNYA backend dan file `.tsx` apa pun diam-diam tidak pernah dijalankan. Perilaku a11y yang sebelumnya disalin manual (CartDrawer lengkap, PopupBanner tanpa trap/restore fokus, QuickVariantModal kosong) dipindah ke `src/hooks/useModalA11y.ts` sebagai sumber tunggal; daftar varian diberi semantik `radiogroup`/`aria-checked` dan stok habis diumumkan lewat nama aksesibel. Infrastruktur test: jsdom + @testing-library (pinned exact), `oxc.jsx.runtime=automatic` (Vitest 4 memakai oxc, bukan esbuild; `@vitejs/plugin-react` gagal karena `vite` bersarang di dalam vitest), `environmentOptions.jsdom.url` (jsdom memblokir localStorage pada origin opaque), dan `tests/setup/jsdom-storage.ts` (Node 26 punya global `localStorage` eksperimental yang menaungi jsdom dan butuh `--localstorage-file`). BUG NYATA DITEMUKAN OLEH TEST BARU: filter `offsetParent` pada focus trap selalu kosong di jsdom dan bernilai null untuk elemen `position: fixed` — trap akan diam-diam mati; diganti filter berbasis atribut — `src/hooks/useModalA11y.ts`, `src/components/storefront/{QuickVariantModal,CartDrawer,PopupBanner}.tsx`, `vitest.config.ts`, `tests/setup/jsdom-storage.ts`, `tests/{modal-a11y.behavior.test.tsx,quick-variant-modal.behavior.test.tsx,cart-store.behavior.test.ts}` — (verifikasi: 38 test frontend baru hijau — 10 kontrak hook, 13 komponen QuickVariantModal, 15 cart store; CDP Obscura di browser nyata mengonfirmasi `aria-modal`, scroll lock, Tab wrap, dan Escape memulihkan overflow. Batas bukti: QuickVariantModal tidak dapat dibuka di dev karena tanpa binding D1 `variantCount` selalu 0, jadi verifikasi browser untuk modal itu bersandar pada hook yang sama lewat CartDrawer)

- 2026-09-09 — SSRF IPv4-mapped, constant-time dikanonisasi, telemetri WA, index ekspresi, dan sharp: (a) `isPrivateIp` mengenumerasi prefix string `::ffff:` dan MELEWATKAN `169.254` sehingga metadata link-local lolos begitu ditulis sebagai IPv4-mapped IPv6; kini tail dotted-quad dinormalisasi lalu diuji per oktet (termasuk bentuk panjang `0:0:...:ffff:a.b.c.d`), plus CGNAT 100.64/10, multicast, dan oktet malformed gagal-tertutup. (b) `constantTimeEqual` dipindah menjadi implementasi tunggal di `src/lib/security.ts`; webhook Telegram yang masih memakai `!==` kini constant-time, dan `timingSafeEqual` WhatsApp berhenti membocorkan panjang secret lewat early-return. (c) `handlePay` WhatsApp gagal tanpa jejak apa pun padahal Web dan Telegram melog — kini `console.error`. (d) Predikat penjadwalan cron membungkus kolom dengan `datetime()` (WAJIB karena baris lama berformat spasi), yang membuat index kolom biasa TIDAK terpakai; ditambah index EKSPRESI atas ekspresi yang sama tanpa mengubah satu pun query. (e) `overrides.next.sharp` masih memaku `0.35.3` yang justru versi rentan advisory `<0.35.4` → dinaikkan ke `0.35.4`; Next TETAP 15.5.24 karena menurunkannya ke 15.5.2 demi memuaskan peer range `next-on-pages` akan membuka kembali lubang keamanan yang ditutup 2026-09-03 — `src/lib/whatsapp/gateway.ts`, `src/lib/security.ts`, `src/lib/payments/dana-qris.ts`, `src/app/api/{telegram,whatsapp}/webhook/route.ts`, `drizzle/{schema.sql,migrations/0024_qris_reissue.sql}`, `package.json`, `AGENTS.md` — (verifikasi: `npm audit --omit=dev` 0 vulnerabilities; 4 test EXPLAIN QUERY PLAN membuktikan planner memakai COVERING INDEX untuk fulfillment_jobs/fulfillment_items/orders/whatsapp_outbox; 9 test SSRF notasi IPv4-mapped; AGENTS.md dikoreksi dari "194/194" usang dan "Next 14")

- 2026-09-09 — Perbaiki galeri PDP lokal yang selalu tampilkan gambar katalog pertama: client find by slug (bukan list[0]) + fallback in-memory db.ts hormati filter p.slug=? — src/app/produk/[slug]/product-detail-client.tsx, src/lib/db.ts, tests/pdp-gallery.regression.test.ts — (verifikasi: vitest 538/538)

- 2026-09-09 — Perbaiki copy pesan lunas Telegram: tegaskan produk dikirim admin via DM Telegram pribadi, nomor WA hanya jalur cadangan (Opsi 1 approved user) — src/lib/telegram/messages.ts (orderPaidMessage), tests/telegram-bot.regression.test.ts — (verifikasi: vitest 534/534)

- 2026-09-09 — Order WA kini masuk grup Telegram admin: `Order Baru — WhatsApp` saat order dibuat + `Lunas — WhatsApp` saat lunas (QRIS Hook/retry admin/approve bukti), marker idempoten + retry cron yang sama dengan Telegram, anti-replay riwayat via migrasi 0023 — messages.ts, order-notifications.ts, webhook WA/DANA, events retry, proofs approve, cron operations, migrasi 0023, tests/whatsapp-admin-notifications, docs — (verifikasi: vitest 534/534, tsc bersih, dev GET / 200)

- 2026-09-09 — Samakan ikon Grup WhatsApp jadi lingkaran hijau penuh ala icons8 seperti tombol Bot Telegram biru — public/brand/whatsapp-circle.svg (baru, circle #25D366 + bubble/telepon putih), src/components/storefront/CommunityBar.tsx (bg #25D366 + object-cover, hapus bg-white/p-0.5) — (verifikasi: vitest 527/527, dev GET / 200 + CSS 200 + /brand/whatsapp-circle.svg 200, Obscura home/eval + PNG 1280×720 diperiksa, user approve visual)

- 2026-09-09 — Hilangkan teks disclaimer third-party/garansi di konfirmasi pembelian Telegram (Langkah 2/4) — src/lib/telegram/messages.ts (confirmVariantBuyMessage) — (verifikasi: vitest 527/527)

- 2026-09-09 — Tutup RR5-01/04/07: deduplikasi audit handover dengan instr literal, fakta pelaku/waktu dari pemenang CAS, validasi identitas/qty seragam pada cron+recovery, UI mengikuti status bisnis akhir — deliver.ts, handover route, OrdersManager.tsx, tests/rr5-remediation — (verifikasi: vitest 527/527, Obscura admin 6 skenario termasuk fault storage/qty dan kontrol HTTP 200 yang belum lengkap)
- 2026-09-09 — Tutup RR5-02/03: budget D1 per invocation 40 termasuk helper/batch dengan cadangan checkpoint, materialisasi orphan bertahap, prioritas fase benar-benar dieksekusi, yield normal tidak menambah kegagalan, antrean admin auto-off tidak tertahan job lengkap — db-access.ts, cron operations, db.ts, fulfillment, Telegram invoice/notifikasi, WA outbox — (verifikasi: vitest 527/527; probe dua orphan 20 item selesai 20–21 run, maksimum 30–32 query/run, counter sesuai adapter; satu timeout pulih 20/20)
- 2026-09-09 — Tutup RR5-05/06/08: finalisasi order/job atomik di bawah lease, settlement inventory/item berpagar, jalur D1 langsung dan cron berbagi kebijakan WA, pemulihan split finalisasi historis tanpa kirim ulang; koreksi klaim laporan round 4 — deliver.ts, tests/helpers/d1-fixture.ts, docs/REVIEW-ROUND5-EXECUTION-2026-09-09.md, README.md, docs/ARCHITECTURE.md — (verifikasi: vitest 527/527 44 file, tsc bersih, build:pages pass, dev GET / + CSS + /admin 200, Obscura home/admin diperiksa; tanpa migrasi baru)

- 2026-09-08 — Eksekusi review round 4 (RR4-01–06): cron per-item + checkpoint item_cursor DB (migrasi 0022) + orphan ringan tanpa kirim inline + sinyal expiry per-jenis (init-basi/manual-WA) + handover reconcile_failed/409 + stamp audit stabil + guard qty + UI recovery POST + FencedJobMutation + sinkron failed — route cron/publish-scheduled, deliver.ts, handover route, OrdersManager.tsx, schema/migrasi 0022, tests rr4-* (22 baru) — (verifikasi: vitest 501/501 43 file, tsc bersih, build:pages pass, dev GET / 200 + CSS 200 + /admin 200, Obscura home + UI handover CDP sukses/gagal, fixture review4 6/6 terbalik-benar)
- 2026-09-08 — Koreksi klaim round 3 yang terlalu luas + tautan laporan round 4 di README/ARCHITECTURE — docs/REVIEW-ROUND3-EXECUTION-2026-09-08.md, docs/REVIEW-ROUND4-EXECUTION-2026-09-08.md, README.md, docs/ARCHITECTURE.md — (verifikasi: vitest 501/501, tsc bersih)

- 2026-09-08 — Eksekusi review round 3 (RR3-01–09): cron fits-per-unit tanpa gate grup + budget 40 jujur + fulfillment selalu-slot + antrean 1 query + recovery WA mandiri + notifikasi paid per-jenis + retry foto invoice durable (migrasi 0021) + handover manifest-check + reconcile idempoten + revokasi fail-closed + fencing lease menyeluruh + UI handover per-item — route cron, deliver.ts, webhook Telegram, invoice-retry.ts, order-notifications.ts, auth.ts, webhook-errors.ts, OrdersManager.tsx, schema/migrasi 0021, tests rr3-* (20 baru) + pengetatan cron-budget/outbox/per-item/traffic — (verifikasi: vitest 479/479 40 file, tsc bersih, fixture review3 13/13 + 9/9 terbalik-benar, dev GET / 200 + CSS 200 + /admin 200, build pass, Obscura home PNG 1280x720)
- 2026-09-08 — Koreksi docs REVIEW-REMEDIATION (tandai historis: 418 test/logout stateless/semua-selesai sudah tidak berlaku) + tautan laporan round 3 di README/ARCHITECTURE — docs/REVIEW-REMEDIATION-2026-09-08.md, docs/REVIEW-ROUND3-EXECUTION-2026-09-08.md, README.md, docs/ARCHITECTURE.md — (verifikasi: vitest 479/479, tsc bersih)

- 2026-09-08 — Fix CI gagal: test migrasi WA hermetis (DDL historis c5a4748 diinline, bukan `git show` yang mati di checkout dangkal CI) + stub payment_transactions + build:pages lolos — tests/wa-outbox-migration — (verifikasi: sim-CI tanpa histori 459/459 + tsc bersih + build:pages pass)

- 2026-09-08 — Review lanjutan R2/R3/R4/R5/R6/R8/R10/R11/R12: status campuran manual_required (bukan premature delivered), inventory dinormalisasi sebelum mapping + handover manual per-item (endpoint+UI), agregat fulfillment berpagar lease (worker basi tak buka delivered), klasifikasi error di webhook-errors.ts (build pass), race cancel/confirm berpagar, revokasi sesi lintas instance (migrasi 0020), WA CHECK prod rebuild (migrasi 0019) + recovery runtime, attention=COUNT DISTINCT order + needsAction→degraded, cron QueryBudget 45 + fase bergiliran — deliver.ts, webhook-errors.ts, orders route+handover, auth.ts, outbox.ts, service-health.ts, overview/route, cron operations, schema, 0019/0020 — (verifikasi: vitest 459/459, tsc bersih, build pass, GET / 200 + CSS 200, Obscura PNG home+admin, fixture rereview: R4 delivered-tetap + R2/R3/R5/R6/R8/R10/R11/R12 hijau)

- 2026-09-08 — Bot Telegram mati: hanya error transient yang jawab 500 (permanen/bug/config → 200 + failed agar tak menaikkan error rate webhook), catch selalu console.error + akses chat.type defensif — webhook Telegram, tests/telegram-retry (+2 perilaku klasifikasi) — (verifikasi: vitest 421/421, tsc bersih, normal 200 → transient 500 → retry 200)

- 2026-09-08 — Tombol Telegram Admin di halaman sukses pesanan kini ke akun support manusia @axvara_support (sebelumnya ke bot @Axvara_bot) + tes guard — src/app/pesanan/[code]/page.tsx, tests/checkout-integrity — (verifikasi: vitest 419/419)

- 2026-09-08 — Tandai remediasi R1–R12 selesai: docs/REVIEW-REMEDIATION-2026-09-08 (tabel bukti + dampak awam + migrasi 0017/0018 + limitasi), ARCHITECTURE (lease 0018/reviewed_by/R12 budget/R9 hierarki), README (tautan dokumen) — (verifikasi: vitest 418/418, tsc bersih, fixture 16/16, GET / 200 + CSS 200, Obscura PNG 1280x720)

- 2026-09-08 — R12 budget query cron: expiry 4/run + helper 4/run + skip leg stranded saat expiry penuh + sinyal deferred jujur (8 expiry tuntas 2 run @39 query, limit 50 tak lagi abort) — cron operations, tests/cron-budget (2 perilaku) + selaraskan tes traffic/outbox/per-item — (verifikasi: vitest 418/418, tsc bersih)

- 2026-09-08 — R11 monitoring jujur: query last_match mencakup matched (sebelumnya selalu null), kesehatan fulfillment membaca fulfillment_items + usia antrean tertua (macet = degraded, bukan configured), itemAttention dijumlahkan benar, urutan destructure query diperbaiki — overview/route, tests/overview-health (4 perilaku) — (verifikasi: vitest 416/416, tsc bersih, matched→healthy + gagal→degraded + antrean macet→degraded)

- 2026-09-08 — R10 lease outbox WA: klaim sending + worker_id + locked_until 5 mnt (getDue lewati ter-lease, CAS gugurkan snapshot basi, crash-window pulih via migrasi 0018) — outbox.ts, schema, 0018_wa_outbox_lease, tests/wa-outbox-lease (2 perilaku) — (verifikasi: vitest 412/412, tsc bersih, worker ganda→1 kirim + lease lewat pulih retry)

- 2026-09-08 — R9 tanggal omzet kanonis: hierarki bucket WIB kini ledger paid_at → orders.paid_at → reviewed_at → updated_at (order lunas manual tanpa ledger tak lagi dilaporkan di tanggal review admin) — revenue.ts, revenue-time (+1 perilaku SQLite) — (verifikasi: vitest 410/410, tsc bersih, fixture paid 1 Sep/review 7 Sep→bucket 1 Sep)

- 2026-09-08 — R8 revokasi logout: POST /api/auth/logout bump versi sesi → replay cookie lama ditolak revoked, sesi lain tetap hidup; stateless tanpa migrasi (rotasi password tetap kill-switch global) — auth.ts, logout/route, admin-session (+1 perilaku) — (verifikasi: vitest 409/409, tsc bersih, logout→replay revoked + sesi lain OK)

- 2026-09-08 — R7 callback grup: callback tanpa chat.type inferensi privasi dari tanda chat id (negatif = grup → group_redirected), id grup tak pernah tersimpan sebagai identitas user — webhook Telegram, tests/telegram-retry (+1 perilaku) — (verifikasi: vitest 408/408, tsc bersih, fixture grup→redirect + chat tersimpan 77)

- 2026-09-08 — R6 konfirmasi atomik: konfirmasi manual lunas + baris job kini satu batch D1 (crash di antaranya tak lagi strand order paid tanpa job), double-confirm tetap satu baris — db.ts, tests/order-confirm-atomic (3 perilaku) — (verifikasi: vitest 407/407, tsc bersih, interupsi injeksi→pending + 0 job)

- 2026-09-08 — R5 retry Telegram nyata: gagal proses update jawab 500 error_retryable + baris failed agar Telegram redelivery (sebelumnya 200 = silent drop), penolakan permanen tetap 200 tanpa loop, retry klaim sekali tanpa order ganda — webhook Telegram, tests/telegram-retry (3 perilaku) — (verifikasi: vitest 404/404, tsc bersih, fixture crash→500 + failed + reclaim sekali)

- 2026-09-08 — R4 recovery lock per item: releaseStaleJobs pulihkan job + item sending basi (fence parent job, hasil provider ambigu dicatat delivery_outcome_unknown, aktif tak pernah dicuri), tulis status dipagar lease (pekerja basi tak timpa pemilik baru), janji singular-delivery dilarang di komentar — deliver.ts, tests/fulfillment-lease (7 perilaku) + helper stubFulfillmentKey — (verifikasi: vitest 401/401, tsc bersih, fixture stale→released 2 + delivered 1 kirim)

- 2026-09-08 — R3 inventory + kanal web: klaim satu unit per baris unique dalam batch atomik (scoped didahulukan, legacy pool dibagi adil, verifikasi pasca-klaim per mode → kurang = rollback total tanpa order/sisa stok), balapan dua checkout tepat satu menang, quote tolak qty>1 varian unique, item web langsung manual_required + antre serah-terima admin (stabil, tanpa retry loop) — db.ts, quote, deliver.ts, tests/fulfillment-inventory (7 perilaku) — (verifikasi: vitest 394/394, tsc bersih, fixture two-unique→StockReservationError 0 order, web-shared→manual_required)

- 2026-09-08 — R2 agregat kirim tak prematur: all-manual → manual_required (bukan delivered), agregat butuh baris lengkap per identitas item, gagal materialisasi = order tetap terbuka, next_attempt_at hanya tempo cron bukan gate kirim — deliver.ts, tests/fulfillment-delivery (6 perilaku) + lock/per-item diselaraskan — (verifikasi: vitest 387/387, tsc bersih, fixture manual→manual_required + partial→terbuka + happy→delivered 2 kirim)

- 2026-09-08 — R1 pembayaran tak salah pasang: event sebelum invoice + nominal dipakai-ulang tak auto-lunas (ignored + review manual terverifikasi), klaim event+payment+job satu batch atomik, skew 0 + parse UTC untuk timestamp D1 legacy — webhook dana, admin payments/events, db.ts transitionPendingPaymentToPaid, dana-qris.ts, PaymentReconciliation.tsx, migrasi 0017, tests/payment-review + dana-causal-match — (verifikasi: vitest 380+tes R1 hijau, tsc bersih, fixture nearby_old→409 event_predates_invoice + pending, delayed→unmatched + pending)

- 2026-09-07 — BUG parah PDP ganda: blok SEO server (#11) render nama+harga+deskripsi+gambar mentah di atas PDP client sehingga seluruh konten terlihat 2x (judul, Rp1.000, deskripsi, gambar, lalu galeri + kartu beli mengulang semuanya) — hapus blok visual server, sisakan h1 sr-only + JSON-LD (crawler tak butuh blok visual; metadata/OG/canonical tak berubah), visual 100% milik client — produk/[slug]/page.tsx, tests/product-seo — (verifikasi: vitest 373/373, tsc bersih, PDP 200 + slug-ngawur 404, Obscura PDP h1=1 + JSON-LD=1 + PNG diperiksa, home 200 + CSS 200)

- 2026-09-07 — Tutup #16 (BUKAN masalah, dikonfirmasi pemilik): 24 produk seed + angka aktivasi/rating/waktu statis + gambar placeholder adalah DATA UJI staging, bukan konten final menipu — katalog final menunggu keputusan pemilik soal daftar jualan; tanpa perubahan kode, tanpa klaim baru — CHANGELOG saja — (verifikasi: konfirmasi pemilik 2026-09-07, vitest 373/373, dev GET / 200 + CSS 200, Obscura home diperiksa)

- 2026-09-07 — Rencana migrasi adapter Cloudflare (#17, RENCANA saja): `docs/CLOUDFLARE-MIGRATION-PLAN.md` — next-on-pages 1.13 ARCHIVED → target `@opennextjs/cloudflare` di Workers + Static Assets; cakupan konkret (49 file `runtime=edge` dihapus via codemod, binding D1/R2 pindah ke `getCloudflareContext` via `db.ts`/`r2.ts`, wrangler.json ditulis ulang, CI ganti build/deploy, `initOpenNextCloudflareForDev` untuk dev); risiko R1–R7 + mitigasi; eksekusi 4 tahap (preview → porting → bayangan → cutover); rollback = kembalikan DNS ke Pages (proyek lama hidup 7 hari) + D1 Time Travel; 4 keputusan pemilik ditunda; TANPA perubahan runtime di commit ini — docs + CHANGELOG saja — (verifikasi: vitest 373/373, tsc bersih, dev GET / 200 + CSS 200, Obscura home diperiksa)

- 2026-09-07 — Dependensi rentan (#15): audit terbaru `npm audit --omit=dev` 0 vuln (sebelumnya postcss nested 8.4.31 via next, 4 GHSA); pembaruan kompatibel terkecil — dev postcss `^8.4.49`→`^8.5.26` + script `postinstall` dedupe-postcss.js (hapus nested pin-exact Next, require jatuh ke root aman; guard versi + idempoten + tak pernah gagal) + lockfile diselaraskan, Next tetap 15.5.24 tanpa `audit fix --force` ke 16.3.4; eksploitasi tak terbukti (PostCSS build-time only, loader Next selalu `from`=resourcePath, upload hanya gambar, sisa advisory dev-only esbuild/undici/ws/cookie via next-on-pages tak sentuh runtime produksi) — package.json, package-lock.json, scripts/dedupe-postcss.js, tests/dependency-audit.regression.test.ts — (verifikasi: audit --omit=dev 0 vuln, vitest 373/373, tsc bersih, build:pages 56 Edge routes pass, dev GET / 200 + CSS 200, Obscura home + PNG 1280×720 diperiksa)

- 2026-09-07 — WAF Free TERPASANG (#14 follow-up): ruleset "AXVARA API rate limit" via Global API Key — `(http.request.uri.path wildcard r"/api/*")` 100 req/10 dtk/IP → Block 429 10 dtk (batas Free memaksa period 10 + characteristics cf.colo.id+ip.src; period 60 / ip.src saja ditolak API) — entrypoint http_ratelimit 1 rule enabled, ARCHITECTURE/README diselaraskan — (verifikasi: API zones+rulesets success true, entrypoint 1 rule enabled, tanpa menyentuh kredensial di Git)

- 2026-09-07 — Perlindungan trafik & efisiensi query (#14): verifikasi fakta platform (D1 Free 50 query/invocation + 100 bound param + LIKE 50 byte; WAF Free 1 rule counting IP — klaim "WAF tidak tersedia" dikoreksi + rule `/api/*` 100/mnt didokumentasikan) — rate-limit terpusat `src/lib/rateLimit.ts` (RATE_LIMITS + checkRateLimit + clientIp anti-spoof tanpa x-forwarded-for; quote 20 + upload admin 20 kini dilindungi, 4 salinan Map lokal dihapus); cron operations batch 25→8 + helper selaras (notifikasi/reminder/outbox/heal/backfill/due) + JOIN anti-N+1 (expiry, keranjang Telegram 1 JOIN + batch DELETE); quote 2 query IN (bukan 40 N+1); PDP/checkout `?slug=` exact + related `?cat=` 8 (tanpa fetch full katalog); LIKE dipotong 40 char — rateLimit, quote, orders, proof/upload, upload admin, login, cron operations, deliver, cart, order-notifications, outbox, products API, PDP client, checkout, tests, ARCHITECTURE/README — (verifikasi: vitest 369/369, tsc bersih, dev GET / 200 + CSS 200 + PDP 200 + products?slug 200 + catalog?slug 200 + related cat 200 + checkout?buy 200, quote 20×400 lalu 429 + spoof-header tetap dilimit, Obscura home + PDP + PNG 1280×720 diperiksa)

- 2026-09-07 — Monitoring & ketahanan notifikasi (#13): status layanan jujur empat tingkat (configured/healthy/degraded/unknown) via `src/lib/service-health.ts` — overview memakai pengukuran (antrean fulfillment/outbox, kirim terakhir, event QRIS 7 hari) + `system_details` berdetail, kartu Kesehatan sistem tiga warna + tooltip + legenda, bot health memaparkan usia antrean tertua + event QRIS; notifikasi penting WA "Pembayaran Diterima" masuk `whatsapp_outbox` idempoten (claim CAS + backoff 1-5-15-60 + dead) dan diproses cron 5-menit; sesi Baileys dinyatakan eksplisit sebagai milik gateway eksternal — overview, bot/health, cron operations, webhook dana, service-health.ts, whatsapp/outbox.ts, AdminOverview, BotAutomationManager, tests — (verifikasi: vitest 357/357, tsc bersih, dev GET / 200 + CSS 200 + overview/health auth 200 + tanpa-auth 401, Obscura home + PNG 1280×720 diperiksa)

- 2026-09-07 — Ketepatan laporan pendapatan (#12): revenue hari/bulan kini memakai waktu pembayaran tetap `paid_at` (WIB), bukan `updated_at` UTC yang bergerak saat pengiriman/catatan admin/retry notifikasi — semua jalur lunas (QRIS webhook, retry admin, approval bukti manual, konfirmasi admin) menulis `paid_at` sekali via COALESCE-guard dalam batch yang sama; helper kanonis `src/lib/revenue.ts` (hierarki ledger paid_at → reviewed_at bukti → paid_at order → updated_at, offset +7 jam); overview dev-fallback ikut bucket WIB + respons menandai zona Asia/Jakarta dan rentang revenue_from/to; migrasi 0016 backfill eksplisit (QRIS dari ledger, manual dari reviewed_at, sisa dari updated_at, order non-lunas tetap NULL, idempoten) — overview, db.ts, proofs, revenue.ts, schema/migrasi 0016, AdminOverview, tests — (verifikasi: vitest 346/346, tsc bersih, dev GET / 200 + CSS 200 + overview auth 200 tz Asia/Jakarta + tanpa-auth 401, backfill SQLite QRIS/manual/legacy/pending + rerun idempoten, Obscura home + PNG 1280×720 diperiksa)

- 2026-09-07 — Sitemap & SEO halaman produk (#11): sitemap query produk aktif D1 + varian aktif (lastmod updated_at, tanpa fragmen `#katalog`, fallback seed hanya bila D1 gagal); PDP jadi server component — `generateMetadata` per produk (title/desc/canonical/OG/Twitter dari D1), h1 + harga + deskripsi + gambar + JSON-LD Product/AggregateOffer IDR server-rendered, slug ngawur/nonaktif → 404 via `notFound()`; interaktivitas pindah ke `product-detail-client.tsx` tanpa ubah flow (varian/keranjang/checkout via API yang sama) — sitemap.ts, produk/[slug]/page.tsx + product-detail-client.tsx, lib/product-seo.ts, lib/db.ts (fallback varian dev), tests — (verifikasi: vitest 336/336, tsc bersih, dev GET / 200 + CSS 200 + sitemap 24 produk + PDP 200 + slug-ngawur 404, Obscura home + PDP + PNG 1280×720 diperiksa)

- 2026-09-07 — Ketepatan diskon varian (#10): `compare_price` kartu kini dari varian harga-terendah yang sama (subquery `ORDER BY price ASC LIMIT 1`, bukan `MAX` lintas varian) + gate `compare > price` di API dan kartu — diskon fiktif silang hilang, "mulai dari" MIN(price) dan rentang min–max tetap, PDP/modal/Telegram tetap per-varian terpilih — api/products/route.ts, ProductCard.tsx, tests — (verifikasi: vitest 324/324, tsc bersih, dev GET / 200 + CSS 200 + katalog/PDP 200, Obscura home/katalog-8-card + PDP + PNG 1280×720 diperiksa)

- 2026-09-07 — Netralkan formula injection ekspor CSV (#9): seluruh sel (termasuk header) lewat `sanitizeCsvField`/`csvCell` di `src/lib/csv.ts` — nilai berawalan `= + - @` (setelah buang whitespace/control penyamar) diawali apostrof `'`, quoting/escaping CSV + BOM UTF-8 + CRLF dipertahankan, data aman tak berubah — api/admin/orders/route.ts, lib/csv.ts, tests — (verifikasi: vitest 314/314, tsc bersih, dev GET / 200 + CSS 200, CSV auth 200 + tanpa-auth 401 + BOM OK, Obscura home/admin + PNG 1280×720 diperiksa)

- 2026-09-07 — Penguatan sesi admin (#8): cookie idle kini JWT HS256 2 jam terikat sid yang sama dengan admin JWT — nilai sembarang/palsu/kedaluwarsa/sid-asing/secret-lain ditolak server, refresh hanya memutar idle setelah validasi penuh, rotasi password menggugurkan seluruh sesi lama via claim av (stateless, Edge-safe), admin auth cookie-only (Bearer admin tanpa cookie ditolak; requireAgent/MCP tidak tersentuh), alasan gagal dibedakan (idle_timeout/session_mismatch/revoked) — auth.ts, login/me/refresh/logout, admin page, tests — (verifikasi: vitest 313/313, tsc bersih, dev GET / 200 + CSS 200 + /admin 200, Obscura admin/home + PNG 1280×720 diperiksa, live login→me 200→forged-idle 401→bearer-tanpa-cookie 401→refresh-rotate 200→logout-clear)

- 2026-09-07 — Kunci pengiriman kedaluwarsa dapat dipulihkan (#7): seluruh gerbang lock (claimJob, processItem, getDueJobs, releaseStaleJobs) memakai datetime() normalization agar ISO-8601 dan legacy cocok dalam satu domain — lock kedaluwarsa pulih sesuai waktu, lock aktif tetap tak bisa direbut (CAS) — deliver.ts, tests — (verifikasi: vitest 303/303, tsc + ESLint bersih, build:pages pass, dev GET / 200 + CSS 200, Obscura home/eval + PNG 1280×720 diperiksa)

- 2026-09-07 — Perbaiki CI #6 (tsc ketat): pola createRequire DatabaseSync pada test retry diselaraskan ke tipe SqliteDatabase/SqliteStatement seperti test migrasi — tests/telegram-update-retry.regression.test.ts — (verifikasi: npm test 298/298, tsc bersih, build:pages pass, dev GET / 200 + CSS 200, Obscura home/eval + PNG 1280×720 diperiksa)

- 2026-09-07 — Retry update Telegram yang gagal (#6): claim update_id kini membedakan done / processing aktif / failed / lease kedaluwarsa — reclaim atomik CAS + attempt_count (maks 5) memberi retry nyata tanpa order/invoice ganda — webhook Telegram, tests — (verifikasi: vitest 298/298, tsc + ESLint bersih, dev GET / 200 + CSS 200, Obscura home/eval + PNG 1280×720 diperiksa)

- 2026-09-07 — Pengiriman Telegram private-only (#5): kredensial tidak pernah ke grup — resolveRecipient memakai telegram_user_id buyer (bukan chat_id grup), upsert user tidak menyimpan id grup, checkout/callback grup di-redirect ke deep-link chat pribadi, callback sensitif terikat pemilik (anti take-over), ensurePrivateRecipient warisi chat privat saat buyer START, notifikasi lunas ke private chat (grup hanya notice tanpa kredensial) — webhook Telegram, deliver.ts, order-notifications, messages, tests — (verifikasi: vitest 292/292, tsc + ESLint bersih, dev GET / 200 + CSS 200, Obscura home/eval + PNG 1280×720 diperiksa)

- 2026-09-07 — Fulfillment per item + reservasi + kanal (#4): `fulfillment_items` per (order, item) via migrasi 0015 — processJob kirim SEMUA item (delivered hanya saat semua settled, gagal satu retry item itu saja tanpa kirim ulang), mode per item (snapshot lines → variant → produk), claim CAS per item, reservasi satu secret unique per baris (cart Telegram + batch atomik web), penerima eksplisit per kanal (target kosong → manual, web tanpa push → retry keras), cron backfill job pra-migrasi, job lama tetap kompatibel via fallback — deliver.ts, inventory.ts, db.ts, webhook Telegram, cron operations, schema/migrasi 0015, docs — (verifikasi: vitest 284/284, tsc + ESLint bersih, dev GET / 200 + CSS 200, Obscura home/eval + PNG 1280×720 diperiksa)

- 2026-09-07 — Order lunas tak kehilangan fulfillment (#3): baris job kini ditulis dalam batch atomik yang sama dengan transisi lunas (webhook QRIS, retry_match, approval bukti manual, konfirmasi admin) via INSERT OR IGNORE — tambah `reconcileMissingFulfillmentJobs` di cron operations (sembuhkan order lunas tanpa job via jalur ensure idempoten, sebelum proses due jobs), anti-ganda via UNIQUE order_code + claimJob sekali + markJobDelivered, flag AUTO_FULFILLMENT_ENABLED off tetap simpan job queued + ack Telegram tanpa kirim kredensial — db.ts, deliver.ts, webhook dana, events admin, proofs, admin orders, cron operations, tests — (verifikasi: vitest 274/274, tsc + ESLint bersih, dev GET / 200 + CSS 200, Obscura home/eval + PNG 1280×720 diperiksa)

- 2026-09-07 — Pencocokan kausal pembayaran QRIS (#2): event lama (diamati sebelum invoice baru terbit) tidak dapat lagi melunasi order baru via webhook/retry_match — tambah `isCausallyPlausiblePayment` (event.created_at + skew 60 dtk >= invoice.created_at) di kedua jalur, triase `event_predates_invoice`/`multiple_active_exact_amount` masuk rekonsiliasi eksplisit, claim matched kini CAS (replay = duplicate, tanpa fulfillment ganda), pesan retry admin menjelaskan tiap alasan — webhook dana, events admin, dana-qris.ts, PaymentReconciliation.tsx, tests — (verifikasi: vitest 268/268, tsc + ESLint bersih, dev GET / 200 + CSS 200, Obscura home/eval + PNG 1280×720 diperiksa)

- 2026-09-07 — Konsistensi kedaluwarsa pembayaran/order (#1): expiry ISO vs `datetime('now')` tidak pernah cocok string-wise sehingga invoice pending abadi + slot nominal tertahan — tambah helper kanonis `src/lib/expiry.ts` (isExpiredIso/isFutureIso via Date.parse), kedua cron + webhook + retry admin + reminder + sesi WA memakai semantik identik, publish-scheduled mendelegasikan order ber-ledger ke operations cron (transisi ledger+order satu batch idempoten, tidak ada stok ganda), guard paid-vs-expiry mutually exclusive + guard-id unik per attempt untuk race deterministik, repair aman `repaired_legacy_expiry` untuk data lama terminal-order/pending-ledger — cron operations/publish-scheduled, webhook dana, events admin, db.ts, order-notifications, session, whatsapp webhook, tests — (verifikasi: vitest 260/260, tsc bersih, dev GET / 200 + CSS 200, Obscura home/eval + PNG 1280×720 diperiksa)

- 2026-09-07 — Fix tombol Keranjang tidak muncul: `/start` tidak pernah mengirim `mainReplyMenu()` sehingga reply keyboard bawah tidak pernah tampil di Telegram user; sekarang kirim pesan "Menu Cepat" + `mainReplyMenu()` setelah foto welcome, tambah 🛒 Keranjang + 🔎 Cari di `homeKeyboard` inline, daftarkan `/cart` di `setMyCommands` — webhook, keyboards, api, tests — (verifikasi: vitest 252/252, tsc + ESLint bersih, build Pages pass, dev 200 + CSS 200, Obscura OK)

- 2026-09-07 — Telegram Fase 2 cart + reminder (tanpa review/promo): keranjang multi-item per user (`telegram_carts`, 20 varian maks, 1 baris unik) dengan tombol + Keranjang di qty, kelola ➖/➕/❌, ringkasan konfirmasi `cconfirm`, checkout gabungan 1 order + 1 QRIS + 1 job fulfillment mode dominan dengan rollback stok kompensasi; reminder pending via cron maks 2x interval 60 mnt khusus invoice aktif (marker CAS `telegram_reminder_count`); label 🛒 Keranjang di menu permanen + `/cart` — `src/lib/telegram/cart.ts`, webhook/keyboards/messages/order-notifications, cron, migrasi 0014, schema, tests, docs — (verifikasi: vitest 252/252, tsc + ESLint + diff check bersih, build Pages pass, dev GET `/` 200 + CSS 200, Obscura home/eval + PNG 1280×720 diperiksa)

- 2026-09-07 — Rapikan bot Telegram: katalog tanpa pengulangan sapaan/tanggal/jam (sapaan WIB tetap hanya di welcome/bantuan), /start tanpa bubble "Menu cepat" tambahan (routing teks keyboard lama tetap jalan); sinkron ARCHITECTURE/README — `src/lib/telegram/messages.ts`, `src/app/api/telegram/webhook/route.ts`, `tests/telegram-bot.regression.test.ts`, docs — (verifikasi: vitest 241/241, dev GET `/` 200 + CSS 200, Obscura home/eval + PNG 1280×720 diperiksa)

- 2026-09-07 — Rapikan admin anti-AI-slop: hemat ikon 194→79 (badge jadi teks + warna, ikon hanya aksi nyata/pesan status; Overview 9→0, Settings/Fulfillment/Confirm 0), dialog tanpa header-ikon (judul + tutup), backdrop `bg-black/60` + blur, radius `rounded-2xl/3xl`, rhythm root `mt-4` + `space-y-4`, grid varian 4→3 kolom, angka `tabular-nums`; flow/logic tidak berubah — `src/components/{ui/admin}`, `src/app/admin/page.tsx`, docs — (verifikasi: vitest 241/241, tsc bersih, diff check bersih, dev GET `/` 200 + CSS 200 + `/admin?section=orders` 200, Obscura home/eval + PNG 1280×720 diperiksa)

- 2026-09-07 — Seragamkan admin ke iOS style: badge bersama `ChannelBadge`/`StatusBadge`/`MethodBadge` (ikon + warna brand WA `#25D366`/Telegram `#229ED9`/QRIS cyan), 7 ikon iOS11 Glyph baru (telegram-app, globe, email, refresh, download, info, send — 4 ukuran), dialog header-ikon tone + blur + dvh, tombol pill berikon konsisten di 18 file (Orders/Bot/Rekonsiliasi/Methods/Shell/Overview/Kategori/Artikel/Banner/Settings/Subscriber/Agent/Dropzone/Proof/Variant/Fulfillment/ConfirmDialog/editor produk); flow/logic tidak berubah — `src/components/{ui/admin}`, `src/app/admin/page.tsx`, `public/icons/ios11/*`, docs — (verifikasi: vitest 241/241, tsc bersih, ESLint hanya warning img lama, dev GET `/` 200 + CSS 200 + `/admin?section=orders|bot` 200, 24 ikon 200, Obscura home/eval + PNG 1280×720 diperiksa)

- 2026-09-07 — Telegram Fase 1 navigasi & marketing: menu bawah permanen (Katalog/Cari/Pesanan/Bantuan) + routing teks, command `/cari` + `/orders`, welcome landing 3 bestseller by sold_count, `Terjual X` di kartu produk, riwayat `/orders` + tombol 🔁 Beli Lagi, breadcrumb `Langkah X/4`, pencarian nama/alias via pending `search:`; payment/fulfillment tidak berubah — Telegram webhook/keyboards/messages/types/api, tests — (verifikasi: vitest 241/241, tsc + ESLint + diff check bersih, build Pages pass, dev GET `/` 200 + CSS 200, Obscura home/eval + PNG 1280×720 diperiksa)

- 2026-09-07 — Perbaiki 3 laporan Telegram: hapus tombol `Masukkan Nomor WhatsApp` yang hanya mengulang prompt (input WA tetap reply-only via pending `wa_after_paid`); kirim update `Lunas — Telegram` tersendiri ke grup Axvara_Notif dengan marker `telegram_paid_admin_notified_at` + retry cron agar status grup tidak tertinggal menunggu bayar; naikkan bulk Telegram 20 → 100/order (`TELEGRAM_MAX_QTY`, regex qty 3 digit, keyboard/messages/test diselaraskan) — Telegram webhook/keyboards/messages/order-notifications, cron, fulfillment, migrasi 0013, schema, docs/tests — (verifikasi: vitest 231/231, tsc + ESLint + diff check bersih, build Pages pass, dev GET `/` 200 + CSS 200, Obscura home/eval + PNG 1280×720 diperiksa)

- 2026-09-06 — Koreksi username Telegram support pascapembayaran dari `@support_axvara` menjadi akun yang benar `@axvara_support`, termasuk tombol/link dan dokumentasi — `src/lib/site.ts`, Telegram tests, README, docs/ARCHITECTURE.md — (verifikasi: vitest, tsc, ESLint, dev HTTP/CSS, dan Obscura)

- 2026-09-06 — Sederhanakan dan benahi flow order Telegram: qty menjadi stepper `➖/jumlah/➕` dengan total terlihat + input bulk 1–20; hapus SeaBank/e-wallet sehingga CTA langsung menerbitkan satu QRIS dinamis; pindahkan permintaan WA ke setelah DANA terverifikasi `paid`; kirim notifikasi pembayaran otomatis tanpa tombol cek status; tampilkan WA admin + `@support_axvara`; kirim order-created ke grup Axvara_Notif dengan marker D1 idempoten + retry cron; batasi stok unik qty 1 — Telegram webhook/messages/keyboards/order notifications, fulfillment, cron, migrasi 0012, docs/tests — (verifikasi: vitest 225/225, tsc + ESLint + diff check bersih, build Pages 56 Edge routes pass, dev GET `/` 200 + CSS 200 + webhook/cron fail-closed, Obscura home/eval + PNG 1280×720 diperiksa)

- 2026-09-06 — Overhaul bot Telegram setara WA grup: (1) katalog datar nama produk saja tanpa kategori wajib (kategori jadi filter opsional); (2) hapus deskripsi produk di detail, ganti list garansi per-varian sinkron web/WA; (3) sapaan WIB dinamis + tanggal/jam di welcome/katalog/bantuan; (4) flow baru Varian → Qty (1–20 + bulk, tombol cepat + ketik manual) → pilih QRIS/SeaBank/E-Wallet; (5) QRIS langsung terbit untuk semua mode + order transfer manual atomik (rekening D1, stok reservasi, foto produk web tetap tampil); (6) No WA dipindah ke SETELAH invoice khusus manual + tombol Lewati, guard anti-double-tap/duplikat WA prompt — src/lib/telegram/messages.ts, src/lib/telegram/keyboards.ts, src/app/api/telegram/webhook/route.ts, tests/telegram-bot.regression.test.ts, docs/ARCHITECTURE.md, README.md — (verifikasi: vitest 223/223 pass, tsc clean, dev GET / 200, CSS 200, Obscura home OK PNG 1280×720)

- 2026-09-06 — Perbaiki command admin WA `.d`: gateway Baileys v16 mengubah identitas pengirim grup dari LID ke nomor telepon melalui `participantAlt`, webhook menghentikan seluruh `.d` sebelum pencarian produk, dan penyelesaian admin kini mengubah fulfillment order yang sudah lunas ke `delivered` tanpa salah menganggap status pembayaran `paid` sebagai fulfillment selesai — Heroku axvara-wa-gateway, src/app/api/whatsapp/webhook/route.ts, tests/whatsapp-variants.regression.test.ts, README.md, docs/ARCHITECTURE.md — (verifikasi: gateway TypeScript build pass + health connected, vitest 207/207 pass, tsc pass, ESLint pass, dev GET / 200, CSS 200, webhook route compiled, Obscura home PNG OK)
- 2026-09-06 — Kecilkan kode unik QRIS dinamis dari 1–499 menjadi 1–299 (maks +Rp299) agar pembeli tidak merasa diberatkan; batas diekstrak ke konstanta DANA_QRIS_MAX_UNIQUE_CODE — src/lib/payments/dana-qris.ts, tests/dana-qris.regression.test.ts, docs/PRD.md, docs/ARCHITECTURE.md — (verifikasi: vitest 207/207 pass, dev GET / 200, CSS 200, Obscura home OK PNG 1280×720)

- 2026-09-06 — Ganti tombol Chat WA di bottom sticky nav menjadi tombol Katalog (href /#katalog, ikon shopping-bag iOS11); hapus branch external link WA + import whatsappLink/useStoreSettings yang tak terpakai — src/components/storefront/MobileBottomNav.tsx — (verifikasi: vitest 206/206 pass, dev GET / 200, CSS 200, Obscura nav=BerandaArtikelCara OrderKatalog hasChatWA=false)

- 2026-09-06 — Rapikan copy customer-facing: hapus teks kunci 60 menit di CO, sederhanakan info QRIS/verifikasi otomatis tanpa sebut QRIS Hook DANA, hapus himbauan teknis di halaman scan QRIS — src/app/checkout/page.tsx, src/app/pesanan/[code]/page.tsx — (verifikasi: vitest 206/206 pass, dev GET / 200, CSS 200, checkout 200, Obscura copy lama hilang)

- 2026-09-06 — WA bot 4 fitur baru: (1) Pesan "Pembayaran Diterima" heboh dengan emoji 🎊🎉, detail produk/varian/total/metode, ucapan terima kasih, promo web+Telegram; (2) Admin recognition via env WHATSAPP_ADMIN_NUMBERS; (3) Command .d admin untuk tandai pesanan selesai — dual mode: reply ke pesan pembayaran (extract order code dari quotedText) atau manual .d AXV-xxx, update order status lunas+paid+delivered, reply konfirmasi meriah; (4) Welcome message otomatis member baru via first-message detection, style keren konsisten. Heroku gateway v15: forward quotedText dari quoted message ke webhook. — src/lib/whatsapp/messages.ts, src/lib/whatsapp/gateway.ts, src/app/api/whatsapp/webhook/route.ts, src/app/api/webhook/dana/route.ts, src/app/api/admin/payments/events/route.ts, Heroku axvara-wa-gateway — (verifikasi: vitest 206/206 pass, dev GET / 200, CSS 200, Obscura home OK, Heroku deployed v15)
- 2026-09-06 — Fix 3 bug: (1) Admin compare_price hilang saat re-open modal — fix snake_case/camelCase mismatch di admin/page.tsx variant mapping; (2) Katalog card tidak tampil harga coret & badge diskon — fix API products/route.ts: ganti MIN→MAX compare_price, hapus gate variantCount===1 agar semua produk variant punya comparePrice; (3) sold_count tidak pernah increment — tambah incrementSoldCountForOrder() di db.ts, panggil dari transitionPendingOrder, transitionPendingPaymentToPaid, dan proof approval. Bonus: tambah badge diskon di QuickVariantModal header+item dan PDP desktop variant picker — src/app/admin/page.tsx, src/app/api/products/route.ts, src/lib/db.ts, src/app/api/admin/proofs/[id]/route.ts, src/components/storefront/QuickVariantModal.tsx, src/app/produk/[slug]/page.tsx — (verifikasi: vitest 194/194, dev GET / 200, CSS 200, Playwright desktop+mobile katalog harga coret+badge OK, QuickVariantModal badge diskon OK)
- 2026-09-06 — Sinkronisasi stok Telegram dengan Web/WA: migrasi seluruh legacy path Telegram dari products.stock ke product_variants.stock via catalog.ts. Hapus createAndSendInvoice legacy, handleShowProduct/handleBuyConfirm/handleConfirmPurchase/handleOrderCancel/handlePendingWaInput semuanya pakai variant-level stock. Hapus gating TELEGRAM_VARIANT_FLOW (variant flow selalu aktif). handleShowProducts pakai MIN(pv.price) untuk harga akurat. — src/app/api/telegram/webhook/route.ts — (verifikasi: vitest 194/194, dev GET / 200, CSS 200, Obscura home OK)
- 2026-09-06 — Hapus dot hijau (bg-emerald-400) di badge "Bergaransi" pada Footer — src/components/storefront/Footer.tsx — (verifikasi: dev GET / 200, Obscura eval DOT SUDAH HILANG, 194/194 test pass)
- 2026-09-06 — PDP mobile Shopee-style: sembunyikan inline variant selector & CTA di mobile, ganti dengan hint "Tersedia X varian" tappable, sticky bottom bar tombol selalu enabled (tap = buka QuickVariantModal jika perlu pilih varian), re-use QuickVariantModal existing — src/app/produk/[slug]/page.tsx — (verifikasi: dev GET / 200, CSS 200, Playwright 375×812 sticky bar enabled + no inline CTA, 194/194 test pass)
- 2026-09-06 — Katalog Shopee-style: hapus preview deskripsi dari card, hapus prefix \"Mulai\" di harga, harga coret (comparePrice) tampil di sebelah harga termurah, badge diskon % di kanan atas (sudah ada) — src/components/storefront/ProductCard.tsx — (verifikasi: dev GET / 200, CSS 200, Playwright desktop+mobile card tanpa deskripsi, harga langsung, harga coret + diskon badge)
- 2026-09-06 — Kecilkan category pills di mobile (h-9→h-6, px-4→px-2, text-sm→text-[10px], icon 14→10, gap-2→gap-1.5) agar lebih compact dan lebih banyak kategori terlihat tanpa scroll — src/components/storefront/CategoryPills.tsx — (verifikasi: dev GET / 200, CSS 200, Playwright mobile 390×844 pill width 65-103px height 24px vs sebelumnya 77-122px height 28-36px, desktop sm+ tetap normal, 194/194 test pass)
- 2026-09-06 — Fix icon inactive bottom nav invisible di bg gelap: tint putih (brightness(0) invert(1)) + opacity 55% menggantikan icon hitam tanpa filter — src/components/storefront/MobileBottomNav.tsx — (verifikasi: dev GET / 200, CSS 200, Playwright mobile 390×844 navDisplay=block, inactive filter=brightness(0) invert(1) opacity=0.55, active filter=cyan)
- 2026-09-06 — WA bot: hapus deskripsi produk dari pesan detail, hapus tanda waktu ⏱ (duration) di samping garansi 🛡 di semua pesan (detail/varian/payment), perbaiki link Telegram di footer list dari axvara_support → Axvara_bot — src/lib/whatsapp/messages.ts, src/lib/site.ts — (verifikasi: dev GET / 200, CSS 200)
- 2026-09-06 — Bersihkan footer: hapus disclaimer third-party/garansi, link Ketentuan garansi, dan tombol Katalog/Artikel di bottom bar; sisakan hanya copyright — src/components/storefront/Footer.tsx — (verifikasi: dev GET / 200, CSS 200, Obscura footer text hanya © tanpa disclaimer/tombol)
- 2026-09-06 — Perjelas setup QRIS Hook memakai nilai secret sebenarnya (bukan nama variabel), tampilkan URL/header di rekonsiliasi, dan rotasi webhook secret produksi setelah reproduksi HTTP 401 — QRIS Hook admin UI/API, README/docs, Cloudflare Pages Secret — (verifikasi: vitest 194/194, tsc/lint pass, build:pages 56 routes, Obscura setup/url/header/warning/copy=true, literal nama secret 401 dan nilai baru 200 unmatched)
- 2026-09-06 — Perbaiki bug QuickVariantModal tumpang tindih di kartu katalog: render via React Portal ke document.body agar fixed positioning tidak terjebak parent transform/overflow — src/components/storefront/QuickVariantModal.tsx — (verifikasi: vitest 193/193 pass, npm run lint pass)
- 2026-09-06 — Pasang Mobile Bottom Navigation Bar (Beranda, Artikel, Cara Order, Chat WA) bergaya Liquid Glass dengan ikon iOS 11 Icons8 resmi — src/components/storefront/MobileBottomNav.tsx, src/components/ui/IosIcon.tsx, src/app/layout.tsx, src/components/storefront/Footer.tsx — (verifikasi: vitest 193/193 pass, npm run lint pass, Obscura hasBottomNav=true)
- 2026-09-06 — Pindahkan deskripsi produk ke bawah galeri foto di desktop (whitespace-pre-line) dan buat accordion deskripsi serta floating sticky action bar di mobile ala Shopee — src/app/produk/[slug]/page.tsx — (verifikasi: vitest 193/193 pass, npm run lint pass, build:pages pass 56 edge routes)
- 2026-09-06 — Tampilkan indikator teks sisa stok ringkas ("Sisa X") di bawah harga pada pilihan varian detail produk dan quick modal — src/app/produk/[slug]/page.tsx, src/components/storefront/QuickVariantModal.tsx — (verifikasi: vitest 193/193 pass, npm run lint pass, dev GET / 200)
- 2026-09-06 — Hapus teks durasi redundan di samping nama label varian produk agar tampilan kartu varian bersih — src/app/produk/[slug]/page.tsx — (verifikasi: vitest 193/193 pass, npm run lint pass, Obscura hasDurationNextToLabel=false)
- 2026-09-06 — Perbaiki tipe VariantSummary kuat dan hilangkan `any` cast pada formatDuration/formatWarranty agar ESLint/build Pages lulus — src/app/produk/[slug]/page.tsx, src/components/storefront/QuickVariantModal.tsx — (verifikasi: npm run lint pass 0 error, build:pages pass, vitest 193/193 pass)
- 2026-09-06 — Hapus badge pill garansi/aktivasi/support dan disclaimer independen yang berulang pada halaman detail produk storefront — src/app/produk/[slug]/page.tsx — (verifikasi: vitest 193/193 pass, tsc clean, dev GET / 200, Obscura detail produk hasPills=false hasDisclaimer=false)
- 2026-09-06 — Format teks garansi pada detail produk dan quick modal langsung bersih ("Full Garansi 1 Bulan", "Garansi Terbatas 7 Hari") tanpa awalan "Garansi:" — src/app/produk/[slug]/page.tsx, src/components/storefront/QuickVariantModal.tsx — (verifikasi: vitest 193/193 pass, tsc clean, dev GET / 200 + CSS 200)
- 2026-09-06 — Tata ulang form varian di modal admin menjadi terstruktur per baris (nama, harga/stok ribuan, dan masa garansi terpadu dengan live preview badge tanpa kontrol ganda) — src/app/admin/page.tsx — (verifikasi: vitest 193/193 pass, tsc clean, dev GET / 200 + CSS 200, Obscura 200)
- 2026-09-06 — Perbaiki validasi PUT produk passthrough id/aliases/image, lengkapi input angka & unit garansi per varian ("Full Garansi 1 Bulan", "Garansi Terbatas 7 Hari"), dan perbarui format garansi — src/app/admin/page.tsx, src/app/api/products/[id]/route.ts, src/lib/catalog.ts, tests/whatsapp-variants.regression.test.ts — (verifikasi: vitest 193/193 pass, tsc clean, PUT update produk dengan varian 200 ok)
- 2026-09-06 — Perbaiki persistensi varian saat modal edit produk dibuka kembali, hapus tombol varian redundan dari tabel admin, sediakan MoneyInput ribuan titik tanpa leading-zero, dan tambahkan opsi Garansi serta Durasi ringkas per varian — src/app/admin/page.tsx, src/app/api/admin/variants/route.ts, src/app/api/products/[id]/route.ts, src/components/ui/MoneyInput.tsx — (verifikasi: vitest 193/193 pass, tsc clean, dev GET / 200 + CSS 200, D1 produksi menyimpan 3 varian Canva Pro utuh)
- 2026-09-06 — Satukan kelola varian ke 1 modal produk admin ala Shopee/Tokopedia dan hadirkan Quick-Select Variant Modal di katalog storefront — src/app/admin/page.tsx, src/app/api/products/[id]/route.ts, src/app/api/products/route.ts, src/components/storefront/ProductCard.tsx, src/components/storefront/QuickVariantModal.tsx — (verifikasi: vitest 193/193 pass, tsc clean, dev GET / 200 + CSS 200, Obscura card rendering pass)
- 2026-09-06 — Bersihkan data commerce produksi untuk mulai ulang katalog manual: hard-delete 24 produk, 24 varian, 13 order beserta transaksi/fulfillment/sesi terkait, reset sequence, dan hapus 4 aset R2 terreferensi tanpa mengubah kategori, pengaturan toko, artikel, admin, atau subscriber — Cloudflare D1/R2 produksi untuk Website, Telegram, dan WhatsApp — (verifikasi: seluruh count katalog/order/transaksi 0; `/api/products?active=1` dan `/api/catalog` kosong; produksi 200; Obscura menampilkan 0 produk dan 0 link detail)
- 2026-09-05 — Tata ulang panel admin menjadi workspace operasional: navigasi berkelompok dan responsif, action center, pesanan server-side dengan filter/pagination/CSV/detail serta konfirmasi aman, rekonsiliasi QRIS Hook, status kanal yang jujur, fulfillment per varian, dan modal produk/varian yang konsisten — admin shell/overview/orders/payments/bot/products/variants, API admin, tests/docs — (verifikasi: vitest 193/193, tsc/lint pass, build Pages pass 56 edge routes, dev GET `/` 200 + CSS 200, Obscura desktop/mobile/modal diperiksa)
- 2026-09-05 — Tambahkan Pengaturan Toko terpusat untuk nama, tagline, WhatsApp, jam layanan, footer, dan logo; terapkan nilai dinamis ke identitas serta seluruh tautan dukungan storefront dengan fallback aman — `drizzle/migrations/0011_store_settings.sql`, store settings API/admin UI/hook, storefront, tests/docs — (verifikasi: simpan/pulihkan settings via Obscura pass, API 200, storefront WhatsApp sinkron, vitest 193/193, tsc/lint/build Pages pass)

- 2026-09-05 — Hapus menu Bukti Bayar yang duplikat dan satukan thumbnail serta aksi review bukti WhatsApp ke baris Pesanan tanpa mengubah otoritas QRIS Hook — admin orders/proofs UI+API, tests/docs — (verifikasi: vitest 190/190, tsc/lint pass, build Pages 53 route pass, dev GET `/` 200 + CSS 200, Obscura admin Pesanan pass)

- 2026-09-05 — Tambahkan footer promosi Telegram `@axvara_support` dan website `axvara.tech` pada respons daftar produk WhatsApp — `src/lib/{site,whatsapp/messages}.ts`, tests/docs — (verifikasi: test WhatsApp pass, tsc pass, dev GET `/` 200 + CSS 200, Obscura pass)

- 2026-09-05 — Ganti seluruh pembayaran QRIS web, Telegram, dan WhatsApp menjadi QRIS dinamis DANA Business dengan nominal unik, webhook QRIS Hook idempoten, rekonsiliasi nominal otomatis, QR PNG internal, serta hapus integrasi aktif KlikQRIS dan QRIS statis — `drizzle/migrations/0010_dana_dynamic_qris.sql`, `src/lib/payments/dana-qris.ts`, API checkout/payment/webhook, Telegram/WhatsApp, Cloudflare Pages Secrets, tests/docs — (verifikasi: payload QRIS produksi + CRC dan PNG pass, vitest 190/190, tsc/lint pass, build Pages 54 route pass, dev GET `/` 200 + CSS 200, Obscura checkout/admin diperiksa)
- 2026-09-05 — Sinkronkan katalog dan operasional admin lintas kanal: alias WhatsApp per produk dengan fallback nama web, header/detail ala JemStore dan quoted reply Baileys, pesan pembayaran ringkas, UX modal varian konsisten, tab+paginasi pesanan per kanal, serta pending Telegram mengikuti status order agar pesanan kedaluwarsa tidak dihitung — `drizzle/migrations/0009_product_whatsapp_alias.sql`, CMS produk/varian, admin orders/bot, WhatsApp messages/webhook, tests/docs — (verifikasi: vitest 190/190, tsc/lint pass, build Pages 54 route pass, dev GET `/` 200 + CSS 200 tanpa compile error, Obscura modal/admin/checkout diperiksa)

- 2026-09-05 — Enhance tampilan list produk WhatsApp ala JemStore (waktu WIB, tanggal, greeting dinamis, separator ikon, clean brand names tanpa varian, list lengkap tanpa paginasi) tetap sinkron database D1 — src/lib/whatsapp/messages.ts, src/app/api/whatsapp/webhook/route.ts, tests — (verifikasi: vitest 199/199 pass, tsc clean, dev GET / 200 + CSS 200, Obscura pass)
- 2026-09-05 — Hubungkan Cloudflare Pages ke Baileys gateway Heroku mandiri (zero-watermark, latency instan) via WHATSAPP_GATEWAY_URL fallback & sendTextMessage/sendImageMessage — src/lib/whatsapp/gateway.ts, axvara-wa-gateway, CHANGELOG.md — (verifikasi: vitest 199/199 pass, tsc clean, Heroku Baileys gateway /health 200, test send grup sukses id 3EB0DAF45022F9FF431DD3)

- 2026-09-05 — Aktifkan fitur transaksi & pembayaran grup WhatsApp siap produksi (Payment, Proof Intake, Fulfillment) dan perbaiki resiliency reply Fonnte (fallback direct send jika inboxid ditolak Fonnte, parsing array id) — src/lib/whatsapp/gateway.ts, Cloudflare Pages Secrets, README/docs — (verifikasi: vitest 199/199 pass, tsc clean, lint pass, Pages secrets WHATSAPP_GROUP_PAYMENT/PROOF/FULFILLMENT aktif, Fonnte device connect & webhook token sinkron; dev GET / 200 + CSS 200, Obscura screenshot pass)


- 2026-09-05 — Aktivasi bertahap bot grup WhatsApp: sinkronkan daftar grup Fonnte satu kali, temukan satu grup AXVARA, simpan token/webhook secret/nomor Device/GID sebagai Pages Secrets, pasang webhook + Auto Read grup, dan aktifkan discovery read-only sementara payment/proof/KlikQRIS/fulfillment tetap off — Cloudflare Pages config, Fonnte Device, README/docs — (verifikasi: Device Fonnte `connect`, fetch group sukses 9 grup/1 kandidat AXVARA, update Device sukses, 12 Pages Secrets terdaftar; dev GET `/` 200 + CSS 200 tanpa compile error, Obscura load/eval + screenshot 1280×720 diperiksa; deployment/smoke test grup menyusul via CI)

- 2026-09-05 — Ganti browser verifier wajib proyek dari Chrome DevTools MCP menjadi skill/CLI/CDP Obscura, termasuk command localhost, screenshot/eval, dan checklist selesai — `AGENTS.md` — (verifikasi: `obscura --help` sesuai command; dev GET `/` 200 + CSS 200 tanpa compile error; Obscura load/eval + screenshot PNG 1280×720 diperiksa)

- 2026-09-05 — Perbaiki CI/CD yang gagal di migrasi D1 `0008`: pindahkan sementara FK `order_code` pada transaksi, fulfillment job, dan bukti sebelum rebuild parent `orders`, pulihkan lalu validasi sebelum commit; tambah regression test eksekusi dengan child rows produksi-like — `drizzle/migrations/0008_orders_multichannel.sql`, `tests/migration-and-atomicity.regression.test.ts`, README/docs — (verifikasi: reproduksi sebelum fix gagal `FOREIGN KEY constraint failed`, sesudah fix fixture child rows + `foreign_key_check` pass; vitest 199/199, tsc clean, lint pass dengan warning existing, build:pages pass 53 edge routes, dev GET `/` 200 + CSS 200 + admin bot 200 + webhook tanpa secret 401, Obscura render homepage pass)

- 2026-09-05 — Cross-check dan hardening akhir katalog varian + bot grup WhatsApp: perbaiki migrasi D1 dengan FK deferred dan invariant satu bukti aktif, default variant fresh schema, sinkronisasi/arsip CMS, katalog serta checkout variant-authoritative, webhook Fonnte payload-secret/inbox retry/rate limit/retensi, order+stok+inventory atomik, snapshot copy transaksi, QRIS statis vs dinamis termasuk bukti yang datang setelah callback paid, review bukti admin, payment split-state recovery, fulfillment paid-only per varian/DM, kompatibilitas inventory Telegram, serta cegah token Fonnte ikut terkirim saat unduh media — `drizzle/{schema.sql,migrations/0008_orders_multichannel.sql}`, `src/{app/api/{products,checkout,whatsapp,telegram,payments,cron,admin},components/{admin,storefront},lib/{catalog,commerce,db,payment-proofs,whatsapp,fulfillment}}`, tests, README/docs — (verifikasi: vitest 198/198 pass, tsc clean, lint/build:pages pass 53 edge routes, D1 fresh schema + migrasi 0008 pass dengan 24 produk/24 varian dan foreign_key_check kosong, dev GET `/` 200 + CSS 200 + admin 200 + catalog 200, endpoint admin/webhook tanpa auth 401)

- 2026-09-04 — Audit & perbaikan menyeluruh varian & WhatsApp bot (P0 & P1): (1) Fonnte official parser (sender=group, member=phone, inboxid=reply/id, url/filename/ext), (2) timingSafeEqual constant-time webhook auth (401 sebelum parse/D1, no secret log, limit 64KB, content-type check), (3) migrasi forward 0008 rebuild tabel orders kompatibel multi-channel (web/telegram/whatsapp) + channel_conversation_id + channel_member_id + FK intact, (4) atomic order creation & stock reservation via D1 operation_guards + deterministic idempotency key, (5) real payment proof intake: streaming HTTPS download (SSRF-safe, private IP check, 5MB limit, magic bytes check, SHA256, R2 private upload, D1 payment_proofs insert with rollback, ack only on success) + admin review API (GET /api/admin/proofs, POST /api/admin/proofs/[id]/review CAS approve/reject), (6) authoritative web variants: variant_id in quote request/token/order, CartDrawer variant info/keys, ProductCard "Pilih Varian →" gate for multi-variant, (7) Telegram bot strongly-typed VariantSummary (product_id mapped, no any, callback cross-validation, variant stock restore on cancel), (8) WhatsApp KlikQRIS reuse vs static fallback, proof gate before fulfillment, DM fulfillment only (never to group), (9) all lint errors fixed & build:pages 53 edge routes 0 errors — `drizzle/{schema.sql,migrations/0008_orders_multichannel.sql}`, `src/lib/{whatsapp/gateway.ts,commerce.ts,db.ts,catalog.ts,fulfillment/deliver.ts,r2.ts}`, `src/app/api/{whatsapp/webhook,admin/proofs/*,admin/variants,checkout/quote,orders}`, `src/components/{storefront/{ProductCard,CartDrawer},admin/VariantEditor}`, tests — (verifikasi: vitest 170/170 pass, tsc clean, build:pages pass 53 edge routes, dev GET / 200, CSS 200, admin 200)

- 2026-09-04 — Implementasi penuh Varian Produk Terpusat & Bot Grup WhatsApp AXVARA (Fase 1-7): migrasi 0007 (product_variants, whatsapp_sessions, inbox_events, outbox, payment_proofs, backfill default variants), catalog service bersama (`src/lib/catalog.ts`), canonical warranty policy (`src/lib/warranty-policy.ts`), feature flags (`src/lib/feature-flags.ts`), CMS VariantEditor (`src/components/admin/VariantEditor.tsx`), variant admin API (`/api/admin/variants`), catalog API (`/api/catalog`), variant selector storefront (`/produk/[slug]`), cart variant support (`src/stores/cart.ts`), checkout variant support (`/checkout`), Telegram variant flow (`/api/telegram/webhook`), Fonnte WhatsApp gateway + webhook (`/api/whatsapp/webhook`), channel-agnostic commerce (`src/lib/commerce.ts`), 22 test baru (153 total) — `drizzle/{schema.sql,migrations/0007_product_variants.sql}`, `src/lib/{catalog,warranty-policy,feature-flags,commerce,whatsapp/*}`, `src/app/api/{admin/variants,catalog,whatsapp/webhook}`, `src/components/admin/VariantEditor.tsx`, `src/app/{admin,produk/[slug],checkout}/page.tsx`, `src/stores/cart.ts`, `tests/whatsapp-variants.regression.test.ts`, docs — (verifikasi: vitest 153/153 pass, tsc clean, dev GET / 200, CSS 200, admin 200)

- 2026-09-04 — Perbarui rencana WA sesuai flow grup: setelah pilih varian bot meminta `pay/payment`, mengirim QRIS + SeaBank + e-wallet, mewajibkan reply bukti terikat order/member untuk review, serta menambah command garansi dari policy Telegram yang sama — `docs/WHATSAPP-GROUP-BOT-PLAN.md`, README, docs/ARCHITECTURE.md — (verifikasi: review flow/code payment-proof-warranty + `git diff --check` pass; dev GET `/` 200 + CSS 200 + `/admin?section=bot` 200, log tanpa compile error)

- 2026-09-04 — Revisi handoff menjadi arsitektur varian terpusat: produk induk + SKU berisi plan/durasi/garansi/harga/stok/fulfillment dikelola CMS untuk web, Telegram, dan alur WhatsApp sederhana `list → nama produk → varian bernomor` — `docs/WHATSAPP-GROUP-BOT-PLAN.md`, README, docs/ARCHITECTURE.md — (verifikasi: review screenshot/codebase + `git diff --check` pass; dev GET `/` 200 + CSS 200 + `/admin?section=bot` 200, log tanpa compile error)

- 2026-09-04 — Matangkan handoff bot grup WhatsApp existing: produk langsung dari D1 tanpa kategori/sinkronisasi, group-to-private order, reuse KlikQRIS Telegram, migrasi multi-channel, security/idempotency, test, rollout, dan rollback — `docs/WHATSAPP-GROUP-BOT-PLAN.md`, README, docs/ARCHITECTURE.md — (verifikasi: review planning + `git diff --check` pass; dev GET `/` 200 + CSS 200 + `/admin?section=bot` 200, log tanpa compile error)

- 2026-09-04 — Arahkan buyer pasca-pembayaran ke Telegram support `@axvara_support`, tambah `/chatid`, tetapkan grup privat `Axvara_Notif` sebagai satu tujuan notifikasi order web/bot, tunggu percobaan notif web sebelum respons selesai, dan aktifkan bot produksi — `src/lib/telegram/*`, `src/lib/fulfillment/deliver.ts`, `src/app/api/{orders,telegram/webhook}`, Cloudflare Pages, README/docs — (verifikasi: vitest 131/131, tsc clean, build Pages pass; dev GET / 200 + CSS 200 + /admin 200, webhook route compile)

- 2026-09-04 — Pulihkan login admin produksi: normalisasi quote secret, proof PBKDF2 browser→challenge JWT 5 menit, rotasi hash admin di Cloudflare Pages, dan recovery deploy `98428ac7` — `src/lib/auth.ts`, `src/app/api/auth/login/route.ts`, `src/app/admin/page.tsx`, Cloudflare Pages auth config, tests/docs — (verifikasi: vitest security 39/39, tsc/build pass, dev GET / + CSS 200, prod challenge 200 + login 200 + `/api/auth/me` 200)

- 2026-09-04 — Tambah kontak admin & notifikasi order: (1) halaman sukses /pesanan/[code] perjelas pesan "admin akan menghubungi via WA" + tambah tombol Telegram @axvara_support, (2) notif Telegram otomatis ke admin saat order web masuk (best-effort, fire-and-forget) dengan tombol WA Buyer + Panel Admin, (3) Telegram bot minta no WA buyer HANYA untuk produk fulfillment manual (shared/unique skip karena bot otomatis kirim), migrasi 0006 tambah kolom pending_action di telegram_users — `src/lib/{site,telegram/{messages,keyboards,api}}.ts, src/app/{pesanan/[code],api/{orders,telegram/webhook}}/route.ts, drizzle/{schema.sql,migrations/0006}` — (verifikasi: vitest 127/127 pass, tsc clean, dev GET / 200 + CSS 200 + /pesanan 200)

- 2026-09-04 — Bersihkan /start BOT ke welcome simpel (opsi 1): disclaimer third-party dihapus dari teks, tombol Garansi & Ketentuan tetap ada + proteksi /garansi/pre-bayar tidak berubah — `src/lib/telegram/messages.ts, tests/telegram-bot.regression.test.ts, docs/ARCHITECTURE.md` — (verifikasi: vitest telegram 25/25 pass, tsc clean, dev GET / 200 + CSS 200)

- 2026-09-04 — Rewrite docs/SEO-GEO-PLAN.md: deep audit SEO (score 28/100) & GEO (12/100), 6-phase improvement plan dengan riset 2026, diagnosis CSR blocking, metadata gaps, schema gaps, performance issues, AI crawler rules, content strategy — `docs/SEO-GEO-PLAN.md` — (verifikasi: file-only, no code change)

- 2026-09-04 — Pasang proteksi garansi third-party di BOT Telegram: /start disclaimer + tombol Garansi & Ketentuan, command /garansi (ketentuan + 6 syarat klaim ganti-bukan-refund), konfirmasi pre-bayar "Saya Paham, Lanjut Bayar", detail produk pointer garansi ikut deskripsi, follow-up pasca-kirim simpan invoice — `src/lib/telegram/{messages,keyboards,api}.ts, src/app/api/telegram/webhook/route.ts, tests/telegram-bot.regression.test.ts` — (verifikasi: vitest 127/127 pass, tsc clean, dev GET / 200 + CSS 200)

- 2026-09-04 — Perketat garansi third-party anti-refund: halaman /garansi-replace jadi acuan ketentuan (bukan official, garansi 1x24 jam–30 hari ikut deskripsi produk, klaim = ganti bukan refund), checkbox persetujuan wajib blokir submit checkout, disclaimer + link garansi di detail produk/footer/sukses pesanan — `src/app/{garansi-replace,checkout,produk/[slug],pesanan/[code],page}.tsx, src/components/storefront/Footer.tsx, docs/ARCHITECTURE.md, README.md` — (verifikasi: dev GET / 200 + CSS 200 + /garansi-replace 200 + checkout?buy 200, copy third-party/DYOR tampil)

- 2026-09-04 — Fix adapter KlikQRIS sesuai dokumentasi resmi: auth via header `x-api-key` + `id_merchant` (bukan body), tambah mode `inhouse` (PG KlikQRIS `/api/qris/*`), perbaiki endpoint sandbox (`/api/sandbox/qris/*`) dan mypg (`/api/qrisv2/*`), pisahkan callback parser inhouse (flat `{ order_id, status, total_amount, signature }`) dari mypg (nested `{ data: { order_id, amount_paid, status, merchant_id } }`), tangani `qris_image` data URI sebagai fallback `qris_url`, 13 test baru (114 total) — `src/lib/payments/{types,klikqris}.ts`, `src/app/api/telegram/webhook/route.ts`, `.env.example`, tests/klikqris — (verifikasi: vitest 114/114 pass, tsc clean, dev GET / 200 + CSS 200)

- 2026-09-03 — Implementasi MVP Bot Telegram auto-order + KlikQRIS + Fulfillment: migrasi 0005 (6 tabel baru + kolom products/orders), Telegram API wrapper/keyboards/messages (HTML escape, stateless callback ≤64B, pagination, Bahasa Indonesia), adapter KlikQRIS sandbox/MY PG terisolasi (timeout, URL allowlist, parser strict), vault AES-256-GCM (WebCrypto, fingerprint SHA-256, no reveal), inventory reserve/release/consume atomik, fulfillment outbox (manual/shared/unique + retry 1-5-15-60 menit), webhook route (secret header, update dedupe/lease, Zod validation), callback route (signature+amount+status API confirm, idempotent paid/expired/failed), cron operations (stale init, expired, missed callback recovery, due jobs, stale locks), admin UI "Bot & Otomasi" (health card, webhook setup, product fulfillment mode, shared secret input, bulk inventory import ≤100), feature flags default off, 27 test baru (101 total), docs updated — `drizzle/{migrations/0005,schema.sql}`, `src/lib/{telegram,payments,fulfillment}/*`, `src/app/api/{telegram/webhook,payments/klikqris/callback,cron/operations,admin/{telegram/setup,bot/health,fulfillment}}`, `src/components/admin/{BotAutomationManager,AdminShell}.tsx`, `src/app/admin/page.tsx`, `mcp-worker`, `.env.example`, README, docs/ARCHITECTURE.md, tests — (verifikasi: vitest 101/101 pass, tsc --noEmit pass, dev GET / 200 + CSS 200 + /admin 200, webhook 503 tanpa secret, callback 503 disabled, cron 401 tanpa auth, admin endpoints 401 tanpa auth, Bot & Otomasi section tampil health card + 24 produk dropdown + fulfillment config)

- 2026-09-03 — Jadikan GitHub Actions satu-satunya CI/CD push `main`: test, type-check, build, migrasi D1 tertunda, deploy Pages, dan deploy MCP Worker; agent wajib berhenti setelah push tanpa memantau workflow — `.github/workflows/ci.yml`, `wrangler.json`, AGENTS.md, README, docs/ARCHITECTURE.md — (verifikasi: actionlint 1.7.12 pass, 3 Actions Secrets terset, migrasi 0002–0004 dibaseline dan remote menyatakan tidak ada migrasi tertunda, Cloudflare Git deployments production/preview dinonaktifkan)
- 2026-09-03 — Tambah jalur impor gambar artikel dari URL publik agar agent tidak bergantung pada base64 panjang, pertahankan multipart untuk file lokal, serta lindungi fetch dengan validasi URL/redirect, batas streaming 5 MB, dan pemeriksaan WebP — `src/app/{mcp,api/agent/media}`, `src/lib/{article-media,remote-media}.ts`, `mcp-worker`, tests, README/docs — (verifikasi: vitest 74/74 + tsc/build pass, dev home/CSS 200, Pages deployment `3e765ee6`, Worker version `459a4f86`, kedua MCP memuat 11 tool, multipart→URL import menghasilkan WebP 17.954 byte, URL privat ditolak, token dan dua objek R2 QA dihapus)
- 2026-09-03 — Buat artikel "Cara Memilih Aplikasi Produktivitas yang Sesuai Kebutuhan Kerja" (~1200 kata, 7 langkah praktis, target pekerja digital Indonesia) lalu submit untuk review — `src/app/api/agent/articles/route.ts`, draft id 22 — (verifikasi: create draft 201, submit review 200, status review)

- 2026-09-03 — Audit `mocasus/telegram-auto-order-bot` dan revisi planning: tetapkan implementasi native AXVARA, repo eksternal hanya referensi UX karena Python polling/SQLite/VPS, kontrak KlikQRIS usang, fulfillment/idempotency tidak memadai, dan CI terbaru gagal — `docs/TELEGRAM-BOT-KLIKQRIS-PLAN.md`, README — (verifikasi: audit source commit `9e870bc`, Python compile pass, GitHub Actions terbaru gagal pada Docker/config/import, `git diff --check` pass, dev GET `/` 200 + CSS 200 tanpa compile error)

- 2026-09-03 — Ganti identitas login admin dari email hostname Pages menjadi `admin@axvara.tech` pada fallback, form, konfigurasi contoh, dokumentasi, dan secret Cloudflare Pages lalu deploy — `src/lib/auth.ts`, `src/app/admin/page.tsx`, `.env.example`, README, Cloudflare Pages — (verifikasi: vitest 72/72 + tsc pass + build pass, dev home/CSS/admin 200, login email baru 200 dan lama 401, production admin 200 dengan bundle hanya memuat email baru, MCP 405/401 sesuai kontrak, deployment `bf8e48a0`)
- 2026-09-03 — Terapkan migrasi kategori/newsletter ke D1 produksi dan deploy pembaruan storefront/admin ke Cloudflare Pages — `drizzle/migrations/0004_categories_newsletter.sql`, Cloudflare D1/Pages — (verifikasi: Bundle Kucing memakai slug `bundle-kucing` + ikon `packaging`, tabel subscriber valid, deployment `e7ef769e`, production home/CSS/artikel/cara-order/garansi/admin/categories/sitemap 200, subscribers publik 401, POST email QA 201 lalu data uji dibersihkan, Obscura visual pass)
- 2026-09-03 — Sederhanakan copy footer, ubah tema artikel menjadi Seputar AI dan teknologi, pusatkan WA admin `089519388264`, serta tambah halaman Cara Order dan Garansi & Replace dari menu Bantuan — `src/lib/site.ts`, `src/components/storefront/Footer.tsx`, `src/app/{page,artikel,cara-order,garansi-replace,pesanan,sitemap}`, README/docs — (verifikasi: tsc pass, vitest 72/72, lint exit 0 warning `<img>` lama, build pass, Obscura visual footer/artikel/2 halaman bantuan pass, dev semua route 200)
- 2026-09-03 — Matangkan kategori: 12 pilihan ikon tersimpan, modal edit nama/ikon/urutan, slug stabil saat rename, proteksi hapus kategori berisi produk, dan footer Jelajah memakai kategori D1/API yang sama — `src/lib/category-icons.ts`, `src/components/{admin/CategoryManager,storefront/{CategoryPills,Footer}}.tsx`, `src/app/api/categories`, `src/lib/db.ts`, schema/migration 0004 — (verifikasi: CRUD dev create→rename slug tetap + ikon crown→delete 200, filter URL aktif dan ikon shield via Obscura, build pass)
- 2026-09-03 — Ganti form Tetap update dari WhatsApp ke email tersimpan dan tampilkan daftar terproteksi di panel admin — `src/app/api/subscribers/route.ts`, `src/components/admin/{NewsletterSubscribers,AdminShell}.tsx`, `src/app/admin/page.tsx`, `drizzle/{schema.sql,migrations/0004_categories_newsletter.sql}` — (verifikasi: POST email 201, GET publik 401, GET admin 200 dengan 1 email cocok, deduplikasi test pass, build pass)
- 2026-09-03 — Tolak encoding base64url JWT checkout yang nonkanonis agar perubahan karakter token tidak lolos sebagai byte signature yang sama — `src/lib/auth.ts`, `tests/checkout-integrity.regression.test.ts` — (verifikasi: vitest 72/72, build pass)

- 2026-09-03 — Matangkan handoff implementasi Bot Telegram auto-order + KlikQRIS MY PG: arsitektur webhook, schema transaksi/inventory, vault AES-GCM, idempotency/outbox, flow admin, owner gates, fase eksekusi, test, rollout, dan rollback; belum mengubah runtime — `docs/TELEGRAM-BOT-KLIKQRIS-PLAN.md`, README — (verifikasi: review dokumen + tautan sumber resmi, `git diff --check` pass, dev GET `/` 200 + CSS 200 tanpa compile error)

- 2026-09-03 — Cross-check dan tuntaskan audit checkout/UI: quote harga-stok-rekening HS256 60 menit, retry order idempotent, reservasi/restore stok D1 atomik termasuk stok unlimited, expiry Pending 24 jam, direct-buy dan race quote aman, sumber katalog/payment authoritative, admin bisa edit QRIS serta tambah bank, bukti lama tidak dapat terkirim ulang, drawer aksesibel, dan visual/polling status mengikuti keadaan aktual — `src/{app/api/{checkout,orders,payment-methods,admin/orders,cron},app/{checkout,pesanan,produk,page},components/{admin,storefront},lib,stores}`, `drizzle/schema.sql`, tests, README/docs — (verifikasi: tsc pass, vitest 70/70, lint exit 0 dengan warning `<img>` lama, dev GET `/` 200 + CSS 200 tanpa error log, API quote/order retry/status pass, Obscura visual checkout/cart pass)

- 2026-09-03 — Terapkan migrasi integritas checkout ke D1, perbaiki 3 URL gambar katalog mati, isi URL QRIS authoritative, nonaktifkan banner uji `Mantapppea` yang menghalangi checkout, batasi popup ke homepage, dan deploy Pages — `drizzle/migrations/0003_checkout_integrity.sql`, D1 `axvara-db`, `PopupBanner`, Cloudflare Pages — (verifikasi: D1 2 kolom order + guard table + dead image 0, production home/CSS/quote/payment/detail/admin 200, endpoint admin/cron tanpa auth 401, checkout tanpa popup via Obscura, deploy `9422cbf2`)

- 2026-09-03 — Jadikan `axvara.tech` satu-satunya jalur publik dengan Bulk Redirect 308 dari hostname produksi+preview `axvara.pages.dev`, pindahkan origin internal Worker ke domain utama, dan deploy ulang Worker tanpa mengekspos kredensial — Cloudflare Bulk Redirects, `mcp-worker/wrangler.toml`, README, docs/ARCHITECTURE.md, docs/PRD.md, tests — (verifikasi: root/path/query/preview `pages.dev` → `axvara.tech` 308, POST MCP mengikuti redirect dan berakhir 401 auth, apex 200, Worker MCP 401 auth, tsc pass, dev home+CSS 200, vitest MCP/CMS 18/18; suite penuh 56/57 dengan 1 assertion checkout lama di luar perubahan; Worker version `9155e451`)

- 2026-09-03 — Konfirmasi DS registrar telah terpublikasi dan status DNSSEC Cloudflare berubah aktif — Cloudflare DNSSEC, README, docs/ARCHITECTURE.md — (verifikasi: DS identik terjawab oleh 1.1.1.1+8.8.8.8, API DNSSEC `active`)

- 2026-09-03 — Selesaikan aktivasi delegasi `axvara.tech`, verifikasi HTTPS apex dan redirect `www`, lalu aktifkan DNSSEC Cloudflare untuk menunggu publikasi DS registrar — Cloudflare DNS/Pages/SSL/DNSSEC, README, docs/ARCHITECTURE.md — (verifikasi: NS Cloudflare cocok di 1.1.1.1+8.8.8.8, zone `active`, HTTP→HTTPS 301, HTTPS apex 200, `www`→apex 308 dengan path/query, sertifikat Google Trust valid untuk apex+wildcard; DNSSEC `pending` sampai DS dipasang)

- 2026-09-03 — Siapkan `axvara.tech` sebagai domain utama: buat Cloudflare Free zone, kaitkan apex+`www` ke Pages `axvara`, pasang CNAME proxied dan redirect 308 `www`→apex, aktifkan Universal SSL/HTTPS/TLS 1.2+, serta sinkronkan canonical/sitemap/robots dan dokumentasi; aktivasi akhir menunggu nameserver registrar — Cloudflare DNS/Pages/Rules, `src/app/{layout,sitemap,robots,artikel}`, `src/components/admin/AgentIntegration.tsx`, `.env.example`, README, docs — (verifikasi: API zone + 2 custom domain `pending`, dev GET `/` 200 + CSS 200 + `/admin` 200, robots/sitemap memakai `axvara.tech`; vitest 56/57 dan tsc/deploy tertahan error `BANKS` checkout yang sudah ada)

- 2026-09-03 — Rapikan status bukti pembayaran menjadi preview/status card + lightbox profesional, sederhanakan empty-state artikel, kosongkan nama awal agent, bebaskan scroll sentuh dari orbit, pertahankan rasio asli banner WebP, dan cegah popup banner di `/admin` — `src/{app/admin,app/artikel,components/admin,components/storefront}`, tests, README, docs/ARCHITECTURE.md — (verifikasi: vitest 57/57, tsc pass, build:pages 38 Edge route pass, Chrome CDP touch orbit scrollY 0→260, admin banner request/dialog 0, popup prod 924×665 object-contain, deploy Pages `8c341e53`)

- 2026-09-03 — Permanenkan jalur deploy MCP Worker memakai Global API Key lokal tanpa mengekspos kredensial, dengan fallback domain Workers.dev dan origin `axvara.pages.dev` — `package.json`, `.cf-credentials` (git-ignored), `mcp-worker/wrangler.toml` — (verifikasi: `npm run deploy:mcp -- --dry-run` pass, Global API Key aktif, `.cf-credentials` tetap di-ignore)

- 2026-09-03 — Standarkan seluruh URL sementara ke `axvara.pages.dev`, pindahkan canonical/sitemap/email admin, aktifkan MCP Worker + cron melalui `axvara-mcp.sailinnadia1.workers.dev` memakai Global API Key, dan sisakan `axvara.tech` hanya sebagai rencana custom domain — `src/{app,lib,middleware.ts}`, `mcp-worker/wrangler.toml`, `.env.example`, `next.config.mjs`, docs, tests — (verifikasi: vitest 51/51, build:pages 38 route pass, deploy Pages `90c537c1`, Worker version `fdfe8d96`, prod home/CSS/sitemap 200, Worker→Content API berhasil, dev login domain baru 200, pencarian source domain lama 0)

- 2026-09-03 — Aktifkan Remote MCP langsung di Pages `/mcp` (10 tool, Streamable HTTP), jadikan Worker vanity-domain/cron opsional, pasang `CRON_SECRET`, migrasikan+seed D1 remote 20 Draft, dan deploy produksi `e4cd7c22` — `src/app/mcp/route.ts`, `mcp-worker/`, `drizzle/{migrations,seed-articles.local.sql}`, `.env.example`, docs — (verifikasi: prod home/admin/CSS 200, MCP context→create→update→publish→artikel 200, cron 200, cleanup kembali 20 Draft/0 token; Worker dry-run pass, deploy Worker tertahan izin token Cloudflare Workers Scripts)

- 2026-09-03 — Audit dan perbaiki implementasi CMS admin: schema bootstrap idempotent, status publikasi sinkron, editor Tiptap Markdown+legacy JSON, renderer aman, R2 catch-all privat/publik, upload-only WebP, CRUD fallback satu route, kategori dinamis di modal produk, bukti pembayaran admin, stats kontekstual, sidebar responsif, scheduler, serta regresi agent/MCP — `drizzle/schema.sql`, `src/{app,components/admin,lib}`, `tests/cms-agent.regression.test.ts`, `README.md`, `docs/ARCHITECTURE.md` — (verifikasi: vitest 51/51, lint exit 0 dengan warning lama, build:pages pass 38 Edge routes, dev GET `/` 200 + CSS 200 + CRUD artikel/banner/kategori 200)

- 2026-09-03 — Naikkan Next.js ke patch keamanan 15.5.24, sinkronkan eslint config/Sharp aman, dan izinkan installer adapter Pages menerima peer patch 15.5.x — `package.json`, `package-lock.json`, `.npmrc` — (verifikasi: Next build pass, Cloudflare next-on-pages 1.13.16 build+deploy pass; npm audit production 0 critical, residual PostCSS build-time 1 high/1 moderate)

- 2026-09-02 — Fix overlap Artikel: 4 mini card & card tabel Artikel saling tumpang-tindih karena `mt` hilang saat refactor; fix dengan `mt-4 + mb-2` pada grid mini card & `mt-5` pada setiap card utama (Artikel, Kategori, Produk, Pesanan, Banner, Agent) agar gap konsisten — `src/components/admin/{ArticlesManager,CategoryManager,BannerManager,AgentIntegration}.tsx`, `src/app/admin/page.tsx` — (verifikasi: GET / 200, GET /admin?section=articles 200, CSS 200, tsc pass)

- 2026-09-02 — Fix Artikel: tambah pagination 8/hal (identik Produk) dengan reset `page=1` saat cari/filter + footer `Hal X dari Y • N artikel` + tombol `‹ Sebelumnya / Berikutnya ›` (chevron iOS) — `src/components/admin/ArticlesManager.tsx` — (verifikasi: GET / 200, GET /admin?section=articles 200, tsc pass, complied)

- 2026-09-02 — Seragamkan admin panel ke iOS Glyph + UX konsisten: ganti semua ikon unicode (◌□◇◈≡▧⌁⌕⌰↗●×‹›✕+) ke `IosIcon` (iOS 11 Glyph 4 ukuran), kategori `Ubah→Edit` pill putih + hapus ikon trash bulat merah, artikel/banner/orders/kategori diseragamkan ke layout `ax-glass rounded-[20px] overflow-hidden` + hover `white/[0.03]` + toolbar `search pill + filter pill`, semua tombol Edit/Hapus konsisten di seluruh laman (produk/kategori/artikel/banner/agent); download 19 ikon iOS baru via MCP Icons8 — `src/components/{admin/{AdminShell,CategoryManager,ArticlesManager,BannerManager,AgentIntegration,ImageDropzone},ui/IosIcon.tsx}`, `src/app/admin/page.tsx`, `public/icons/ios11/{dashboard,box,purchase-order,category,news,image,full-image,picture,bot,menu,external-link,exit,edit,link,settings,chat,checked,etc}-*.png` — (verifikasi: GET / 200, CSS 200, admin 200, tsc pass)

- 2026-09-02 — Stabilkan sesi admin development memakai `ADMIN_JWT_SECRET` lokal yang persisted dan ignored Git; restart Next tidak lagi membatalkan JWT admin lokal — `.env.local`, `.env.example` — (verifikasi: restart dev, login 200, cookie jar `GET /api/auth/me` authed true)

- 2026-09-02 — Lengkapi panel yang tersisa: CRUD kategori dan banner dengan media dropzone, thumbnail/lightbox/download bukti pembayaran privat, badge pesanan dipindah ke blok informasi, dan 20 artikel Draft idempotent untuk seed lokal — `src/components/admin/{CategoryManager,BannerManager,ProofThumbnail}.tsx`, `src/app/admin/page.tsx`, `drizzle/seed-articles.local.sql`, `README.md` — (verifikasi: tsc pass, vitest 39/39)

- 2026-09-02 — Koreksi CMS sesuai keputusan final: hapus input slug/excerpt artikel, tambah editor Tiptap JSON client-only dan renderer Markdown/JSON kompatibel, serta dropzone drag-and-drop yang mengoreksi orientasi, resize/crop 16:9, dan mengunggah byte WebP valid ke prefix R2 area; uploader produk ikut mengonversi WebP nyata dan category pills memuat D1 — `src/components/admin/{ArticleEditor,ImageDropzone}.tsx`, `src/{app/admin/page.tsx,app/api/upload/route.ts,app/artikel/[slug]/page.tsx,components/storefront/CategoryPills.tsx,lib/articles.ts}` — (verifikasi: tsc pass, vitest 39/39, lint pass dengan warning lama/non-blocking)

- 2026-09-02 — Tambah fondasi Admin CMS/Agent: schema editorial kompatibel dan migrasi, workflow artikel Markdown/idempotency/audit, Content API berscope, Worker MCP Streamable HTTP stateless, sidebar admin query-section, token sekali-tampil/revoke, kategori CRUD, bukti R2 catch-all privat, dan tautan grup WhatsApp baru — `drizzle/{schema.sql,migrations/0002_editorial_agent.sql}`, `src/{app/api,components/admin,lib}`, `mcp-worker/`, `docs/{ARCHITECTURE.md,README.md}`, `src/components/storefront/CommunityBar.tsx` — (verifikasi: dev GET `/` 200, CSS 200, `/admin?section=agent` 200; tsc pass, vitest 39/39, build pass)

- 2026-09-02 — Crosscheck dan hardening optimisasi storefront: pulihkan hydration/orbit lokal dengan CSP `unsafe-eval` khusus development (production tetap ketat), satu orbit berbasis refs dengan auto-rotate 30 fps/interaksi refresh-rate penuh dan pause saat offscreen, Spotlight/ScrollRope event-driven, SVG logo lokal, glass-card tanpa blur, gambar WebP responsif, refresh D1/banner idle dengan abort cleanup, cache Edge publik yang terpisah dari respons admin, dan skala orbit mobile — `src/middleware.ts`, `src/app/{page.tsx,globals.css,api/{products,categories,banners}/route.ts}`, `src/components/{storefront,ui}`, `public/{_headers,icons}`, `tests/performance.regression.test.ts`, `README.md`, `docs/ARCHITECTURE.md` — (verifikasi: Chrome lokal orbit bergerak + 12 logo termuat + 0 exception, offscreen orbit berhenti/TaskDuration 0,017 detik per 5 detik, filter 24→3 dan keranjang responsif, API cache header sesuai, tsc pass, lint exit 0, vitest 39/39, build pass, GET / 200, CSS 200)
- 2026-09-02 — Download 10 Simple Icons brand SVGs with brand colors to public/icons/: openai (#412991), anthropic (#D4A27F), googlegemini (#4285F4), netflix (#E50914), youtube (#FF0000), nordvpn (#4687FF), perplexity (#1FB8CD), notion (#000000), adobe (#FF0000), spotify (#1DB954). 8 from CDN, 2 (openai/adobe) manual path data — `public/icons/*.svg` — (verifikasi: all 10 files valid SVG with correct fill colors)
- 2026-09-02 — Security hardening 11 fixes: **DARURAT** F-01 password rotasi ke PBKDF2 random 24-char (set di CF Pages Variables, hapus semua hash dari source), F-02 migrasi PBKDF2 100k iter, F-03 CSP hapus unsafe-eval, F-04 CSRF Origin validation di middleware, F-05 hapus x-forwarded-for spoofable (prioritas cf-connecting-ip only), F-06 hapus hardcoded dev secret (runtime-generated), F-07 proof upload anti-abuse (require origin/referer), F-10 order code regex 8-char strict, F-11 CSP hapus blob: admin, F-12 CI guard commit message, F-14 hapus kredensial dari README — `src/lib/auth.ts`, `src/lib/security.ts`, `src/middleware.ts`, `src/lib/rateLimit.ts`, `src/app/api/{auth/login,orders,orders/[code],proof/upload,admin/orders/[code]}/route.ts`, `.github/workflows/ci.yml`, `README.md`, `tests/security.regression.test.ts` — (verifikasi: vitest 37/37 pass, build clean, admin login PBKDF2 di CF Pages, no hardcoded hashes in source)
- 2026-09-02 — Fix deploy 404 + CI/CD auto deploy: root cause deploy pakai `wrangler pages deploy .next` (salah) → harus `@cloudflare/next-on-pages` build lalu deploy `.vercel/output/static`; revert BUG-15 (runtime edge WAJIB untuk CF Pages); tambah `build:pages` + `deploy` script di package.json; tambah `pages_build_output_dir` di wrangler.json; CI/CD workflow auto test → build → deploy ke CF Pages saat push main (pakai secrets CLOUDFLARE_API_TOKEN + CLOUDFLARE_ACCOUNT_ID) — `wrangler.json`, `package.json`, `.github/workflows/ci.yml`, `src/app/produk/[slug]/page.tsx`, `src/app/pesanan/[code]/page.tsx`, `tests/security.regression.test.ts` — (verifikasi: vitest 36/36 pass, next-on-pages build OK 25 edge routes, deploy `b253de21` all routes 200, admin login OK, produk detail 200)
- 2026-09-02 — Regression tests update: 17→36 tests (19 baru cover 16 bug fixes) + deploy `f35b8e7c` — BUG-01 stock restore, BUG-02 pesanan no early-return, BUG-03 admin_note modal, BUG-04 proof_url required, BUG-05 no duplicate generateOrderCode, BUG-06 bank placeholder block, BUG-07 rate limit 5, BUG-09 enum dedup, BUG-10 sitemap domain, BUG-11 sold/stock null-safe, BUG-12 CommunityBar live, BUG-13 proof ext, BUG-14 login email, BUG-15 no runtime edge, BUG-16 slideInRight — `tests/security.regression.test.ts` — (verifikasi: vitest 36/36 pass, build EXIT:0, deploy `f35b8e7c.axvara.pages.dev`)
- 2026-09-02 — Deep Bug Audit: fix 16 bugs (3 Critical, 4 High, 5 Medium, 4 Low) — **CRITICAL**: (1) stok dikembalikan saat admin batalkan pesanan (stock restore on cancel), (2) halaman pesanan selalu fetch status terbaru dari server (bukan cuma localStorage stale), (3) admin modal input lisensi/key saat konfirmasi lunas (PRD AC-3); **HIGH**: (4) proof_url wajib di POST /api/orders (sebelumnya optional → bypass), (5) hapus duplicate generateOrderCode weak di utils.ts (re-export dari security.ts crypto-safe), (6) block checkout ke bank placeholder belum aktif (BCA/Mandiri/BRI/BNI), (7) login rate limit 12→5 per menit (sesuai ARCHITECTURE spec); **MEDIUM**: (9) Zod enum dedup di orders schema, (10) sitemap domain configurable SITE_URL bukan hardcoded pages.dev, (11) product detail sold/stock conditional fix (JavaScript truthiness bug), (12) CommunityBar WA link live ke admin (bukan dead href="#"), (13) proof upload extension sesuai tipe asli (bukan selalu .webp); **LOW**: (14) login response return email, (15) hapus runtime edge dari client pages (no-op), (16) CartDrawer animasi slideInRight bukan fadeInUp — `src/app/api/admin/orders/[code]/route.ts`, `src/app/pesanan/[code]/page.tsx`, `src/app/admin/page.tsx`, `src/app/api/orders/route.ts`, `src/lib/utils.ts`, `src/app/checkout/page.tsx`, `src/app/api/auth/login/route.ts`, `src/app/sitemap.ts`, `src/app/produk/[slug]/page.tsx`, `src/components/storefront/CommunityBar.tsx`, `src/app/api/proof/upload/route.ts`, `src/components/storefront/CartDrawer.tsx`, `tailwind.config.ts` — (verifikasi: dev GET / 200, CSS 200, all routes 200, vitest 17/17 pass, POST /api/orders tanpa proof_url → 400 rejected)

- 2026-09-02 — Fix 7 temuan (regex lockout, open redirect, PBKDF2, stock, PII, 500): admin regex 8-char, banner // reject, hash PBKDF2 100k + compat, stok atomik decrement, PII mask WA/email, 500 generic — `src/app/api/admin/orders/[code]/route.ts`, `src/app/api/banners/*`, `src/lib/auth.ts`, `src/app/api/orders/route.ts`, `src/lib/db.ts` — (verifikasi: admin PATCH 8-char 200, banner // 400, login ok, prod stock 48->47, PII masked, 500 generic, build pass)
- 2026-09-02 — CI GitHub Actions: `npm ci` → `npm test` (17) → `npm run build` + guard `.cf-credentials` leak — `.github/workflows/ci.yml` — (verifikasi: `npm test` 17/17 pass, `npm run build` EXIT:0)
- 2026-09-02 — Regression tests 5 temuan: vitest 4.1.11 (17 tests pass) — code 8 hex, rateLimit 20, idle cookie __Host- check, .env sanitized, hash dev!=prod, stock agregasi, proof magic bytes + bukti/ — `tests/security.regression.test.ts`, `src/lib/security.ts`, `vitest.config.ts` — (verifikasi: `npm test` 17/17 pass, `npm run build` EXIT:0)
- 2026-09-02 — Fix 5 temuan sequential thinking: code 8 hex (6B) + rateLimit 20/m pada lookup order, requireAdmin cek idle 2h (admin tanpa idle 401), DEV fallback hash rotasi & .env.example sanitize (hapus #Kecitran123 leak), POST orders agregasi qty per product (cagak stock duplicate), PROOF upload baru POST /api/proof/upload (magic bytes, 5MB, bukti/ private) + checkout wajib proofUrl — `src/app/api/orders/{route,[code]/route}.ts`, `src/app/api/proof/upload/route.ts`, `src/lib/auth.ts`, `src/lib/db.ts`, `src/app/checkout/page.tsx`, `.env.example` — (verifikasi: code 8 char, stock 11>9 reject, admin no-idle 401, .env 0, SVG reject, prod `7ac6fd09` OK, build pass)
- 2026-09-02 — Sesi admin 2 jam idle + 8 jam absolut: JWT 8h (__Host- + Secure), idle cookie 2h sliding via /api/auth/refresh tiap 90s + on focus/visibility, /api/auth/me cek idle → 401 idle_timeout, logout hapus keduanya — `src/lib/auth.ts`, `src/app/api/auth/{login,logout,me,refresh}/route.ts`, `src/app/admin/page.tsx` — (verifikasi: dev 2 cookie 28800/7200, me tanpa idle 401 idle_timeout, prod `23514802` same, build pass)
- 2026-09-02 — Hardening + Artikel + Banner + SEO: fail-closed auth (requireEnv), __Host- cookie + x-forwarded-proto Secure, orders D1 server-authoritative (POST /api/orders validasi zod + hitung subtotal DB + clamp qty 1-20 + Cek stok/is_active + crypto random AXV-, GET /api/orders/[code]), admin orders PATCH state machine pending→lunas/dibatalkan, middleware CSP/HSTS/X-Frame/DENY + Permissions-Policy, R2 bukti private (/api/admin/bukti auth + no-store), upload ketat (jpeg/png/webp only + magic bytes FF D8/PNG/WebP + 5MB + SVG reject + random 16B hex name), rateLimit shared 20/m products, public is_active=1 only, image URL allowlist CDN-only, cart max20 clamp, pagination B02 reset on q/cat, B01 ?buy fetch D1, footer rewrite premium editorial, cart shake restore, admin hilangkan redundan (Masuk sebagai + Kembali hapus, single header), artikel CRUD + /artikel + /artikel/[slug] minimal 65ch + PopupBanner 1.5s sessionStorage + sitemap.xml/robots.txt/JSON-LD + SEO-GEO-PLAN staged — `src/lib/auth.ts`, `src/lib/rateLimit.ts`, `src/middleware.ts`, `src/lib/db.ts`, `drizzle/schema.sql`, `src/app/api/{orders,admin/orders,buki,articles,banners,products,upload}`, `src/app/{artikel,checkout,pesanan,page,r2,robots,sitemap}`, `src/components/storefront/{Navbar,AdminNavbar,Footer,PopupBanner}`, `src/stores/cart.ts`, `docs/SEO-GEO-PLAN.md` — (verifikasi: build pass `EXIT:0` middleware 34.5kB, dev 9/9 gates pass, prod `4127ca05` POST /api/orders 201 AXV- valid, headers CSP+HSTS+Deny, deploy `92f64583` sitemap 200 robots 200)
- 2026-09-02 — Ganti kredensial admin ke `#Kecitran123` (SHA256 `3e3812f3daeb315a0ac17a094bffce7d67ff7c391f5e852cc9373d33ac38adbc`), update fallback hash & example env — `src/lib/auth.ts`, `.env.example` — (verifikasi: build pass, dev login `axvara123` → 401, `#Kecitran123` → 200 Set-Cookie HttpOnly + GET /api/auth/me 200)
- 2026-09-01 — Overhaul AXVARA full: (1) hapus dummy & kapsul D1+R2 ("Data produk di D1…", "D1+R2", "Demo: admin@…") dari admin; (2-4) seriuskan admin & keamanan industri: login edge-safe HS256 JWT HttpOnly SameSite=Lax Secure 8h (WebCrypto SHA-256, tanpa bcrypt di edge), proteksi API POST/PUT/DELETE /api/products + /api/upload wajib JWT, Zod validation ketat (slug regex, price min, url allowlist /r2/ & https), rate-limit login 12/menit/IP, error presisi; (5) loading animation di semua titik: RouteLoading top bar, spinner di login/admin save/upload/delete, skeleton shimmer katalog & detail, overlay memuat produk; (6-7) error handling & validation message menyeluruh: inline field error di checkout (nama/WA/email regex), form admin validate sebelum submit + toast, API return 400/401/409 dengan pesan Indonesia, banner "Coba lagi" di katalog/detail/admin; (8) tombol hapus produk kini merah solid bg-red-500 + ikon trash SVG jelas + ConfirmDialog bahaya (tidak bisa salah klik); (9) pagination admin 8/hal (prev/next + "Hal x dari y"); (10) navbar storefront (katalog/search/keranjang drawer) & footer otomatis hilang di /admin, diganti AdminNavbar khusus (logo AXVARA + tab Produk/Pesanan + Live + Keluar) — `src/lib/auth.ts`, `src/app/api/auth/{login,me,logout}/route.ts`, `src/app/api/products/{route,[id]/route}.ts`, `src/app/api/upload/route.ts`, `src/app/admin/page.tsx`, `src/app/layout.tsx`, `src/app/page.tsx`, `src/app/produk/[slug]/page.tsx`, `src/app/checkout/page.tsx`, `src/components/storefront/{Navbar,Footer,CartDrawer,AdminNavbar}.tsx`, `src/components/ui/{Loading,Toast,ConfirmDialog,RouteLoading}.tsx`, `src/app/globals.css`, `.env.example`, `package.json` — (verifikasi: build pass `npm run build` 0 error, dev `GET / 200` `CSS 200` `GET /admin 200` `GET /api/products 200` 24 produk, auth flow POST /api/auth/login 200→set-cookie HttpOnly, GET /api/auth/me 401→200 dengan cookie, POST /api/products tanpa auth 401, validasi 400, create valid id:25 lalu delete 401→200, dummy text 0, delete button class bg-red-500 ok, admin pagination rendered, navbar null di /admin, footer null di /admin, AdminNavbar "Live" muncul)
- 2026-09-01 — Foto produk CMS sekarang tampil di storefront: root cause halaman detail hanya render `product.image` (1 foto), bukan `product.images[]` array dari D1; fix tambah gallery thumbnail strip clickable di `produk/[slug]/page.tsx` — main image + thumbnail 60×46px border cyan aktif, klik ganti gambar utama; hooks dipindah ke atas early return (rules-of-hooks) — `src/app/produk/[slug]/page.tsx` — (verifikasi: prod 3 thumbnail muncul, klik thumb ke-2 main berubah ke `/r2/*.webp`, Pages build pass, deploy `d2e893b1` success)
- 2026-09-01 — Fix toggle Aktif admin: Playwright analisis menemukan 2 akar masalah (1) `db-edge.ts` + `runtime:edge` bikin isolasi memori antar route sehingga PUT→GET tidak persist, (2) hover CSS `hover:brightness` kepurge Tailwind; fix: gabungkan db.ts jadi edge-safe D1 prod + shared-mem dev tanpa `better-sqlite3`/`fs` import, toggle button `<button>` native class `toggle-btn` + CSS `.toggle-btn:hover !important` di globals.css biar hover+scale nyala; Pages build pass `npx @cloudflare/next-on-pages` 7 Edge Routes + 8 Prerendered — `src/lib/db.ts`, `src/app/api/{products,categories}/*.ts`, `src/app/admin/page.tsx`, `src/app/globals.css` — (verifikasi: lokal PUT false→GET false + PUT true→GET true + Playwright klik true→false `23 aktif` + hover `brightness(1.18) scale(1.06)`; prod HOME 200, ADMIN 200, API 24, CATEGORIES 4, FAVICON 200, PUT toggle false→count 23→true→count 24 persist D1)
- 2026-09-01 — Production Cloudflare Pages berhasil fullstack: Next 15.5.2 + `@cloudflare/next-on-pages 1.13.16` menghasilkan `_worker.js`/7 Edge Functions, D1 `axvara-db` dan R2 `axvara-assets` binding aktif; storefront & detail fetch katalog D1 live dengan fallback seed; favicon Prism putih midnight — `wrangler.json`, `src/app/api/*`, `src/app/r2/[key]`, `src/app/{page,produk}/`, `src/app/layout.tsx`, `public/favicon.svg`, `package.json` — (verifikasi prod: `HOME 200`, `ADMIN 200`, `DETAIL 200`, `CHECKOUT 200`, `FAVICON 200`, API 24 produk, kategori 4, deployment `5dfc8ba9` success)
- 2026-08-31 — Revisi massal UI + D1 CMS: favicon fix ke AXVARA prism (bukan WA), placeholder `Link grup & bot menyusul` dihapus, footer `© 2026 AXVARA` tanpa Brotherstore06, CommunityBar mobile jadi 2 kolom flex-row `Grup WhatsApp | Bot Telegram` simpel, card tambah `terjual` tipis + `Lihat detail →`, logo AXVARA prism pasang di tab `icon.svg + layout icons`, D1 `axvara-db c21453bf` create + seed 24 produk, `public/uploads` auto WebP, admin CMS overhaul (tabel+modal enable R2 noted) — `src/app/layout.tsx`, `src/app/icon.svg`, `src/components/storefront/CommunityBar.tsx`, `src/components/storefront/Footer.tsx`, `src/components/storefront/ProductCard.tsx`, `src/lib/products.ts`, `src/lib/db.ts`, `src/app/api/products/*`, `src/app/api/upload/route.ts`, `drizzle/schema.sql`, `wrangler.toml`, `data/axvara.db` — (verifikasi: `GET / 200`, `CSS 200`, `ADMIN 200`, placeholder 0, `© 2026 AXVARA` ok, `terjual 1` `Lihat detail 1`, `icon.svg 200`, D1 24 produk ok, dev OK)
- 2026-08-31 — Ganti logo WA ke SVG resmi baru `#fff bubble #40c351 + telp putih` — `public/brand/whatsapp.svg 48px` ganti `whatsapp.png`, circle `bg-white p-0.5` biar border presisi — `src/components/storefront/CommunityBar.tsx`, `src/components/storefront/Footer.tsx` — (verifikasi: `GET / 200`, `CSS 200`, `WA SVG 200`, `TG SVG 200`, dev OK)
- 2026-08-31 — Pasang logo resmi WA & Telegram valid — `public/brand/whatsapp.png 1024px 69KB` (hijau bulat ikon chat), `public/brand/telegram.svg 1000x1000` gradient `#2AABEE→#229ED9` + path putih resmi; CommunityBar & footer pakai `<img src=/brand/...>` cover — `src/components/storefront/CommunityBar.tsx`, `src/components/storefront/Footer.tsx` — (verifikasi: `GET / 200`, `CSS 200`, `WA 200`, `TG 200`, dev OK)
- 2026-08-31 — Fungsikan search navbar + Community Bar placeholder WA/Telegram — `useSearch` Zustand, filter `name/desc/category` live, X clear, scroll ke katalog; strip `Grup WhatsApp AXVARA + Bot Auto Order Telegram` di bawah hero + footer — `src/stores/search.ts`, `src/components/storefront/Navbar.tsx`, `src/components/storefront/CommunityBar.tsx`, `src/app/page.tsx`, `src/components/storefront/Footer.tsx` — (verifikasi: `GET / 200`, `CSS 200`, wa/tg bar ok, dev OK)
- 2026-08-31 — Hero subheadline Opsi 1 premium hemat — `Berbagai tools AI dan aplikasi premium dengan harga jauh lebih hemat dari official. Bergaransi.` hapus Aktivasi manual & garansi full — `src/app/page.tsx` — (verifikasi: `GET / 200`, `CSS 200`, old text 0, dev OK)
- 2026-08-31 — Produk serupa compact peek 2.1 card di HP — `w-[156px] sm:w-[300px] gap-3` + varian `compact` tanpa deskripsi/tombol, `156px` peek ke-3, judul `11.5px` harga `13px` — `src/app/produk/[slug]/page.tsx`, `src/components/storefront/ProductCard.tsx` — (verifikasi: `GET /produk/claude 200`, cards 8, width 156 ok, dev OK)
- 2026-08-31 — Katalog mobile 2 kolom (Opsi A) + compact card premium — `grid-cols-2 lg:grid-cols-4 gap-3`, card `rounded 16/22px m-1/1.5 h8/9 text 10-14px`, badge kecil HP — `src/app/page.tsx`, `src/components/storefront/ProductCard.tsx` — (verifikasi: `GET / 200`, `CSS 200`, grid-cols-2 ok, dev OK)
- 2026-08-31 — Produk serupa scrollable di detail — 8 card kategori sama dulu lalu lain, `overflow-x-auto snap-x 280px`, `Beli Langsung` direct `?buy` — `src/app/produk/[slug]/page.tsx` — (verifikasi: `GET /produk/spotify 200`, serupa 1, cards 8, scrollable ok, dev OK)
- 2026-08-31 — Kembali ke midnight polos #070a1e (hapus super light grain+aurora — tetap berat) — `src/app/globals.css`, `src/app/layout.tsx` — (verifikasi: `GET / 200`, `CSS 200`, `ax-bg 0`, dev OK)
- 2026-08-31 — Super light: grain 2.2% + 1 soft aurora 900×600 blur 48px drift 60s — tanpa jaring/bintang/meteor, paling ringan — `src/app/globals.css`, `src/app/layout.tsx` — (verifikasi: `GET / 200`, `CSS 200`, `ax-bg 3`, dev OK)
- 2026-08-31 — Rollback background ke midnight polos (tanpa jaring/galaxy) — berat di device; backup B di `/tmp/globals.opsi-b.bak` — `src/app/globals.css`, `src/app/layout.tsx` — (verifikasi: `GET / 200`, `CSS 200`, `ax-bg 0`, dev OK)
- 2026-08-31 — [Opsi B] Galaxy: nebula 4-wash + 3-layer stars twinkle 7/10/12s + 3 meteor 14/18/22s ekor 160-260px, drift 48s; backup A di `/tmp/globals.opsi-a.bak` — `src/app/globals.css`, `src/app/layout.tsx` — (verifikasi: `GET / 200`, `CSS 200`, `ax-bg-nebula/stars/meteor` in HTML, dev OK)
- 2026-08-31 — [Opsi A] Jaring besar 140px + node cyan + sapuan cyan 22s memencar (delay 9s gold) — ganti dot-grid 28px; aurora pelan 36/42s; backup `/tmp/globals.living-bg.bak` — `src/app/globals.css`, `src/app/layout.tsx` — (verifikasi: `GET / 200`, `CSS 200`, `ax-bg-net/sweep` in HTML, dev OK)
- 2026-08-31 — Living background: grain 3% + grid dot 28px radial fade + 3 aurora blob (cyan/gold/indigo) drift 38-52s + vignette, `--ax-bg-intensity` toggle — `src/app/globals.css`, `src/app/layout.tsx` — (verifikasi: `GET / 200`, `CSS 200`, `ax-bg` in HTML, aurora 1, dev OK)
- 2026-08-31 — Hero tanpa kapsul + headline dominan `42/56/62px 700 -0.045em 0.88`, `pt-10/14 pb-4`, gap katalog `pt-2` biar tidak kosong — `src/app/page.tsx` — (verifikasi: `GET / 200`, `CSS 200`, hero OK, dev OK)
- 2026-08-31 — Hapus kapsul hero `AXVARA • 24 PRODUK KURASI • SUPPORT` — hero langsung headline `Satu tempat untuk…`, lebih lega di mobile tanpa wrap — `src/app/page.tsx` — (verifikasi: `GET / 200`, `CSS 200`, `24 PRODUK KURASI 0`, hero OK, dev OK)
- 2026-08-31 — Checkout default null: 3 menu bersih `QRIS / E-WALLET / TRANSFER BANK` tanpa bocor nomor/QR; detail (QR image / nomor e-wallet / accordion bank + guide) hanya render setelah `method` dipilih, validasi `method`+`bankKey` sebelum submit — `src/app/checkout/page.tsx` — (verifikasi: `GET /checkout 200`, `GET /checkout?buy=chatgpt-plus-1-bulan 200`, HTML awal `axvara-qris.jpg 0`, label 3 OK, dev OK)
- 2026-08-31 — Wire Beli Langsung direct `?buy=slug` isolate tanpa pollute cart (keranjang checkout tetap bawa semua) — `src/components/storefront/ProductCard.tsx`, `src/app/checkout/page.tsx` — (verifikasi: `GET / 200`, `CSS 200`, `GET /checkout?buy 200`, dev OK)

- 2026-08-31 — Hero Opsi A editorial anti-slop: kapsul `AXVARA • 24 PRODUK KURASI • SUPPORT` tanpa dot/em-dash, headline `Satu tempat untuk semua tools premium.`, CTA `Lihat Katalog` + `Hubungi Admin`, social proof `1.200+ • 8 menit • 4.9/5`, restart dev 3000 — `src/app/page.tsx`, `src/app/layout.tsx` — (verifikasi: `GET / 200`, slop `Gerbang/Bayar 10 detik/Jelajahi 0`, new headline+CTA 1, dev OK)
- 2026-08-31 — Footer berisi: 4 kolom (brand+badges, Katalog, Bantuan, Info Promo WA form), hapus nomor bank/QRIS & nav Cara Bayar, `use client` fix — `src/components/storefront/Footer.tsx` — (verifikasi: `GET / 200` di 3000/3002, `bank leak 0`, `Cara Bayar 0`, `promo 1`, dev OK)
- 2026-08-31 — Tune Spotlight: radius kecil (700→420, aura 560→310), gelap (~45% opasitas), no delay (lerp 0.085 → snap langsung) — `src/components/ui/Spotlight.tsx` — (verifikasi: `GET / 200`, `CSS 200`, dev OK)
- 2026-08-31 — Spotlight cursor: cahaya tipis lembut ngikutin mouse (lerp 0.085 rAF, 3 layer radial white/cyan, blur 8–18px, `pointer:fine` only, fade on leave) — `src/components/ui/Spotlight.tsx`, `src/app/layout.tsx` — (verifikasi: `GET / 200`, `CSS 200`, spotlight in HTML 1, dev OK)
- 2026-08-31 — Fix kapsul & tombol: kapsul aktif icon jadi `tint black` (tidak hilang di `bg-white`), tombol Checkout icon `tint black` di biru `#00E5FF`, Navbar Keranjang icon `tint black` di biru, Cart `add()` tanpa `drawerOpen:true` (hanya shake+count) — `src/components/ui/IosIcon.tsx`, `src/components/storefront/CategoryPills.tsx`, `ProductCard.tsx`, `Navbar.tsx`, `src/stores/cart.ts` — (verifikasi: `GET / 200`, `CSS 200`, `ios refs ~24`, `drawerOpen 0` di HTML, dev OK)
- 2026-08-31 — [TRY] CartDrawer + pagination Chevron → iOS glyph (rollback ready `/tmp/*.lucide.bak`): `close/minus/plus/trash` 32px + `chevron-left/right` 32px — `src/components/storefront/CartDrawer.tsx`, `src/app/page.tsx`, `public/icons/ios11/{close,trash,minus,plus,chevron-left,right}-*.png` — (verifikasi: `GET / 200`, `CSS 200`, `GET /icons/ios11/close-32.png 200`, `lucide 0` di HTML, dev OK; rollback: `cp /tmp/CartDrawer.lucide.bak ...` + `cp /tmp/page.lucide.bak ...`)
- 2026-08-31 — Orbit: ganti CapCut scissor & Canva Iconify → SVG resmi file-based tanpa 404; CapCut ribbon hitam di white sphere, Canva C gradien ungu di white circle — `public/icons/capcut.svg`, `public/icons/canva.svg`, `src/components/storefront/OrbitHero.tsx` — (verifikasi: `GET / 200`, `CSS 200`, `GET /icons/capcut.svg 200`, `GET /icons/canva.svg 200`, dev OK)
- 2026-08-31 — Migrasi lucide → Icons8 iOS 11 Glyph: `CategoryPills`, `ProductCard`, `Navbar`, `checkout`, `produk/[slug]`, `pesanan/[code]` + wrapper `IosIcon.tsx` + `88` PNG — `src/components/ui/IosIcon.tsx`, `src/components/storefront/CategoryPills.tsx`, `Navbar.tsx`, `ProductCard.tsx`, `src/app/{page,checkout,produk,pesanan}/*`, `public/icons/ios11/*.png`, `~/.config/opencode/{opencode.json,mcp.json}` (icons8 MCP) — (verifikasi: `GET / 200`, `CSS 200`, `GET /icons/ios11/search-32.png 200`, `prism 1`, `ios refs ~15`, dev OK)
- 2026-08-31 — Rollback Ribbon Möbius → Prism (pilihan final) — restore `Navbar.tsx`/`Footer.tsx`/`OrbitHero.tsx` dari `/tmp/*.prism.bak`, `axvara-mark/logo.svg` dari `*-prism.svg` — `src/components/storefront/Navbar.tsx`, `Footer.tsx`, `OrbitHero.tsx`, `public/brand/axvara-mark.svg` — (verifikasi: `GET / 200`, `CSS 200`, `prism 5`, `ribbon 0`, `VAULT 0`, dev OK)
- 2026-08-31 — [TRY] Ganti Prism → Ribbon Möbius (trial, rollback ready) di Navbar/Footer/Orbit — backup Prism ke `axvara-mark-prism.svg`+`/tmp/*.prism.bak`, crop ribbon jadi PNG transparan 64/192/512 — `public/brand/axvara-ribbon*.png`, `src/components/storefront/Navbar.tsx`, `Footer.tsx`, `OrbitHero.tsx` — (verifikasi: `GET / 200`, `CSS 200`, `ribbon refs 6`, `prism 0`, dev OK; rollback: `cp /tmp/*.prism.bak src/...` + `cp public/brand/axvara-mark-prism.svg public/brand/axvara-mark.svg`)
- 2026-08-31 — Orbit hero: ganti matahari oranye → Prism AXVARA midnight glass + halo cyan (sinkron Navbar), hapus teks `VAULT` dari orbit & Navbar — `src/components/storefront/OrbitHero.tsx`, `src/components/storefront/Navbar.tsx` — (verifikasi: `GET / 200`, `CSS 200`, `VAULT count 0`, `Prism 5 paths`, dev OK)
- 2026-08-31 — Sinkron peraturan project: `axvara/AGENTS.md` khusus scope axvara (tidak ubah `~/AGENTS.md` global) + `axvara/CHANGELOG.md` wajib tiap ubahan + `README.md` update aturan dev-only & brand Prism — `axvara/AGENTS.md`, `axvara/CHANGELOG.md`, `README.md` — (verifikasi: `GET / 200`, `CSS 200 45632 bytes`, `M60 4 L6.5 104` OK, `~/AGENTS.md` untouched, dev `Ready`)
- 2026-08-31 — Pasang logo Prism wireframe pilihan (Vercel × vault) — vektor SVG grid-perfect ganti kotak `A` gradient, sinkron Navbar & Footer, tambah `public/brand/axvara-mark.svg` + `axvara-logo.svg` — `public/brand/axvara-mark.svg`, `public/brand/axvara-logo.svg`, `src/components/storefront/Navbar.tsx`, `src/components/storefront/Footer.tsx` — (verifikasi: `GET / 200`, `CSS 200`, `build pass 9.62kB`, `M60 4 L6.5 104` di HTML)
- 2026-08-31 — Perbaiki CSS 404 & dev 500 (`Missing script: dev` karena `npm run dev` dari `/Users/macbookair` bukan `axvara/`, ENOENT vendor-chunks) — `rm -rf .next`, start dengan `workdir: axvara` — `src/app/globals.css`, `.next`, `/tmp/axvara-dev.log` — (verifikasi: `GET / 200`, `GET /_next/static/css/app/layout.css 200`, `Compiled / in ~1.6s`)
- 2026-08-31 — Generate 10 alternatif logo AXVARA via Muse Image (studi OpenAI/Perplexity/Claude/Vercel/Gemini) — `~/.opencode/muse-images/muse-image-*.png` — (verifikasi: 10 PNG 753KB, 5+5 batch)
- 2026-08-31 — Upload & pasang QRIS Brotherstore06 hi-res (NMID ID1022191087959 A01) ke checkout & homepage — `public/qris/axvara-qris.jpg` (104KB), `.png`, `src/app/checkout/page.tsx` — (verifikasi: `GET /qris/axvara-qris.jpg 200`, `GET /checkout 200`)
- 2026-08-31 — Implementasi homepage katalog pagination `PER_PAGE=8`, orbit 3D hero (perspective 900px, 4 rings, draggable+wheel), tali scroll, micro-interaction keranjang — `src/app/page.tsx`, `src/components/storefront/OrbitHero.tsx`, `ScrollRope.tsx`, `Navbar.tsx`, `ProductCard.tsx` — (verifikasi: `GET / 200`, `build 9.5kB`)
- 2026-08-31 — Scaffold Next.js 14.2.33 + Tailwind + Zustand + D1/R2 schema + seed 24 produk + checkout/admin — `package.json`, `tailwind.config.ts`, `next.config.mjs`, `drizzle/schema.sql`, `wrangler.toml`, `src/lib/products.ts`, `src/stores/cart.ts`, `src/app/*`, `src/components/storefront/*` — (verifikasi: `npm run build` ✓ `First Load JS 87.3kB`)
- 2026-08-31 — Dokumen fondasi PRD/DESIGN/ARCHITECTURE/VPS-RESEARCH — `docs/PRD.md`, `docs/DESIGN.md`, `docs/ARCHITECTURE.md`, `docs/VPS-RESEARCH.md`, `README.md` — (verifikasi: docs 264/235/341/143 baris)
